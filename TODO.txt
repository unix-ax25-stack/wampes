- tun/tap device: /dev/tun testen (Linux)
  Recherche macOS (2026-08): aktuelle Implementierung (src/tun.c) ist
  FreeBSD-Stil von 2006 und auf heutigen Macs funktionslos. Sie oeffnet
  /dev/tun0..N (Zeile 194) - das gab es nur mit der tuntaposx Kext, die
  seit 2015 ungepflegt ist und ab Big Sur (SIP/Notarisierung) bzw. auf
  Apple Silicon gar nicht mehr laedt. macOS hat seit 10.6.4 eingebaute
  "utun"-Interfaces, aber KEIN Char-Device: Man oeffnet einen
  Kernel-Control-Socket socket(PF_SYSTEM, SOCK_DGRAM, SYSPROTO_CONTROL),
  ioctl(CTLIOCGINFO, "com.apple.net.utun_control"), connect(ctl_id,
  unit+1) und liest/schreibt dann IP-Pakete direkt auf dem Socket.
  So machen es openconnect/tinc/scapy. IP-Konfiguration am besten via
  ifconfig utunN (SIOCSIFADDR reicht teils nicht). TAP gibt es auf macOS
  NICHT eingebaut, nur tun/utun (tap bräuchte NetworkExtension).
  Der vorhandene tun_packet-Header (AF_INET + data, tun.c:48) passt
  schon gut fuer utun. TODO: utun-Backend implementieren (spaeter).
- IPIP (src/ipip.c) auch via IPv6 - der letzte Rest der Umstellung. Erledigt
  sind AXIP/AXUDP und alles, was ueber build_sockaddr() geht: der Dienstsocket
  samt seiner tcp:-Ziele (2026-08-14 mit tcp:[::1]:9099 durchgefahren),
  netcmd und Port 8010, tcpgate, bridge, cnet. ipip.c kennt AF_INET6 gar nicht.
  Ebenfalls noch v4-only, aber je eigene Baustelle: src/ni.c, die versteckten
  TCP-Verbindungen in src/n8250.c und src/tun.c. Nicht gemeint sind domain.c
  und krnlif.c - das ist WAMPES' eigener IPv4-Stack.
- AGWPE-Server-Protokoll: durch den Dienstsocket ueberholt, aber nicht
  entschieden begraben. Beschlossen ist "das AGWPE-Protokoll wird nicht
  erweitert", und der Weg ueber sockets/ax25 (connect/datagram/listen, ein
  Socket je Session) leistet dasselbe ohne aus einer echten Verbindung eine
  virtuelle zu machen. Ein AGWPE-Server in WAMPES lohnt nur, falls AGWPE
  irgendwann doch die dauerhafte Grenze wird. Siehe AGWPE-BEWERTUNG.md.
- DAMA master und slave Implementierung
  xnet macht das so: https://packet-radio.net/wp-content/uploads/2017/04/xnet138.pdf
  https://linux-ax25.in-berlin.de/wiki/DAMA - inux implements DAMA slave support but not DAMA master mode. Hilft bei Suchen in den kernel sourcen wie dama slave implementiertist.

  https://github.com/kq6up/tnn/blob/04986f2d1264646fefdf20231e78de5ddd43541b/src/l2dama.c#L64

  Nachgesehen 2026-08-15, zur Einordnung des Aufwands:

  DA IST SCHON: der DAMA-Bit wird in beiden Richtungen korrekt behandelt -
  ntohax25 liest ihn vom Draht (ax25hdr.c:151, active low), htonax25 schreibt
  ihn zurueck (:72), ax25dump zeigt " [DAMA]" (:41). Es HANDELT nur niemand
  danach. Wieder das Muster dieses Baums: Vorkehrung da, nicht angewandt.

  ES IST EINE ZUSTANDSMASCHINE, und zwar an anderer Stelle als die von 6pack.
  Der Kanalzugriff von 6pack liegt UNTER AX.25: er verzoegert einen Rahmen,
  den die Schicht darueber laengst abgeschickt hat, T1 laeuft weiter, und der
  Verzug sieht aus wie ein langsamer Kanal. DAMA greift IN die Zeitsteuerung
  von AX.25: ein Slave darf nur senden, wenn er gepollt wurde, also muss die
  T1-Wiederholung unterdrueckt werden - ein Slave, der unaufgefordert
  wiederholt, ist genau das, was DAMA verhindern soll. Beim Poll gibt er alles
  Anstehende hinaus und schweigt dann. Das aendert, wann lapb_output() laufen
  darf, und das ist die heikelste Datei im Baum.

  ASYMMETRIE: Slave ist die kleinere Haelfte und Linux hat sie (Quelltext zum
  Nachlesen, siehe der Wiki-Link oben). Fuer den Master gibt es im Kernel
  nichts - nur XNETs Beschreibung und TNNs l2dama.c -, und er ist das
  groessere Stueck: Stationsliste, Pollzyklus, Fairness, Zeitgrenzen fuer
  Stationen die nicht mehr antworten.

  DIE GEFAEHRLICHSTE STELLE (Thomas, 2026-08-15): kommt T1 wieder zum Tragen,
  wenn der Master aufhoert zu pollen? Ein Slave, der T1 aussetzt, weil er auf
  den Poll wartet, hat sein eigenes Wiederherstellungsmittel abgeschaltet.
  Faellt der Poll aus - verlorener Rahmen, Station aus der Liste gefallen,
  Master weg -, wartet er endlos auf etwas, das nie kommt, und nichts weckt
  ihn: das Weckmittel ist ja gerade das stillgelegte. Die Verbindung stirbt
  still.
  Es braucht also eine WACHHUNDZEIT auf das Gepolltwerden, getrennt von T1:
  bleibt der Poll zu lange aus, gilt DAMA als gescheitert und der Slave faellt
  in den normalen Betrieb zurueck, womit T1 wieder greift und die Verbindung
  sich selbst repariert. Wieder eintreten, sobald wieder DAMA-markierte Rahmen
  kommen.
  ZU RECHERCHIEREN: sagt die Spezifikation das, oder ueberlaesst sie es den
  Implementierungen? Schweigt sie, waehlt jede Implementierung ihre eigene
  Frist - und das merkt man erst an echten Digis.

  ZWEITER WEG, verhindern statt erholen (Thomas): ab und zu proaktiv senden,
  damit die Verbindung nicht einschlaeft und vom Master aufgeraeumt wird.
  Nicht wesensfremd - beim BETRETEN des Kanals sendet ein Slave ohnehin
  unaufgefordert, sonst erfuehre der Master nie von ihm, und ein Wiedereintritt
  ist derselbe Vorgang. Es ist also eine Frage der Haeufigkeit, nicht der
  Erlaubnis. Hilft im engen Fall, dass der Master STILLE Stationen ausraeumt,
  obwohl sie noch da sind. Und dafuer gibt es in AX.25 schon ein Mittel: T3,
  den Leerlauf-Poll - die Frage waere nur, ob dessen Poll auf den Sendeschlitz
  wartet oder ihn erzwingen darf.
  ENTSCHIEDEN (Thomas, 2026-08-15): BEIDES, und der Wiedereintritt ist teuer.
  Nicht der AX.25-Aufbau kostet - der ist in Sekunden erledigt -, sondern
  ALLES DARUEBER. Sein Beispiel: mit der Mailbox verbunden, ein Artikel ueber
  30 Paketlaengen im Lesen, die Verbindung schlaeft ein und wird aufgeraeumt.
  Danach neu anmelden (unaufgefordert, "ich bin neu und will sprechen"),
  Verbindung aufbauen, Mailbox connecten, Begruessung, Login, "read" - und
  dieselben 30 Pakete noch einmal durch die Luft. Ein paar Minuten Sendezeit
  gegen ein gelegentliches "mich gibt es noch, ich habe noch Daten".
  Der Sitzungszustand ueber der Verbindung ist das, was verloren geht, und den
  kann keine Schicht darunter wiederherstellen.
  Also: der Wachhund bleibt die Notbremse - er ist der einzige Ausweg ohne
  Zutun der Gegenseite -, und das Auffrischen kommt dazu, wenn das Protokoll
  es zulaesst. Zu recherchieren bleibt nur noch, OB es zulaessig ist und in
  welchem Schlitz.

  JE PORT, und zwar wirklich je Port (Thomas, 2026-08-15): das eine Interface
  kann Master sein, das naechste Slave, das dritte ohne DAMA. Der Zustand
  gehoert also an struct iface und nirgends global - Rolle, Pollzyklus,
  Stationsliste beim Master, "bin ich gerade gepollt" beim Slave. Nicht fuer
  axip; das ist eine Sache der HF-Ports.

  ZUSTAND AN ZWEI ORTEN, nicht an einem (Thomas, 2026-08-16): Timer je
  INTERFACE (Pollzyklus, Stationsliste, Rolle) UND je SESSION (bin ich
  gerade gepollt, wann war der letzte Poll fuer diese Verbindung). Das ist
  der Grund, warum es mittelkomplex ist und nicht klein: es fasst struct
  iface und die Verbindungszustandsmaschine gleichzeitig an.

  DER TOLERANZSCHALTER, und er gehoert zur MASTER-Rolle (Thomas,
  2026-08-16): manche DAMA-Master erzwingen DAMA - wer nicht mitmacht,
  kommt nicht zum Zug -, andere lassen CSMA-Stationen daneben laufen. Sind
  WIR Master, brauchen wir die Wahl. Die Spezifikation steht auf der
  toleranten Seite ("All protocol elements keep their original meaning
  which allows both versions to be utilized on the same frequency", DK4EG
  1989, siehe unten), Erzwingen ist also eine Implementierungsentscheidung
  jenseits des Papiers - und genau deshalb ein Schalter und keine Vorgabe.

  SCHREIBWEISE (Thomas, 2026-08-16), in WAMPES-Form also am ifconfig:
      ifconfig <if> dama off | master | master-enforce | slave
  Drei Rollen plus aus, und "enforce" ist die harte Master-Variante.

  DER SLAVE IST FERTIG (2026-08-17), Commits f89b9ef, 2f38778, 8fd2fb7,
  beschrieben in doc/DAMA-SLAVE.md. Gegen Simulatoren gemessen, NICHT gegen
  ein echtes Geraet - drei Stellen, an denen zwischen Papier und Praxis zu
  entscheiden war und die am echten Master zu pruefen sind: markiert er nur
  den Aufbau oder jeden Rahmen, ist sein Poll wirklich ein Kommando mit P,
  und nimmt er unsere zurueckgehaltene Quittung auf der Pollantwort an.
  Zwei Fehler haben erst Thomas' Testwuensche gefunden - das Fenster gehoert
  der Station und nicht der Verbindung (Digipeat- und Multiconnect-Fall),
  und ein Slave, der nur schweigt, wiederholt NIE etwas (30% Verlust).
  DER SLAVE IST GEBAUT (2026-08-17), siehe doc/DAMA-SLAVE.md. Gebaut sind:
      ifconfig <if> dama off | slave
      ifconfig <if> damatimeout <sekunden>      Vorgabe 120
  Zwei Schluessel statt eines mit optionalem zweitem Wort, weil ifconfig
  seine Zeile als PAARE liest (iface.c, "for(i=2;i<argc-1;i+=2)") - ein
  drittes Wort wird stillschweigend der naechste Schluessel. Gefunden,
  indem "dama slave 5" geschrieben wurde und die 5 nirgends ankam.
  "dama master" antwortet mit einem Hinweis statt mit "unbekannt".

  KEINE KANALGRUPPIERUNG BEIM SLAVE (Thomas, 2026-08-17, und er hat recht):
  das Zusammenfassen von Ports zu einem DAMA-Kanal ist Sache des MASTERS -
  nur der hat einen Pollzyklus, und nur er muss den Uplink ueber Ports
  serialisieren, die sich eine Frequenz teilen. Ein Slave antwortet auf
  Polls, die an ihn adressiert sind, auf dem Port, auf dem sie ankommen,
  und es ist ihm gleich, wie der Master sich organisiert. Die Notiz weiter
  unten ("die Rolle haengt am KANAL") gilt also nur fuer den Master.

  AUS DER TNN-DOKUMENTATION GEZOGEN (TNN179mh05.pdf, 2026-08-16) - TNN ist
  reiner MASTER, einen Slave kennt es nicht; den hat Linux. Die beiden
  Quellen ergaenzen sich also.

  * TNN IST DIE UNTOLERANTE SEITE, und zwar abgestuft: "Stationen, die
    keine DAMA-faehige Usersoftware benutzen und OHNE Aufforderung durch
    den DAMA-Master senden, werden IM QSO verwarnt. Sind mehr Verwarnungen
    noetig, als unter DAMA-MaxPol angegeben, so wird der User
    disconnected." Der Zaehler ist ein Parameter: par 6, Vorgabe 5,
    "Maximale Verstoesse gegen DAMA Protokoll dann Disconnect". Unser
    "master-enforce" ist damit kein Schalter, sondern eine ZAHL - und die
    Verwarnung geht als Text IN die Verbindung.
  * Weitere Parameter: par 4 DAMA-Speedfaktor 4800, par 5 DAMA-MaxPri 10,
    par 7 = 50 ms "Wartezeit wenn ein User nicht antwortet". Die 50 ms
    passen NICHT zu DK4EGs "etwa 1/2 Sekunde" Antwortfrist - vor dem Bauen
    klaeren, worauf sich welche Zahl bezieht, nicht eine davon abschreiben.
  * "Alle anderen Benutzer sollten unbedingt FRACK auf den Maximalwert
    einstellen." Das ist die Gegenseite unserer T1-Frage: auf einem
    DAMA-Kanal ist die T1-Wiederholung einer CSMA-Station das Problem, und
    TNNs Rat an die Nutzer ist, T1 gross zu machen.
  * EIN DAMA-KANAL IST NICHT EIN PORT. TNN fasst mehrere Ports zu einem
    DAMA-Kanal zusammen (1..16): "DAMA-Kanal 1: Port 0 = 1k2 auf 70cm,
    Port 1 = 4k8 auf 70cm, Port 2 = 9k6 auf 70cm". Multibaud auf DERSELBEN
    Frequenz, also derselbe Funkkanal - der Pollzyklus muss geteilt werden.
    Thomas' Gegenlesart (2026-08-16): vielleicht haben sie nur alle
    DAMA-Ports in EINE Funktion gepackt, statt mehrere Timer parallel
    laufen zu lassen. Dagegen spricht die Doku selbst - "Fuer jede
    EINSTIEGSFREQUENZ wird ein DAMA-Kanal verwendet", und die Beispiele
    gruppieren nach Band. Waere es Bequemlichkeit, gaebe es EINE
    DAMA-Funktion und nicht sechzehn nummerierte Kanaele.
    Und unabhaengig von ihrer Absicht muss es so sein: DAMA regelt, wer
    auf einem FUNKKANAL sendet. Drei Modems auf einer Frequenz mit je
    eigenem Pollzyklus erteilen drei Sendeerlaubnisse auf dieselbe Luft -
    genau die Kollision, die DAMA verhindern soll.
    FUER DIE KONFIGURATION aendert das wenig, und da hat Thomas recht: es
    gibt Ports mit und ohne DAMA, und solange niemand Multibaud auf einer
    Frequenz faehrt, sind "je Port" und "je Kanal" dasselbe. Vorschlag,
    der beides traegt:
        ifconfig <if> dama off | master | master-enforce | slave
        ifconfig <if> dama channel <n>      optional, Vorgabe: eigener
    Ohne "channel" ist jedes Interface sein eigener Kanal. Der ZUSTAND -
    Pollzyklus, Stationsliste - haengt am Kanal, und ein Kanal hat im
    Normalfall genau ein Interface.

  DER BEFUND, DER DEN MASTER TEUER MACHT: TNN braucht vom TNC eine
  Rueckmeldung, WANN ein Rahmen tatsaechlich hinausgegangen ist. Woertlich:
  "Im Port-TNC, auf dem DAMA laufen soll, muss das DAMA-Bit im EPROM
  gesetzt sein. Bei gesetzten DAMA-Bit sendet der TNC an den Knotenrechner
  die Information zurueck, wenn ein Paket an einen User abgesendet wurde.
  Diese Information wird fuer die Steuerung der DAMA-Funktion benoetigt."
  Und entsprechend: "Bei der Linux-Version funktioniert DAMA NUR mit
  Tokenring oder Vanessa!"
  KISS GIBT DAS NICHT HER. Wir wissen, wann wir einen Rahmen in den TNC
  geschoben haben, nicht wann er auf der Luft war - dazwischen liegen
  TxDelay, Warteschlange und Kanalzugriff des TNC. Fuer den MASTER ist das
  die zentrale offene Frage; fuer den SLAVE stellt sie sich nicht, der
  reagiert nur auf empfangene Polls.
  GEPRUEFT 2026-08-16, und die Antwort ist differenzierter als ja/nein:
  6PACK TRAEGT DEN SENDEZUSTAND, aber keine Sendequittung je Rahmen.
  Im Prioritaetskommandobyte stehen drei Zustandsbits, gegengeprueft am
  Linux-Treiber drivers/net/hamradio/6pack.c (v6.6), und sie stimmen mit
  unserem sixpack.h ueberein:
      SIXP_PRIO_CMD_MASK  0x80     dies ist ein Prioritaetskommando
      SIXP_PRIO_DATA_MASK 0x38     ... und dies sein Zustandsfeld
      SIXP_TX_MASK        0x20     TNC sendet
      SIXP_RX_MASK        0x10     TNC empfaengt
      SIXP_DCD_MASK       0x08     Traeger
  Ein "Rahmen ist raus" gibt es NICHT als eigene Meldung - das Ereignis ist
  der UEBERGANG des TX-Bits von gesetzt nach geloescht, und den muss der
  Host selbst bilden. Der Kernel-Treiber tut es nicht einmal; er zaehlt
  tx_packets aus dem Pufferzustand.
  Fuer DAMA reicht der Uebergang im Prinzip - er ist dieselbe Information,
  die TNNs DAMA-EPROM liefert, sogar mit Anfang UND Ende. Was daran haengt
  und NICHT aus dem Papier zu beantworten ist: wie schnell und wie
  zuverlaessig eine echte 6pack-Firmware diese Bytes schickt. Nur bei
  Aenderung? Verzoegert? Das entscheidet, ob ein Pollzyklus darauf gebaut
  werden kann, und es ist an einem ECHTEN Geraet zu messen - der Simulator
  in testtools/fake6pack.py beweist dazu nichts.

  UNSER CODE WIRFT ES HEUTE WEG: sixpack_command() nimmt nur das DCD-Bit
  (sixpack.c, "sp->dcd = (c & SIXP_STATE_DCD) != 0"), TX und RX werden
  gelesen und verworfen. Das Anschliessen ist klein - wieder das Muster
  dieses Baums, diesmal in unserem eigenen Code von gestern.

  KISS IST NICHT VOM TISCH (Thomas, 2026-08-16, und er hat recht - meine
  Folgerung "auf KISS nur schaetzbar" war zu schnell). Der Fall, in dem es
  traegt, ist VOLLDUPLEX, und das ist bei Einstiegen inzwischen der
  Normalfall: dann sind wir Herr auf unserer Sendefrequenz, der TNC muss
  auf nichts warten, und sein Kanalzugriff - der bei KISS das Unbekannte
  ist - faellt als Frage weg. Wir wissen, was wir in die Warteschlange
  gelegt haben und in welcher Reihenfolge; mit der HF-Bitrate laesst sich
  ausrechnen, wann ein Rahmen draussen ist, und die Pollzeiten bzw. die
  Sendezuweisungen an die Slaves lassen sich darauf abstimmen.

  WAS DAFUER FEHLT, nachgesehen 2026-08-16:
    * DIE HF-BITRATE IST NICHT BEKANNT. WAMPES kennt nur die serielle
      Geschwindigkeit (n8250.h, "long speed - Line speed in bits per
      second"); struct iface hat gar nichts dergleichen. 9600 Bd seriell
      zu 1200 Bd HF ist der Normalfall, die eine sagt ueber die andere
      nichts. Es braucht also eine eigene Angabe je Interface, etwa
          ifconfig <if> hfrate <bit/s>
      und ohne sie kann ein KISS-Master seine Zeiten nicht rechnen.
    * DIE WARTESCHLANGE MUSS FLACH BLEIBEN. Die Rechnung stimmt nur, wenn
      wir wissen, was vor unserem Poll noch im TNC liegt. Ein Rahmen in
      Arbeit, der naechste erst nach der errechneten Sendezeit - sonst
      addieren sich die Unbekannten. Auf einer 9600er Leitung zu einem
      1200er Funkport ist das ohnehin die richtige Betriebsart.
    * WAS AUCH DANN GESCHAETZT BLEIBT: die Eigenlatenz des TNC und ob er
      TxDelay genau einhaelt. Das ist je Geraet eine Konstante und einmal
      messbar - kein Grund, es nicht zu bauen, aber ein Grund, den Wert
      nachstellbar zu machen.
    * HALBDUPLEX BLEIBT DER SCHWIERIGE FALL: dort entscheidet der TNC,
      wann er sendet, und KISS sagt es uns nicht. 6pack schon, ueber
      Traeger UND Sendezustand.

  FOLGERUNG FUER DIE REIHENFOLGE: der SLAVE haengt an nichts davon - er
  reagiert auf empfangene Polls und braucht keine Sendequittung. Also
  Slave zuerst. Der MASTER geht auf Vollduplex-KISS mit bekannter
  HF-Bitrate und auf 6pack; auf HALBDUPLEX-KISS ist er der wacklige Fall,
  und dort gehoert "master-enforce" wahrscheinlich abgelehnt statt
  schlecht gemacht - eine Station zu disconnecten, weil sie unaufgefordert
  gesendet hat, waehrend wir selbst nicht wissen, wann unser Poll auf der
  Luft war, ist die falsche Art von Strenge.

  ------------------------------------------------------------------
  DIE PRIMAERQUELLE IST GELESEN (DK4EG 1989, sieben Seiten, 2026-08-16)
  ------------------------------------------------------------------
  Was sie beantwortet, was sie offenlaesst, und wo sie schweigt.

  BEANTWORTET:
  * AUS DER LISTE FLIEGEN: "If after a certain number of transmitted
    polls this station still has not answered, then it is considered to
    be unavailable by the node and is then dropped completely from the
    list." Die ZAHL nennt sie nicht - "a certain number". Ebenso beim
    Verbindungsaufbau vom Knoten aus: "If after a certain amount of
    tries no UA is received, the user is assumed to be inoperable."
  * DER LEERLAUF-RUECKZUG, und er ist verkehrsabhaengig: antwortet ein
    Nutzer nur mit RR#, wird die Zeit bis zum naechsten Poll verlaengert,
    und "the exact amount of time added is determined by total channel
    activity" - viel I-Verkehr anderer, laengere Pause fuer den Stillen.
    "This is the principle of the self-alignment mechanism of DAMA."
    Wer ein I-Frame schickt, bekommt seine Prioritaet zurueck.
  * DER MASTER QUITTIERT NICHT SOFORT: "the node will not acknowledge
    received frames the instant it hears them. Instead it will first
    service all the other uplinked stations and then come back with an
    RR# ... along with a poll." Das steht quer zu normalem T2-Verhalten
    und ist beim Bauen die unangenehmste Stelle im Master.
  * UNAUFGEFORDERT SENDEN darf der Slave nur beim VERBINDUNGSAUFBAU
    (SABM im CSMA-Verfahren). Trennen dagegen nicht: "When the user wants
    to disconnect from the node, he will wait to send his DISC-frame
    until polled by the master."
  * DAS DAMA-BIT ist "the dormant bit 5 of the master's SSID address
    field", und "DAMA test versions set this bit to 0" - ACTIVE LOW,
    genau wie ax25hdr.c es liest, und es ist das SSID-Feld des MASTERS.
    Der Master kuendigt also seine Faehigkeit an, der Nutzer erkennt sie.
  * TOLERANZ: "All protocol elements keep their original meaning which
    allows both versions to be utilized on the same frequency, yet
    throughput increases as more and more users switch over." Und: "it
    does not require everybody to change everything all at once." Die
    Spezifikation steht also EINDEUTIG auf der toleranten Seite;
    TNNs Disconnect nach N Verstoessen ist eine Zutat.
  * ZEITEN, und es sind nur drei: Antwortfrist "say around 1/2 second";
    T2/DWAIT der Nicht-DAMA-Nutzer "under 1 second"; und die
    Wiederholung nach einem unquittierten I-Frame, also T1/FRACK - das
    Papier umschreibt sie als "the time interval from when an I-frame is
    sent to when the TNC sends an RR# to ask for a pending ACK" und will
    sie "clearly higher than the time between two polls of the master
    (usually more than 30 seconds at 1200 baud)".
    ES IST T1 UND NICHT T3, und die Gegenprobe steht in der
    TNN-Dokumentation, die denselben Rat mit dem Parameternamen gibt:
    "Alle anderen Benutzer sollten unbedingt FRACK auf den Maximalwert
    einstellen." Zwei Quellen, ein Parameter. (T3 ist das ANDERE Thema -
    der Leerlauf-Poll gegen einschlafende Verbindungen, siehe "ZWEITER
    WEG, verhindern statt erholen" weiter oben. Am 2026-08-16 hatten wir
    die beiden kurz vermischt.)
    KEINE Zeit ist je Station angegeben - der Mechanismus ist
    beschrieben, die Zahlen sind Implementierungssache.

    UND DARAUS FAELLT DER WACHHUND AB, ohne einen neuen Zeitgeber. Die
    Bedingung "T1 groesser als der Pollabstand" heisst umgekehrt: ein
    FEUERNDES T1 im DAMA-Betrieb ist genau das Ereignis "es wurde zu
    lange nicht gepollt". Die Spezifikation beschreibt keinen Wachhund,
    aber sie stellt die Bedingung auf, die einen zum Nulltarif ergibt -
    und es ist genau Thomas' Skizze vom 2026-08-15: laeuft T1 ab, gilt
    DAMA als gescheitert, der Slave faellt in den normalen Betrieb
    zurueck, T1 greift wieder und die Verbindung repariert sich selbst.
    Statt sie abzuraeumen, wie der Linux-Kernel es tut.
    Zu klaeren bleibt damit nur noch, was der Slave mit T1 waehrend des
    DAMA-Betriebs ueberhaupt tut - siehe die offene Frage zum
    Linux-Kernel unten.

  SIE SCHWEIGT - und zwar genau bei Thomas' gefaehrlichster Stelle:
  * ZUM WACHHUND DES SLAVE steht NICHTS. Die Frage aus dieser Datei
    ("sagt die Spezifikation das, oder ueberlaesst sie es den
    Implementierungen?") ist damit beantwortet: sie ueberlaesst es ihnen.
    Das Papier sagt nicht einmal, dass der Slave T1 aussetzt - es sagt
    nur, dass er nicht SENDET, bevor er gepollt wurde.
  * ZUR SENDEQUITTUNG des TNC steht ebenfalls nichts. TNNs
    EPROM-Anforderung ist Implementierung, nicht Spezifikation.

  ------------------------------------------------------------------
  WAS DIE BEIDEN UMSETZUNGEN TUN (2026-08-16)
  ------------------------------------------------------------------
  LINUX-KERNEL, DAMA-SLAVE (net/ax25/ax25_ds_*.c, v6.6):
  * Der Zustand liegt GENAU SO, wie Thomas es vorgesehen hat: je Geraet
    (ax25_dev->dama) und je Verbindung (AX25_COND_DAMA_MODE in
    ax25_cb->condition).
  * ES GIBT DEN WACHHUND: AX25_VALUES_DS_TIMEOUT, heruntergezaehlt im
    Sekundentakt. ABER ER TUT DAS GEGENTEIL VON DEM, WAS WIR WOLLTEN.
    Laeuft er ab, schickt er allen DAMA-Verbindungen des Geraetes ein
    DISC, trennt sie mit ETIMEDOUT und schaltet DAMA am Geraet ab:
        ax25_send_control(ax25, AX25_DISC, AX25_POLLON, AX25_COMMAND);
        ax25_disconnect(ax25, ETIMEDOUT);
        ...
        ax25_dev_dama_off(ax25_dev);
    Kein Rueckfall auf CSMA, keine Rettung der Sitzungen - genau der
    teure Ausgang, den Thomas am 2026-08-15 vermeiden wollte ("dieselben
    30 Pakete noch einmal durch die Luft"). UNSERE ABWEICHUNG DAVON IST
    ALSO BEWUSST und gehoert begruendet dokumentiert: bei uns soll der
    Wachhund in den normalen Betrieb zurueckfallen, nicht abraeumen.
  * GEKLAERT, und das ist die Antwort auf die Frage, an der unser Entwurf
    hing: T1 WIRD NICHT AUSGESETZT - es laeuft weiter, aber SEIN ABLAUF
    IST STUMM. ax25_ds_t1_timeout(), Zustand 3:
        case AX25_STATE_3:
            if (ax25->n2count == ax25->n2) {
                ax25_send_control(ax25, AX25_DM, AX25_POLLON, AX25_RESPONSE);
                ax25_disconnect(ax25, ETIMEDOUT);
                return;
            } else {
                ax25->n2count++;
            }
            break;
        ...
        ax25_calculate_t1(ax25);
        ax25_start_t1timer(ax25);
    Kein ax25_requeue_frames(), kein ax25_kick(), keine Abfrage - anders
    als die Nicht-DAMA-Fassung. Nur der Zaehler steigt. Genau die Regel
    "ein Slave sendet nicht unaufgefordert", umgesetzt als: Zeitgeber
    laeuft, Handlung unterbleibt. Das ist eleganter als T1 anzuhalten,
    denn der Zaehler misst weiter, wie lange nicht gepollt wurde.
  * ZWEI SICHERUNGEN, BEIDE TOEDLICH: nach n2 stummen T1-Ablaeufen DM und
    Disconnect je Verbindung, dazu der geraeteweite DS_TIMEOUT, der alle
    DAMA-Verbindungen trennt und DAMA abschaltet. EIN RUECKFALL AUF CSMA
    EXISTIERT NIRGENDS.
    UNSER ENTWURF WEICHT GENAU HIER AB, und jetzt begruendet: statt beim
    n-ten stummen Ablauf zu sterben, fallen wir in den normalen Betrieb
    zurueck - dann greift T1 wieder mit seiner normalen Wirkung und die
    Sitzung repariert sich. Der Preis dafuer ist bekannt und beherrschbar:
    ist der Master doch noch da und nur langsam, senden wir unaufgefordert
    und handeln uns auf einem "enforce"-Knoten Verwarnungen ein. Also
    grosszuegige Schwelle, und Wiedereintritt in DAMA, sobald wieder ein
    DAMA-markierter Rahmen kommt.
  * GEKLAERT (2026-08-17, am TNN-Quelltext /tmp/foo/TheNetNode-CB):
    EIN POLL IST KOMMANDO MIT GESETZTEM P-BIT, und zwar innerhalb eines
    DAMA-markierten Rahmens. src/l2rx.c:

        #ifdef DAMASLAVE
            if (rxfDA)                          /* DAMA?                */
            {
              if (damaslave(rxfprt))            /* DAMA-Slave erlaubt?  */
              {
                portpar[rxfprt].damaok = 12000; /* 2 Minuten Timeout    */
                if (rxfPF && rxfCR)             /* wenn gepollt         */
                {
                  l2stma(stbl10a);              /* DAMA-Poll receive    */
                  portpar[rxfprt].sendok = 1;

    ZWEI UNABHAENGIGE UMSETZUNGEN, DIESELBE REGEL - Linux prueft
    "type == AX25_COMMAND && pf", TNN "rxfPF && rxfCR". Die Unschaerfe im
    Papier ist damit durch die Praxis entschieden.
  * UND DAMIT AUCH DIE GESCHAERFTE FRAGE: sendok wird NUR beim Poll
    gesetzt. Ein I-Frame mit P=0 erteilt keine Sendeerlaubnis.
    LINUX MACHT ES GENAUSO, nachgesehen in ax25_ds_in.c, Zustand 3:
        if (pf) {
            ax25_ds_enquiry_response(ax25);
        } else {
            if (!(ax25->condition & AX25_COND_ACK_PENDING)) {
                ax25->condition |= AX25_COND_ACK_PENDING;
                ax25_start_t2timer(ax25);
            }
        }
    Kein sofortiges RR, nur ein Quittungsvermerk, der beim naechsten Poll
    mitgeliefert wird. Zwei Umsetzungen, dieselbe Bauform.
    ACHTUNG BEIM NACHBAUEN, ungeklaert: Linux startet dort T2. Feuert T2
    im normalen AX.25, geht die Quittung hinaus - eine UNAUFGEFORDERTE
    Aussendung. Entweder unterdrueckt der DAMA-Pfad das, oder man
    verlaesst sich darauf, dass der Poll vorher kommt. Genau die Stelle,
    an der ein Slave versehentlich dazwischenfunkt und sich auf einem
    "enforce"-Master eine Verwarnung einfaengt. Bei UNS also bewusst
    entscheiden: entweder T2 im DAMA-Betrieb stummschalten wie T1, oder
    den Quittungsvermerk ausschliesslich beim Poll ausliefern. DER SATZ
    AUS DEM PAPIER - "The user will acknowledge I-frames immediately with
    an RR#" - WIRD VON KEINER DER BEIDEN UMSETZUNGEN BEFOLGT, und zwar aus
    genau dem Grund, den Thomas genannt hat: ein Master, der mehrere
    Stationen hintereinander bedient, will die Quittungen nicht dazwischen
    hoeren. Wir folgen der Praxis, nicht dem Papier.
  * KORREKTUR: das DAMA-Bit im SSID war NICHT als Kandidat auszuscheiden,
    wie ich am 2026-08-16 geschrieben hatte. "if (rxfDA)" ist die AEUSSERE
    Bedingung - nur DAMA-markierte Rahmen zaehlen ueberhaupt -, und die
    Erlaubnis ist P+Kommando INNERHALB davon. Zwei Ebenen, beide Bits
    gebraucht.
  * DER WACHHUND IST BEZIFFERT: damaok = 12000, also 2 MINUTEN,
    aufgefrischt von JEDEM DAMA-markierten Rahmen - nicht nur von Polls.
    Bei Ablauf sendok = 1, also senden statt trennen (l2dama.c).

  TNN, l2dama.c und l2rx.c. LIZENZLAGE GEAENDERT (2026-08-17): der Baum
  /tmp/foo/TheNetNode-CB hat ein LICENSE - ALAS, "Allgemeine Lizenz fuer
  Amateurfunk Software", NORD><LINK 1994, aus der GPL abgeleitet und mit
  dem ausdruecklichen Satz "Es ist aber jedem Nutzer freigestellt, an
  Stelle dieser Lizenz die GPL Bedingungen anzuwenden." DAMIT IST TNN NACH
  THOMAS' VORGABE ALS CODEQUELLE VERWENDBAR, nicht nur als
  Verhaltensreferenz. Die frueher notierte Einschraenkung galt kq6up/tnn,
  das gar keine Lizenzangabe hat.
  * Master, und Slave hinter #ifdef DAMASLAVE.
  * DER SPEEDFAKTOR IST DORT WIRKLICH EINE RECHNUNG:
        dsf = (DamaSpeedFactor / 100) / portpar[...].speed;  if (dsf<1) dsf=1;
        if (lnkpoi->zael < lnkpoi->anzl * dsf) lnkpoi->zael++;
    Die Doku dazu: bei DAMA-Speedf 9600 werden 1200-Baud-Nutzer "erst in
    jeder 8. Runde bedient", und "die korrekte Baudrate muss im
    Speed-Parameter fuer den jeweiligen Port richtig eingetragen sein".
    DAS BESTAETIGT ZWEIERLEI: die Portgeschwindigkeit IST bei TNN ein
    konfigurierter Wert je Port, und der Pollzyklus ist ueber Ports
    verschiedener Geschwindigkeit HINWEG geteilt - sonst gaebe es nichts
    zu gewichten. Die Kanal-Frage von weiter oben ist damit entschieden.
  * Leerlauf: damapc = 6000 (etwa ein Poll pro Minute) fuer Stationen
    ohne Aktivitaet, sonst damapm * 100. Zaehlerdeckel dama_max.
  * Unaufgefordertes Senden: pollcnt hoch, und bei MaxPollCnt
        "> DISCONNECT: Too many non-DAMA Polls (%u) !!\r"  dann dsclnk()
    Die Verwarnung geht also wirklich als Text IN die Verbindung.

  KORREKTUR AN MEINER EIGENEN AUSSAGE VON HEUTE: ich hatte aus der
  TNN-DOKUMENTATION geschlossen, ein Master brauche eine SENDEQUITTUNG
  ("wenn ein Paket an einen User abgesendet wurde"). Der Quelltext tut
  das nicht - er fragt den TRAEGER ab, iscd(port) -> DAMA_DCD, und
  plant erst weiter, wenn der Kanal frei ist. Gebraucht wird also
  TRAEGERERKENNUNG, nicht eine Quittung je Rahmen. Das aendert die Lage:
  6pack liefert genau das, KISS nicht - und Thomas' Vollduplex-Fall
  braucht es gar nicht, weil dort niemand sonst auf unserer
  Sendefrequenz ist. Beide Wege fuehren zum selben Schluss, von zwei
  Seiten.

  ------------------------------------------------------------------
  DAMA GIBT KEINE ZEITSCHLITZE VOR - und was daraus folgt (2026-08-16)
  ------------------------------------------------------------------
  Thomas' Modell war "der Master weist Zeitschlitze zu, und ein Slave
  koennte seinen verpassen". Das Papier beschreibt etwas anderes:
  sequenzielles Pollen mit einer ANTWORTFRIST, keinen Fahrplan. "If the
  user does not respond within a given time frame (say around 1/2 second)
  then the master ... passes permission to transmit to all other active
  stations and when completed comes back to the first user." Es gibt
  keine angekuendigte Slotgrenze, die jemand verpassen koennte.

  SEINE FOLGERUNG STIMMT TROTZDEM, mit anderer Begruendung. Liegen hinter
  dem Poll noch Rahmen in der Warteschlange, dann sendet der Master im
  HALBDUPLEX noch, waehrend der Slave antwortet - Kollision. Der Poll muss
  also das LETZTE sein, bevor der Master die Taste loslaesst. Dieselbe
  Anforderung an die Queuetiefe, nur ist der Grund Kollisionsvermeidung
  und nicht Slotverletzung. Eine verspaetete Antwort ist entsprechend
  nicht "ausserhalb ihres Slots", sondern kollidiert mit dem, was der
  Master als naechstes tut.

  EINE VEREINFACHUNG, die daraus faellt: der Master muss NICHT wissen,
  wann der Poll beim Slave ankommt - die Laufzeit ist vernachlaessigbar.
  Er muss wissen, wann SEINE EIGENE AUSSENDUNG ENDETE, denn dort beginnt
  die Frist. Zu modellieren ist nur der eigene Sender.

  WOHER DIE HALBE SEKUNDE KOMMT, nachgerechnet: ein RR sind rund 18 Byte
  = 144 Bit. Bei 1200 Bd 120 ms, dazu TxDelay des Slave von typisch
  250 ms = 370 ms; die 1/2 s ist also eine 1200-BAUD-ZAHL mit etwa 130 ms
  Luft. Bei 9600 Bd waeren es 15 ms + 100 ms = 115 ms, die Frist duerfte
  viermal kuerzer sein. DAS ist der eigentliche Grund, warum die HF-Rate
  ans Interface gehoert: damit die Antwortfrist mit der Geschwindigkeit
  skaliert, statt eine Konstante von 1989 zu bleiben.

  WIE TNN ES LOEST: gar nicht durch Rechnen. Es fragt den Traeger ab -
      if (iscd(port)) return (DAMA_DCD);      /* nicht busy(), iscd() ! */
      if (!dcd && (dama_timer[k] > 0)) ...
  dazu ein Kanaltimer (dama_timer[k] = dama_init) und
  pause_delay[k] = MIN_DELAY + dama_init, MIN_DELAY 3000. Keine
  Bitratenrechnung, kein Sendeende-Signal. Unsere Rechnung ist also der
  ERSATZ fuer DCD, und auf KISS ohne Vollduplex der einzige Weg.

  UND EIN NEBENBEFUND AUS DEMSELBEN QUELLTEXT, der die Wachhund-Frage
  entscheidet: TNNs Slave macht es GENAU ANDERSHERUM als der
  Linux-Kernel -
      if (damaslaveon(port))                /* DAMA-Slave aktiv?      */
        if (--portpar[port].damaok == 0)    /* Slave-Timeout abgelaufen? */
          portpar[port].sendok = 1;         /* ja - Reste senden      */
  Laeuft der Slave-Timeout ab, wird gesendet statt getrennt. Zwei
  Umsetzungen, zwei entgegengesetzte Antworten - und unsere geplante
  (Rueckfall statt Abbruch) ist die von TNN, nicht die von Linux.
  NICHT BEANTWORTBAR aus l2dama.c: WIE der Slave einen Poll erkennt.
  damaslaveon() ist dort nur gerufen, nicht definiert, und wo damaok
  gesetzt wird, liegt im L2-Empfangspfad. Dort weitersuchen.

  ------------------------------------------------------------------
  KISS UND DCD: der TNC WEISS es, er SAGT es nur nicht (Thomas, 2026-08-17)
  ------------------------------------------------------------------
  "Alle TNCs haben channel busy detection und gehen erst auf Sendung wenn
  Kanal ruhig." Das mildert die KISS-Bewertung erheblich, und zwar an der
  entscheidenden Stelle: was bei KISS fehlt, ist nicht die
  TRAEGERERKENNUNG, sondern nur unsere KENNTNIS davon. Der TNC handelt
  ohnehin danach.

  FOLGE FUER DEN SIMPLEX-MASTER: er kann nicht ueber die Antwort des Slave
  hinwegreden, weil sein TNC von sich aus wartet. Es gibt also KEINE
  Kollisionsgefahr aus unserer Unkenntnis. Der Preis ist ein anderer und
  kleinerer: unser Poll geht spaeter hinaus als berechnet, und wir merken
  nicht, wann die Antwort zu Ende war - also sitzen wir die Frist aus,
  statt sofort den naechsten zu pollen. Das kostet Durchsatz, nicht
  Korrektheit.
  FOLGE FUER DEN VOLLDUPLEX-MASTER: mit gesetztem FullDup sendet der TNC
  sofort, die Zeitrechnung wird vorhersagbar, und der Downlink darf
  waehrend der Wartezeit weiterlaufen (siehe naechster Abschnitt).

  DAMIT IST DIE FRUEHERE FORMULIERUNG "auf Halbduplex-KISS ist der Master
  der wacklige Fall" ZU STARK. Richtig ist: er ist der LANGSAMERE Fall.
  Ob "master-enforce" dort trotzdem abzulehnen ist, bleibt zu entscheiden -
  das Argument dafuer war die Ungewissheit ueber den eigenen Sendezeitpunkt,
  und die besteht weiter.

  ------------------------------------------------------------------
  VOLLDUPLEX-DAMA - ausserhalb des Papiers, und dort steckt der Gewinn
  (Thomas, 2026-08-17)
  ------------------------------------------------------------------
  DK4EG schreibt DAMA ausdruecklich als ALTERNATIVE zum Vollduplex: "In a
  full duplex system the hardware expense will normally be much higher and
  the system will occupy two frequencies but will only realize the maximum
  throughput of one. A better approach might be to increase the throughput
  by reducing the collisions on a SINGLE CHANNEL system rather than
  spreading the load onto two channels." Das Papier ist also ein reines
  Simplex-Papier; Vollduplex-DAMA kommt darin nicht vor.

  THOMAS' BEOBACHTUNG: das Warten auf das RR kostet die eigentliche Zeit,
  und im Vollduplex entsteht durch die gezielte Ansprecherei und Warterei
  effektiv wieder Halbduplex. Richtig - aber zwei Dinge, die im Simplex
  zusammenfallen, sind zu trennen:
    * DIE SERIALISIERUNG DES UPLINKS bleibt noetig. Das ist der ganze
      Zweck: die Nutzer hoeren einander nicht, also darf nur einer senden.
      B wird nicht gepollt, bevor A geantwortet hat oder seine Frist um
      ist.
    * DAS SCHWEIGEN DES DOWNLINKS ist reine Verschwendung. Im Simplex
      musste der Master die Taste loslassen, um hoeren zu koennen; im
      Vollduplex nicht. Er kann waehrend der Wartezeit Daten an andere
      schicken.
  Der Master wartet also weiter, bevor er DEN NAECHSTEN POLLT, schweigt
  dabei aber nicht mehr. Die 1/2 s wird von Totzeit zu Sendezeit. Bei 16
  Stationen und einer stillen Runde sind das acht Sekunden, die im Simplex
  verloren sind.

  KORREKTUR AN MEINER AUSSAGE VOM 2026-08-16: ich hatte gesagt, im
  Vollduplex brauche man die Traegererkennung gar nicht. Das gilt nur fuer
  UNSEREN SENDER - dort konkurriert niemand. Fuer die Gegenrichtung wird
  sie WICHTIGER: DCD auf der Empfangsfrequenz sagt, wann die Antwort von A
  zu Ende ist, und erst das erlaubt, B sofort zu pollen statt die Frist
  auszusitzen. Ohne DCD bleibt nur die Frist als Notbremse, und ein Teil
  des Gewinns ist wieder weg. 6pack ist also auch im Vollduplex im
  Vorteil, nur aus dem umgekehrten Grund als gedacht.

  ------------------------------------------------------------------
  DIE HF-BITRATE GEHOERT ANS INTERFACE, NICHT IN DIE DAMA-KONFIGURATION
  (Thomas, 2026-08-16)
  ------------------------------------------------------------------
  Sein Punkt: die Kenntnis der HF-Rate hilft auch dem AX.25-Retry-Timing,
  ist dort vielleicht besser aufgehoben, und "was zaehlt ist
  min(serielle Bitrate, HF-Bitrate)" - die langsamere der beiden ist der
  Engpass, und nur sie bestimmt die Laufzeit eines Rahmens.

  Nachgesehen, was WAMPES heute tut: T1 wird bereits GEMESSEN, nicht
  geraten - lapb.c fuehrt srt und mdev als Integratoren
  (axp->srt = ((axp->srt*7) + rtt + 4) >> 3) und setzt T1 = 4*mdev + srt.
  Der STARTWERT kommt aus T1init (5000 ms), skaliert nur mit der Zahl der
  Digipeater (lapb.c: mdev = (T1init * (1 + 2*ndigis) + 2)/4), NICHT mit
  der Geschwindigkeit. Daraus folgt sauber:
    * Fuer AX.25 verbessert die Rate nur den KALTSTART - nach dem ersten
      Umlauf misst WAMPES ohnehin besser, als jede Rechnung raten kann.
      5 s sind fuer 1200 Baud knapp und fuer 9600 grosszuegig; das ist
      der Gewinn, mehr nicht.
    * Fuer DAMA ist sie unentbehrlich, weil dort VORHERGESAGT werden muss
      statt gemessen: wann unser Poll draussen ist, laesst sich nicht im
      Nachhinein feststellen.
  Also eine Angabe an struct iface, nicht am DAMA-Teil, und beide
  benutzen sie:
      ifconfig <if> hfrate <bit/s>        0 = unbekannt, wie bisher
  und wo eine Laufzeit gebraucht wird, zaehlt min(serielle, hfrate).
  Zu bedenken: n8250 kennt die serielle Rate (n8250.h "long speed"),
  andere Interfacetypen nicht - die Bildung des Minimums muss damit
  umgehen koennen.

  AUS DEM XNET-HANDBUCH (xnet138.pdf, nachgelesen 2026-08-17 auf Thomas'
  Hinweis, dass sich eine der Dokumentationen zum Abschalten des Slave
  auslaesst - es war diese):

  * ABSCHNITT 4.3.14.2.2 "ds", und er bestaetigt unsere Vorgabe von einer
    Seite, an die ich nicht gedacht hatte: "Der Slave-Mode wird
    vollautomatisch beim Verbindungsaufbau zu einem Master aktiviert. BEI
    DIGIS IST DIESE AUTOMATISCHE AKTIVIERUNG DES SLAVE-MODES NICHT
    ERWUENSCHT. Sie kann deshalb mit Hilfe des 'pa tnc ds' - Parameters
    ausgeschaltet werden. Wer (X)NET als TNC-User-Software einsetzt und auf
    einem DAMA-Einstieg arbeitet muss diesen Parameter einschalten!"
    Der Parameter heisst dort "ds", beschrieben als "allow DAMA slave
    mode", Vorgabe 1. Bei uns ist "ifconfig <if> dama slave" dieselbe
    Erlaubnis, und die Vorgabe "off" ist damit die richtige: ein Knoten,
    der selbst Infrastruktur ist, soll nicht durch fremde DAMA-Bits in die
    Slave-Rolle gedraengt werden.
    FEINHEIT, die wir anders lesen: XNET aktiviert "beim Verbindungsaufbau
    ZU einem Master", also wenn ES anruft. Wir rasten bei jedem markierten
    Rahmen ein, auch wenn wir angerufen werden. Thomas' Entscheidung vom
    2026-08-17 macht das unerheblich - siehe unten.
  * EINE DAMA-RUNDE UEBER MEHRERE PORTS: "Eine DAMA-Runde kann sich ueber
    mehrere Ports erstrecken", bis zu vier unabhaengige Runden. Damit ist
    die Kanalfrage aus drei unabhaengigen Quellen bestaetigt (DK4EG dem
    Sinn nach, TNN, XNET) - und weiterhin eine reine MASTER-Frage.
  * ZAHLEN FUER DEN TNC, die den 6pack/CSMA-Punkt weiter oben bestaetigen:
    "Fuer den korrekten Ablauf des DAMA-Modes muss auf den entsprechenden
    Ports die PERSISTENZ AUF 255 und die SLOTTIME AUF 1 gestellt werden!"
  * VIER MASTER-PARAMETER, fuer spaeter: Connectslot Periode, Connectslot
    Dauer, Pollwait, Txdpri. Der CONNECTSLOT ist der interessante - "eine
    Wartezeit, in der der Digi keine User pollt, um einem User den Einstieg
    (den Connect) zu erlauben". Das ist die Vorkehrung des Masters dafuer,
    dass ueberhaupt jemand neu hereinkommt, und im Papier von DK4EG kommt
    sie nicht vor. Beim Bau des Masters zu beruecksichtigen.

  FUER DEN MASTERBAU VORGEMERKT (Thomas, 2026-08-17): SIND WIR MASTER,
  DUERFEN WIR NICHT ZUM SLAVE WERDEN, nur weil uns jemand mit gesetztem
  DAMA-Bit connected. Die Sperre gibt es im Code schon - dama_heard_frame()
  kehrt zurueck, wenn ifp->dama nicht DAMA_SLAVE ist -, aber bisher nur als
  Nebenwirkung davon, dass es keine dritte Rolle gibt. Am 2026-08-17 als
  tragende Bedingung kommentiert, damit sie einen "Aufraeumer" ueberlebt.
  Es ist dieselbe Begruendung, die XNET fuer seinen ds-Parameter angibt:
  ein Master ist ein Digi, und "bei Digis ist diese automatische
  Aktivierung des Slave-Modes nicht erwuenscht".
  WAS ein Master mit einem markierten Rahmen tun soll, ist eine andere
  Frage und gehoert in die Masterhaelfte: die Markierung heisst "diese
  Station spricht DAMA", er darf sie also unter die Pollordnung nehmen.
  Auskunft, kein Rollenwechsel.

  UNMARKIERTER SABM AUF EINEM SLAVE-PORT - GEBAUT (Thomas, 2026-08-17):
  ruft uns jemand ohne DAMA-Bit an, arbeitet diese Verbindung im
  nicht-DAMA-Stack. Die Disziplin haengt jetzt am LINK statt am Port, was
  ohnehin die Lesart des Papiers ist ("This state would then remain in
  effect until disconnect") und die des Kernels (AX25_COND_DAMA_MODE im
  Kontrollblock). Beseitigt zugleich die Pathologie, dass eine Verbindung
  ohne jeden Bezug zum Master im Wachhundtakt zappelt.
  ZU WISSEN: unsere EIGENE Markierung bleibt auf allen Links des Ports -
  sie sagt "wir sprechen DAMA", nicht "diese Verbindung ist DAMA". Wer das
  Bit liest, haelt uns also fuer den Master. Ein DAMA-faehiger Nutzer-TNC,
  der uns anruft, sperrt sich selbst und wartet auf Polls, die nie kommen.
  Auf einem DAMA-Kanal ist das die gewollte Folge (siehe unten), aber
  "dama slave" auf einem Port heisst damit auch "Direktverbindungen sind
  hier nicht vorgesehen".

  FUER DEN MASTERBAU, ZWEITER PUNKT (Thomas, 2026-08-17): WIE GEHEN WIR MIT
  EINEM SABM OHNE DAMA-BIT UM, wenn WIR der Master sind? Vermutlich wie
  TNN: ablehnen. Ausser wir sind "lazy" konfiguriert - dann empfehlen wir
  DAMA, erzwingen es aber nicht. Das ist dieselbe Achse wie
  "master" gegen "master-enforce" weiter oben, hier von der anderen Seite
  betrachtet: enforce entscheidet nicht nur, was mit einem Verstoss im
  laufenden QSO geschieht, sondern schon, WER ueberhaupt hereinkommt.
  Zu bedenken beim Bauen: TNNs MaxPollCnt-Weg verwarnt IM QSO und trennt
  erst danach - das ist milder als ein abgelehnter Verbindungsaufbau und
  fuer den Nutzer verstaendlicher, weil er einen Text bekommt. Ein
  abgewiesenes SABM sagt ihm gar nichts.

  ABER: IM QSO VERWARNEN GEHT BEI UNS NICHT (Thomas, 2026-08-17), und das
  ist ein Architekturunterschied, kein Umsetzungsdetail. TNN und XNET
  BESITZEN die Benutzeroberflaeche - dort haengt der Nutzer im Knotenmenue
  und hangelt sich weiter, ein eingeschobener Text landet also da, wo er
  gelesen wird. Bei uns ist die Verbindung, in die wir schreiben wuerden,
  irgendetwas: ein Login, eine Passwortabfrage, eine Mailbox, ein Programm
  hinter "listen" - oder sie verwirft den Text ganz. Und beim VIA-CONNECT
  haben wir im Nutzinhalt ueberhaupt nichts verloren; ein eingeschobener
  Text geriete dort in einen fremden Datenstrom.

  THOMAS' VORSCHLAG, und er ist der einzige Weg, der niemandes Verbindung
  anfasst: als UI an QST senden, mit dem Rufzeichen des Nutzers benannt -
      "Warning: use DAMA client protocol"
  UI ist dafuer auch protokollseitig sauber: die Spezifikation nimmt
  Datagramme ausdruecklich aus der Pollordnung heraus (siehe
  doc/DAMA-SLAVE.md), ein UI stoert die Runde also nicht.

  Beim Bauen zu bedenken:
    * RATENBEGRENZUNG, sonst wird die Warnung selbst zur Stoerung. Nicht
      eine je Verstoss - TNN zaehlt bis MaxPollCnt -, sondern hoechstens
      eine je Station und Zeitraum.
    * QST ODER AN DAS RUFZEICHEN DES NUTZERS? An QST sehen es alle: auf
      einem DAMA-Kanal stoert ein unaufgefordert sendender Nutzer ohnehin
      alle, und oeffentlich hingewiesen wird im Amateurfunk ueblicherweise
      auch. An sein Rufzeichen adressiert ist leiser, aber viele TNCs
      zeigen UI nur bei eingeschaltetem Monitor - und ein Nutzer im
      verbundenen Betrieb sieht es dann womoeglich gar nicht. Beides hat
      dieselbe Schwaeche: wir wissen nicht, ob er es liest.
    * NUR ALS MASTER. Ein Slave verwarnt niemanden; die Kanalordnung ist
      nicht seine Sache.
    * BEI "lazy" IST DIE WARNUNG ALLES, bei "enforce" geht sie dem Trennen
      voraus.

  DIE GEGENRICHTUNG, und sie ist der eigentliche Gewinn (Thomas,
  2026-08-17): eine Warnung nuetzt nur, wenn die Gegenseite etwas damit
  anfangen kann. Ist so eine Meldung EINDEUTIG, koennte der EMPFAENGER das
  UI auswerten und weiterreichen - auf die Konsole, und bei einer
  Verbindung ueber den Dienstsocket als
      *** Disconnected (use DAMA)
  Die Form gibt es dort schon: remote_net.c liest Zeilen, bis eine mit
  "***" beginnt, und axserver.c schreibt sie ("*** incoming %s %s",
  "*** %s is not answering"). Ein Nutzer erfuehre damit, WARUM seine
  Verbindung gescheitert ist, statt nur DASS.
  ZUERST NACHSEHEN, WAS DER RMNC TUT (Thomas): vielleicht sendet er so
  etwas ohnehin als Broadcast. Danach richtet sich, worauf man ueberhaupt
  passen kann.
  VORBEHALT, der dazugehoert: freien Text zu erkennen ist wackelig - es
  haengt am genauen Wortlaut dessen, was der jeweilige Knoten sendet, und
  ein Fehlalarm ist billig zu erzeugen, weil jeder so ein UI senden kann.
  Also hoechstens als Bequemlichkeit fuer den Betreiber, nie als etwas,
  worauf sich Code verlaesst.

  ZWEI SLAVES, DIE SICH DIREKT VERBINDEN - ENTSCHIEDEN (Thomas, 2026-08-17):
  Weil wir unsere eigenen Rahmen markieren, halten zwei WAMPES-Slaves
  einander fuer den Master und sperren sich gegenseitig aus, bis der
  Wachhund ablaeuft; danach setzen sie sich sofort wieder gegenseitig in
  den DAMA-Modus. Ich hielt das fuer einen Fehler und wollte einen Schalter
  bauen. Thomas' Einordnung, und sie ist besser: DAS IST DAS GEWUENSCHTE
  VERHALTEN. Auf einem DAMA-Kanal reden potenzielle Slaves nicht
  miteinander; muessen sie es doch, weil der Digi aus ist und die
  Verbindung wichtig, dann konfigurieren sie sich um ("ifconfig <if> dama
  off"). Zwei DAMA-Master auf einer Frequenz - der echte und einer der
  beiden Nutzer - waeren ohnehin keine gute Idee. KEIN CODE, nur diese
  Begruendung.

  UND EINE KORREKTUR AN MIR (Thomas, 2026-08-17): ich hatte vermutet, ein
  Master koenne ein Rufzeichen von uns, das er durch den Verbindungsaufbau
  kennengelernt hat, in seine Pollliste nehmen. Falsch - was soll er
  pollen, wo er gar nicht beteiligt ist? Ein RR auf eine nicht existente
  Verbindung wird mit DM oder FRMR beantwortet. Ein Master pollt Links, die
  er HAT.

  WECHSELWIRKUNG mit dem 6pack-Kanalzugriff vom 2026-08-15: auf einem
  DAMA-Kanal entscheidet der Master, wer spricht. p-Persistenz und Polling
  sind zwei Antworten auf dieselbe Frage; beide zugleich laufen zu lassen ist
  falsch. Wird DAMA auf einem Port eingeschaltet, gehoert der CSMA dort
  abgeschaltet (bei 6pack: wie Vollduplex behandeln und den Poll ueber die
  Sendeerlaubnis entscheiden lassen). Dasselbe gilt fuer jeden TNC, der selbst
  CSMA macht - bei DAMA muss dessen Persistenz auf 255.
- 6pack: der Mehrgeraetering. GEBAUT ist alles andere (src/sixpack.c, seit
  2026-08-15): Kodierer, Dekoder, Pruefsumme, Kanalzugriff mit p-Persistenz
  und Traegerabfrage, Parameter ueber "param <iface> TxDelay|Persist|SlotTime
  |FullDup". Die TNC-Adresse steht dabei fest auf 0, ein Geraet je Leitung.
  Thomas: begegnet ihm selten, deshalb hierher.

  DIE UEBERLEGUNG DAZU, und sie bestimmt den Aufwand: jedes Geraet am Ring ist
  ein eigener FUNKKANAL - eigene Frequenz, eigenes Rufzeichen, eigener
  Traeger, eigene TxDelay und Persistenz, eigene AX.25-Verbindungen. In WAMPES
  haengt all das an struct iface. Ein Ring mit acht TNCs sind also ACHT
  INTERFACES AUF EINER LEITUNG, und nicht ein Interface mit acht Adressen.
  (Dieselbe Formdiskrepanz ist der Grund, warum es unter Linux mkiss als
  eigenen Daemon gibt und nicht im Treiber.)

  Zu tun waere also ein Schnitt durch struct sixpack, die heute beides
  vermischt:
    * eine LEITUNG: das Geraet, der Byte-Dekoder, die Leseschleife, und der
      Empfang wird ueber die Adresse im oeffnenden SEOF dem Port zugestellt.
    * N PORTS: je ein struct iface mit eigenem dcd, eigenen Parametern,
      eigener Sendeschlange und eigenem Zeitgeber. Der Kanalzugriff ist je
      Port zu rechnen - es sind verschiedene Kanaele.
  Offen ist auch die Schreibweise beim Anlegen: die Leitung einmal anhaengen
  und die Ports einzeln dazu, oder alle acht im attach. Nicht entschieden.

  Zum Kernel-Fix CVE-2026-53082 / bf9a388, nachgesehen 2026-08-14: er betrifft
  uns NICHT direkt. sixpack_receive_buf() lief ueber den Flags-Puffer der
  TTY-Schicht, in dem der Kernel je Byte Uebertragungsfehler vermerkt, ohne
  den Datenzeiger im Gleichschritt weiterzuruecken. WAMPES hat keine Line
  Discipline und keinen Flags-Puffer. Uebertragbar ist nur die Lehre: Zaehler
  und Zeiger duerfen nie auseinanderlaufen.

  Und eine Eigenschaft des Protokolls, die man kennen sollte: die Pruefsumme
  ist eine 8-BIT-SUMME, keine CRC. Jeder Einzelfehler wird erkannt, zwei die
  sich aufheben nicht - im Mittel etwa einer von 256 gestoerten Rahmen kommt
  durch. SMACK ist mit CRC-16 an dieser Stelle deutlich staerker. Steht auch
  im Kopf von sixpack.c.

- kiss: TNC-Initialisierung beim attach. RECHERCHIERT 2026-08-14, Thomas'
  Skizze war im Kern richtig und braucht eine Ergaenzung, die den Kern trifft.
  Es gibt im Baum bisher NICHTS davon; die Stelle waere asy_attach (config.c:126),
  nicht kissattach - das ist die Linux-Seite.

  Der Ausloeser: Thomas hat auf HF schon "KISS ON" und "\n" gesehen. Das ist ein
  TNC im CONVERSE- oder TRANSPARENT-Modus, wo jedes Byte gesendet wird. Es sind
  also DREI moegliche Ausgangszustaende, nicht zwei, und C0 FF C0 holt nur aus
  dem KISS-Modus heraus.

  ENTWURF, mit Thomas festgelegt 2026-08-14:

      attach asy ... kiss ... init tapr | kantronics | "<eigene Folge>"

  Die beiden Fertigen sind nur vorgefertigte Zeichenketten derselben Sprache,
  damit die Sonderfaelle nicht im Code stehen:

      tapr        \xC0\xFF\xC0 \d ^C^C^C \d - "KISS ON\r"      \p "RESTART\r" \p -
      kantronics  \xC0\xFF\xC0 \d ^C^C^C \d - "INTFACE KISS\r" \p "RESET\r"   \p -

  "tapr" heisst so, weil es das ist; "kenwood" ist ein Alias (der eingebaute
  TNC der Kenwoods ist ein TAPR-TNC2-Nachbau, neuere Firmwares kennen dieselben
  Befehle, teils auch KISS OFF).

  Die Sprache, klein gehalten:
      "text"   woertlich senden, mit \r \n \\ \xHH darin
      ^C       Steuerzeichen, lesbarer als \x03
      \d       1 s warten - die CMDTIME-Schutzzeit
      \p       100 ms
      \w<ms>   beliebige Wartezeit (HBAUD-Umschaltung, VFO-Wahl)
      -        Empfangenes verwerfen
      ?"text"  auf eine Zeichenkette warten, mit Zeitgrenze

  REIHENFOLGE: C0 FF C0 ZUERST, dann ^C^C^C - Thomas' Entscheidung, und der
  tragende Grund ist, dass KISS das Ausschalten des TNC ueberlebt (steht im
  KPC-3+-Handbuch). Der Normalfall nach einem WAMPES-Neustart ist also "TNC
  steht noch im KISS-Modus", Converse ist die Ausnahme. So sind unsere ^C erst
  unterwegs, wenn KISS schon verlassen ist, und koennen nicht Teil eines
  Rahmens werden, den der TNC sendet. Der Preis: steht der TNC ausnahmsweise
  im Converse-Modus, gehen drei binaere Bytes ueber die Luft - erheblich
  harmloser als ein lesbares "KISS ON".

  Auch Kantronics kennt C0 FF C0 (KPC-3+-Handbuch, drei Wege aus KISS: harter
  Reset, TCP/IP-Kommando, C0 FF C0). Beide Sequenzen teilen den Vorspann.

  ZWEI EIGENSCHAFTEN, die feststehen:
    * Die Initialisierung laesst das Attach NIE scheitern. Bleibt ein ?"..."
      aus, wird es vermerkt und weitergemacht. Thomas' Fall: Rechner faehrt
      hoch, WAMPES startet, der Sysop steckt den TNC fuenf Minuten spaeter an -
      das Interface ist laengst oben und die Selbstheilung holt es ein. Das
      macht die Selbstheilung zum eigentlichen Mechanismus, nicht die
      Initialisierung beim Attach.
    * Es braucht nicht nur den Zustand "ich initialisiere gerade" (waehrend-
      dessen gehen ankommende Bytes zum Warten und Verwerfen statt in den
      SLIP-Dekoder), sondern auch "ich habe gerade NEU initialisiert": dann
      muss die Sendeschlange des Interfaces verworfen werden. Sonst gehen
      stundenalte Rahmen hinaus, sobald der TNC wieder da ist, und sorgen
      anderswo fuer FRMR.

  Geraeteunterschiede, deshalb muss die Sequenz konfigurierbar sein:
    * Kenwood TM-D700/D710, TH-D72, TAPR TNC2:  "KISS ON" + "RESTART"
      Etablierter Init-String fuer den D710: ^C^C^C^M,KISS ON^M,RESTART^M
    * Kantronics (KPC-3+, KAM):  "INTFACE KISS" + "RESET"
      Achtung: KISS bleibt dort ueber das Ausschalten hinweg gesetzt.
  EOL ist CR, nicht CRLF - beantwortet.

  Offen und noch zu entscheiden:
    * Konfiguration: Vorgabe aus, Sequenz je Interface aenderbar.
    * Selbstheilung. Thomas' Kriterium: der TNC sendet Text und schliesst NIE
      mit FR_END ab.  Der Anknuepfungspunkt dafuer ist schon gebaut -
      slip.c:233: kommen mehr als slip_maxframe() Bytes ohne FR_END, wird
      verworfen, errors++ gezaehlt und SLIP_OVERRUN gesetzt, bis endlich ein
      FR_END kommt.  Ein Text redender TNC loest das im Takt von maxframe
      Bytes immer wieder aus, waehrend nie ein Rahmen entsteht;
      Leitungsrauschen erzeugt dagegen hier und da ein FR_END und gelegentlich
      einen gueltigen Rahmen.  Regel also: aufeinanderfolgende Ueberlaeufe
      OHNE einen abgeschlossenen Rahmen dazwischen, ab dem zweiten neu
      initialisieren.
      NICHT brauchbar als Hinweis, geprueft 2026-08-14: eine ausbleibende
      CRC-Rueckmeldung.  Die SMACK-Aushandlung hat keine eigene Abfrage - ein
      SMACK-TNC arbeitet nach dem Einschalten ohne Pruefsumme und schaltet
      seine Sendeseite erst um, wenn er den ersten Rahmen MIT Pruefsumme
      empfangen hat; die Rueckmeldung reitet also auf dem naechsten Datenrahmen
      mit, den er ohnehin sendet.  Bleibt sie aus, heisst das nur, dass auf dem
      Band nichts gehoert wurde - auf einem ruhigen Kanal der Normalfall.
      Als ZWEITES, schwaecheres Signal faellt daraus aber etwas ab: weil ein
      SMACK-TNC nach dem Einschalten wieder ohne Pruefsumme anfaengt, ist "wir
      stehen auf CRC_16, empfangen aber ploetzlich Rahmen ohne CRC-Bit" ein
      Hinweis auf einen Neustart des TNC.  Ein einzelner verlorener Rahmen
      macht dasselbe Bild, also nur als Bestaetigung, nicht als Ausloeser.
      Quelle: symek.com/g/smack.html

      Zwei Dinge dazu noch entscheiden: ob zusaetzlich geprueft wird, dass die
      verworfenen Bytes wie Text aussehen (alles druckbar plus CR/LF) - macht
      Fehlalarme fast unmoeglich und kostet nichts -, und eine Rueckhaltezeit,
      damit eine tote Leitung nicht endlos neu initialisiert wird; jeder
      Versuch schreibt schliesslich auf die Schnittstelle.
  Quellen: symek.com/pdf/extra.pdf (CMDTIME, Transparent-Escape),
  trinityos.com/HAM/CentosDigitalModes/misc/D710-TNC-setup.txt (Init-String),
  Kantronics KPC-3+ Handbuch (INTFACE KISS / RESET, C0 FF C0).

- kiss Protokoll: multiport-Kiss. NACHRECHERCHIERT 2026-08-14, und das Ergebnis
  aendert die Lage:
    * Der LINUX-KERNEL kann es NICHT. drivers/net/hamradio/mkiss.c sieht sich
      nur "*p & 0x0f" an, genau wie unser kiss.c. Was er kann, ist genau das,
      was wir auch koennen: SMACK ueber Bit 0x80, FlexNet ueber 0x20, mit
      Autoerkennung und Umschaltung (CRC_MODE_FLEX_TEST/SMACK_TEST entspricht
      unseren CRC_TEST_16/CRC_TEST_RMNC). Auch das Abschneiden der zwei
      CRC-Bytes machen beide (bei uns in check_crc_16, crc.c:35).
    * Multiport mit Portnibble gibt es nur im USERSPACE-mkiss, ax25-tools/
      kiss/mkiss.c: "c = (c & 0x0F) | (port << 4)" beim Senden, beim Empfang
      "(*tty->obuf & 0xF0) >> 4".
    * WICHTIG, und der Grund fuer die Reihenfolge: Portnibble und CRC-Bit
      liegen im SELBEN Byte. Multiport bildet (port << 4) | kommando, SMACK
      belegt davon Bit 7 und FlexNet Bit 5. Der Userspace-mkiss loest das,
      indem er BEIDES NICHT MISCHT: bei FLEX_CRC schreibt er ein festes 0x20
      und traegt gar keinen Port ein, sonst Port und keinen CRC-Typ
      (mkiss.c:208-215). G8BPQ-Checksumme klaut kein Bit und vertraegt sich
      daher mit Multiport, FlexNet und SMACK nicht.
  Folge: auf einem seriellen Link mit CRC ist Multiport nicht zu haben. Thomas'
  Prioritaet ist eindeutig CRC, also ist Multiport nur ein Thema fuer TNCs, die
  mehrere Funkports OHNE CRC auf einer Leitung anbieten.
- FlexNet: lernen und weiterleiten trennen. FlexNet ist ein reines L3-Pfadprotokoll,
  es gibt kein L4 - das Nichtlernen einer Route kappt also keine Verbindung, es macht
  das Ziel nur unerreichbar. Nutzer wollen FlexNet-Routen durchaus sehen. Gewuenscht:
  gelernte Routen wahlweise (a) nicht an FlexNet-Partner auf einem anderen Port
  weitergeben, oder (b) gar nicht erst lernen, damit die eigene Tabelle nicht
  zumuellt. Vorbild: xnet konnte das schon (closed source).
  Anwendungsfall: mehrere FlexNet-Geraete am selben Standort sollen alle die
  gelernten Routen haben (damit Nutzer einfach "connect <ziel>" tippen koennen),
  nach aussen zum Interlink-Partner aber als EIN System mit vollem SSID-Bereich
  auftreten. Das verkleinerte die Knotenlisten deutlich.
  Was der Code dazu schon hat (2026-08-11 nachgesehen):
    * struct dest ist bereits "Rufzeichen + SSID-Bereich": FLEXLEN = AXALEN+1, das
      Extrabyte ist die obere SSID (flexnet.c:59, pullflexcall, sprintflexcall gibt
      CALL-min-max aus). Die gewuenschte Aggregation ist also das native Call-Format.
    * update_axroute() (flexnet.c:156) traegt fuer JEDE SSID im Bereich eine
      AX.25-Route mit dem Partner als Digi ein - genau der "connect <ziel>"-Weg.
    * send_rout() (flexnet.c:326) laeuft ueber alle Dests und meldet je Partner die
      beste Verzoegerung; dort waere der Filter fuer (a) einzuhaengen,
      recv_rout() (flexnet.c:1000) fuer (b).
    * ACHTUNG: struct peer hat KEIN Interface - ein Partner ist ein Rufzeichen, die
      AX.25-Verbindung kommt aus der Routentabelle (setaxp, flexnet.c:303).
      Der natuerliche Schluessel ist hier also der Partner, nicht der Port; das
      Interface waere zur Laufzeit ueber pp->axp->iface abzuleiten. Also andersherum
      als vermutet: per Partner ist das einfache, per Port das aufwendigere.
    * Seit 2026-08-14 gibt es flexnet_is_peer() - die Frage "ist das ein Partner"
      ist damit schon von aussen stellbar, bisher nur fuer axserv_connected().

- FlexNet: WER ueberhaupt Partner werden darf. ACHTUNG, Annahme war umgekehrt:
  der Default ist NICHT "nur eingetragene Partner duerfen FlexNet sprechen".
  flexnet_input() (flexnet.c:1116) legt fuer jeden unbekannten Absender einen Peer an
  (create_peer), abgelehnt wird nur die eigene Adresse. Ab da werden seine FLEX_ROUT
  gelernt (recv_rout -> update_delay -> Dests) und ueber send_rout() an die echten
  Interlink-Partner weitergegeben. recv_rout() prueft nichts ausser ismyax25addr().
  Teilweise selbstbegrenzend: polltimer_expired() loescht nicht-permanente Peers, die
  einen Poll nicht beantworten - wer antwortet, bleibt.
  Das ist derselbe Befundtyp wie W-40 bei RIP (unauthentifizierte Routeninjektion).
  flexnet.c kam im Sicherheitsaudit NICHT vor, der Bereich ist ungeprueft.
  Gewuenscht (Thomas): Nutzern die FlexNet-Abfrage (nodes) erlauben duerfen, ihre
  Routen aber weder lernen noch weiterleiten. Also drei Stufen statt zwei:
  gar nicht sprechen / abfragen duerfen / vollwertiger Partner.

- FlexNet: je Nachbar konfigurieren, WAS wir ankuendigen. Modi:
    (default) alles, wie bisher
    nur unseren eigenen SSID-Bereich
    einzelne SSIDs (Liste, im Stil von tcpgate/ipfilter)
  und getrennt davon: ob von anderen Gelerntes ueberhaupt weitergegeben wird.
  Das sind ZWEI verschiedene Stellen im Code (2026-08-11 nachgesehen):
    * Was wir ueber UNS SELBST sagen, steht in FLEX_INIT, nicht in den Dests:
      send_init() (flexnet.c:243) sendet ein einziges Zeichen,
        '0' + ((pp->axp->hdr.source[ALEN] & SSID) >> 1)
      also unsere eigene SSID auf genau diesem Link, als obere Bereichsgrenze.
      Die Gegenseite legt sie in recv_init() nach pp->call[ALEN+1]; die untere
      Grenze ist die SSID des Rufzeichens, unter dem sie uns kennt. Heute ist der
      angekuendigte Bereich also entartet (unten = oben = Link-SSID) und nicht
      konfigurierbar. "Nach aussen als EIN System mit vollem SSID-Bereich"
      auftreten heisst: dieses eine Byte konfigurierbar machen. Kleiner Eingriff.
    * Ob wir GELERNTES weitergeben, entscheidet send_rout() (flexnet.c:326).
      Dort laeuft die Schleife ueber alle Dests; dorthin gehoert der Filter.
  Wichtig zum Verstaendnis: Dests enthaelt AUSSCHLIESSLICH Gelerntes. Der einzige
  Ort, an dem ein dest entsteht, ist update_delay() (flexnet.c:221), gerufen aus
  recv_rprt() und recv_rout() - also immer von einem Partner. WAMPES hat heute
  keinen eigenen Eintrag in der Zieltabelle. Fuer "nur eigenen SSID-Bereich" gibt
  es daher nichts zu filtern; das laeuft ueber FLEX_INIT.

- Netrom INP3 Unterstuetzung
  Links: TNN: https://github.com/kq6up/tnn
    https://github.com/packet-net/packet.net/blob/main/docs/netrom-inp3-i3-design.md
  Auf Lizenzen achten. Was nichtGPL ist, muessen wir selbst schreiben.
- Der kopflose Knoten wirft weg, was net.rc zu sagen hat. hpux.c:116:
      if ((local_kbd = (isatty(0) && isatty(1))))
  Haengen stdin UND stdout nicht an einem Terminal, werden 0/1/2 geschlossen
  und auf /dev/null neu geoeffnet - und zwar BEVOR net.rc gelesen wird. Genau
  so laeuft eine echte Installation (rc-Skript, systemd), und dort ist damit
  jeder Fehler in der Konfiguration unsichtbar: asy_attach allein hat fuenf
  Meldungen ("Mode %s unknown for interface %s" und andere), die dann niemand
  je zu sehen bekommt. Aufgefallen 2026-08-14 beim KISS-Test, wo genau diese
  Meldung gefehlt haette.
  Zu beachten: was heute per syslog geht, ist fast nichts - drei Aufrufe
  insgesamt (remote_net.c:973, axserver.c:710 und 1286), alle aus der Arbeit
  der letzten Tage, und openlog("wampes-net", LOG_PID, LOG_DAEMON) steht in
  remote_net_initialize(). Ob das frueh genug fuer die Startphase laeuft, ist
  vor dem Bau nachzusehen.
  Gewuenscht: ohne Terminal gehen die Meldungen der Startphase nach syslog.
  Unter systemd faengt der Journal ohnehin stdout/stderr ein - das Schliessen
  der Deskriptoren ist der eigentliche Verlust, nicht das Fehlen von syslog.

- Optimale Moeglichkeit dass er privilleges dropt. ax25 login geht dann nicht mehr. Macht das Programm aber weniger angreifbar
- Versionskennung: der Startbanner sagt seit dem 5.3.2000 dasselbe.
  src/version.c ist von Hand gepflegt (die $Id-Zeile darin ist von 2016, die
  Kennung darin von 2000):
      "@(#)WAMPES-000305-DL9SAU-VJC-VCompat-KRNLIF-TUNTAP-DIGIARP-GDBM"
  Thomas' Vorschlag: den githash hineinschreiben. Vorschlag zur Form -
  Datum UND Hash, weil ein Hash allein nichts ueber das Alter sagt und die
  000305 ja gerade als Datum gemeint war:
      WAMPES-2026-08-15-g40c33dc[+]-DL9SAU-VJC-...
  DER EIGENTLICHE GEWINN ist das "+": der Banner sagt dann sofort, ob
  das laufende Programm zum Baum passt. Genau diese Verwirrung gab es am
  2026-08-15 ("neu gebaut ist das nicht" - die installierte Fassung war drei
  Tage alt und niemand sah es ihr an).
  Nicht "-dirty", wie git es nennt: kein Mensch betreibt gern eine Software,
  die sich selbst als schmutzig bezeichnet. Ein angehaengtes "+" sagt
  dasselbe - hier steckt mehr drin als im Stand des Hashes.
  Beim Bauen zu beachten: version.c ist verfolgt, darf also nicht erzeugt
  werden, sonst ist der Baum dauerhaft schmutzig. Zwei Wege: ein erzeugter,
  nicht verfolgter Header, den version.c einbindet - oder ein -D auf der
  Uebersetzungszeile nur fuer version.o, das dann aber von .git/HEAD abhaengen
  muss, damit der Hash nicht veraltet. Und ein Rueckfall auf die feste
  Zeichenkette, wenn kein git da ist (Quelltextarchiv).

- MTU-Wahl, zur Diskussion (Thomas, 2026-08-15): wie waehlt man sie, damit
  weder unnoetige AX.25-Segmentierung noch IP-Fragmentierung entsteht?
  Sein Punkt: bei IP ueber AX.25 UI laeuft man in die Grenze
  MTU <= 256 - IP-Kopf - (UDP|TCP)-Kopf, und AX.25 UI kann - seiner
  Erinnerung nach - keine Segmentierung, zu grosse Pakete werden also
  verworfen statt geteilt.
  Nachzupruefen waere vor der Diskussion: was macht WAMPES heute? segmenter()
  in ax25subr.c bzw. send_ax25() teilt auf paclen, aber gilt das auch fuer
  UI? Und was ist der Zusammenhang zwischen ifp->mtu, paclen und dem, was
  ip_route() aufteilt?

- Projekt auf github. Voraussichtlich https://github.com/dl9sau/WAMPES/ - der
  Name steht fuer Thomas schon fest. Danach zwei Verweise nachtragen, die
  heute nur deshalb fehlen, weil es das Ziel noch nicht gibt:
    * in doc/LIBAX25-BRIDGE.md ein Link auf das Projekt selbst
    * umgekehrt in libax25 eine kurze Datei, die auf doc/LIBAX25-BRIDGE.md
      zeigt - wer von der libax25-Seite kommt, findet es sonst nur ueber den
      Verweis in axports.5
  Ausserdem: github actions fuer compile fuer i386, 64bit, raspberry pi ARM, macos in intel- und arm Technologie
- Debian-, RPM-, brew- und ports-Pakete bauen koennen. Die Vorarbeit steht:
  doc/INSTALL-PATHS.md sortiert jeden von TCPDIR abgeleiteten Pfad nach FHS,
  und BINDIR/SBINDIR/PUBLICBINDIR merkt sich lib/configure bereits. Offen sind
  die drei restlichen Variablen SYSCONFDIR, LOCALSTATEDIR, RUNDIR und danach
  die Paketbauerei selbst.

- netrom Protokoll: jenseits des Standards. Normalerweise sind connectende nodes
  Infrastruktur und trusted. Ich moechte meinen Nutzern die Moeglichkeit geben
  mit mir Netrom-Protokoll zu sprechen, um 1. Knoten (Ziele) zu lernen 2. Einen L3-
  Connect dorthin zu machen und 3. Per inp3-Protokoll IPv4-Routen zu lernen und
  ggf. zu pushen (fuer Lernen auf unserer Seite braucht es eine whitelist node und bei ip3 bei ip-route-annoucement des erlaubten annoucten ip-ranges).
  Diese user-Knoten vermitteln wir nicht upstream.
  Wer WAMPES als User einsetzt: hier soll wampes als user-NODE verwendbar sein.
- Bei flexnet-connect eines users (nicht eingetragener linkpartner) soll dieser unsere routen lernen koennen - aber er nicht nach aussen annouced werden und die routen die er annouced sollen nicht in userer routing-Tabelle landen (siehe unten: Filter "only him"
  Zudem: kein flexnet RTT Test zu solchen usern. Belastet Link unnoetig. Also wenn rtt-tests anstehen, diese user auslassen. Ich weiss aber nicht ob die wampes-Implementierung das ueberhaupt macht. Das ist eine Art Echo-Paket mit 256 bytes und er misst in regelmaessigen Abstaenden, wie lage das braucht.

- Filter: GEBAUT am 2026-08-16, Commit 9ca9703, beschrieben in
  doc/ROUTE-FILTER.md. Zwei Achsen statt der unten skizzierten zwei mal drei
  Werte, weil beim Durchgehen des Codes nur zwei echte Entscheidungen uebrig
  blieben:
      <proto> filter default|port=<name>|<call> [in <modus>] [advert yes|no]
      in all | only-him | none      was von ihm in unsere Tabellen kommt
      advert yes | no               ob ANDERE von IHM erfahren
  "except-him" von unten ist "advert no"; "us/except-us" ist entfallen, weil
  jeder, mit dem wir ueberhaupt sprechen, unsere Ankuendigungen bekommt.
  Call schlaegt Interface schlaegt default; ohne jede Zeile bleibt alles wie
  bisher. Dazu "netrom broadcast enable|disable <n>" je Eintrag (Befund 3).
  Der "feed"-Schalter stand hier bis 2026-08-17 als offen und IST GEBAUT,
  als dritte Achse (Commit 4d02fdb): rf_feed() in routefilter.c, bei
  FlexNet je Linkpartner (peer_feed), bei NET/ROM je Port
  (broadcast_feeds). Beschrieben ist er in doc/ROUTE-FILTER.md.

  WIRKLICH OFFEN geblieben, und dort am Ende genannt:
    * die dritte Zugangsstufe "darf gar nicht sprechen" - "in none" kommt
      nahe und ist nicht dasselbe, die Rahmen werden weiter geparst
    * DAS BROADCAST-INTERVALL je Interface. Der Eintrag hiess hier bis
      2026-08-18 "nr_bdcstint/nr_minobs je Interface" und war zu kryptisch;
      ausgeschrieben:
        nr_bdcstint = 1800 - alle 30 Minuten schickt der Knoten seinen
          NODES-Broadcast. EINE Zahl fuer den ganzen Knoten; je Eintrag
          steht nur das OB ("netrom broadcast enable|disable <n>").
        nr_minobs - "Obsolescence count min to be broadcast", und im Code
          steht dahinter "/* not used */". TOTER PARAMETER, er gehoerte gar
          nicht in die Liste.
        nr_obsinit = 3 - das ist der, der wirkt: angekuendigt wird ein
          Eintrag nur, solange alter < nr_obsinit * nr_bdcstint, also
          90 Minuten.
      Und darin steckt die eigentliche Arbeit, nicht in der Variablen:
      INTERVALL UND VERALTERUNG HAENGEN ANEINANDER. Ein Port mit 10 statt
      30 Minuten bekaeme automatisch 30 statt 90 Minuten Haltbarkeit. Wer
      das je Interface einstellbar macht, muss entscheiden, ob nr_obsinit
      mitwandert oder die Veralterung global bleibt.
      NACHGESEHEN AM 2026-08-18, in xnet138 und den TNN-Quellen. Thomas'
      Erinnerung stimmte, und wir sind in DREI Punkten der Ausreisser.

      XNET, "ro pa":
        broadca  600  [300..3000]  Broadcast-Intervall, also 10 Minuten
        minBcas    4  [1..12]      min obs-count fuers Broadcasten
        minQual   69  [0..255]     min Qualitaet fuers Broadcasten
        obsInit    6  [1..12]      Anfangswert des Veraltenszaehlers
      Der Zaehler wird bei JEDER Aussendung heruntergezaehlt (so das
      Handbuch), es ist also ein Zaehler und kein Altersvergleich wie bei
      uns. Mit obsInit 6 und minBcas 4 heisst das: angekuendigt wird ein
      Eintrag zwei Zyklen lang, behalten wird er sechs.

      TNN, l3misc.c, und das ist der interessanteste Teil - es hat ZWEI
      Aussendungen nebeneinander, und WAS ein Peer bekommt, haengt an
      seinem Typ (typedef.h: INP 0, TNN 1, THENET 2, NETROM 3):

        Peer-Typ   nur Aenderungen   ganze Tabelle
        INP        ja                NEIN - gar kein zyklischer Rundspruch
        TNN        ja                alle broint_i  (60, "Infocast")
        THENET     NEIN              alle broint_ui (30)
        NETROM     ja                alle broint_ui (30)

      TNN ist dabei ein EIGENER Typ und nicht INP3 - die beiden stehen
      nebeneinander in derselben Aufzaehlung. Der INP3-Nachbar ist der
      Grenzfall in die andere Richtung: er bekommt AUSSCHLIESSLICH
      Aenderungsmeldungen und keinen Volldurchlauf, was zu INP3 passt,
      das ereignisgetrieben arbeitet.
      Die Einheit widerspricht sich in den Kommentaren der Quelle selbst -
      "in 10s" in global.c, "in 1min." in l3thenet.c -, aber die Notiz
      "(Standard: aller 5min. Aussend.)" spricht fuer 10 s, also 5 und 10
      Minuten. Dazu "worqua", min Qualitaet fuers Autoupdate.

      DAMIT IST DIE ANTWORT AUF BEIDE FRAGEN JA: sie senden haeufiger
      (5..10 min gegen unsere 30), und sie kuendigen AKTIV an - TNN sogar
      dauernd, in Form der Aenderungsmeldung, mit dem Volldurchlauf nur
      als Auffrischung.

      ALS WAS GELTEN WIR AUS TNNS SICHT (Thomas' Frage)? Der Typ ist ein
      WUNSCH plus eine ERKENNUNG: l3nbr.c nimmt den konfigurierten Typ
      nach soll_typ und setzt den wirklichen erst einmal auf NETROM
      ("TNN und INP werden automatisch erkannt"). Erkannt wird INP ueber
      die RTT-/INP-Verhandlung (l3rtt.c); THENET wird gesetzt, sobald ein
      gewoehnlicher NET/ROM-Rundspruch eintrifft und noch kein Typ
      feststeht (l3netrom.c, unter THENETMOD).

      Wir machen weder INP3 noch TNNs Messung, wir schicken nur
      NET/ROM-Rundsprueche. Also gelten wir als THENET - und dessen
      Kommentar in typedef.h lautet woertlich "Nachbar, der nicht auf
      Messung reagiert". Hatte der Sysop sich einen TNN- oder INP-Link
      gewuenscht, bleiben wir bei NETROM haengen. TNN oder INP werden wir
      nie.

      Und nach der Tabelle oben heisst THENET: KEINE Aenderungsmeldungen,
      nur der Volldurchlauf alle broint_ui. Wir sitzen also auf seiner
      stumpfsten Stufe, und das ist ein handfestes Argument fuer den
      INP3-Posten weiter oben - es geht nicht nur darum, was wir lernen,
      sondern auch darum, wie er uns bedient.

      WIR SIND ALSO DER AUSREISSER IN DREI PUNKTEN, und alle drei stehen
      schon als eigene Posten hier:
        * ein Intervall statt zweier, und dreimal so lang
        * kein "nur die Aenderungen" - das ist der verkuerzte Broadcast
        * kein minBcas und kein minQual - unser nr_minobs ist genau XNETs
          minBcas, und es ist bei uns toter Code
    * ein VERKUERZTER Broadcast je Eintrag. Bei NET/ROM ist die Wahl auf
      einem Benutzerport heute alles oder nichts; nur einen Teil der
      Tabelle anzukuendigen waere der naechste Schritt. INP3 stellt
      dieselbe Frage von der anderen Seite, weil es verbunden laeuft.

  Die urspruengliche Skizze, zum Vergleich:
    in: only import him / except-him / all
    out: us / except-us / all
  except-him: wir muessen ihn natuerlich wie einen Knoten behandeln, sonst muessen wir ueberall Sonderbedignugen rein schreiben. Aber er gehoert geflagt, damit wir beim Senden von flexnet-routen oder node-dests ihn selbst aus den annoucemets rausnehben koennen.
  Sinn macht das u.A. dafuer, dass wenn ich am Standort mehrere flexnet-Knoten habe
  die flexnet eingeschaltet haben, mit unterschiedlichen ssid-ranges. Dann soll bei upstram annoucemets zu unseren Likparnern nicht jeder node einzeln annouced werden sondern aggregiert - also gem. unserer Konfiguration, und die more-detail-Eintraege koennen mit der Filteroptipo except-him weggefiltert werde.


- datagram: Empfangsrichtung. Der Dienstsocket kann UI heute nur senden
  (datagram_line -> ax_send_ui); nichts schiebt empfangene UI-Frames zum
  Client zurueck. Fuer ein recvfrom() auf einem AX.25-Datagrammsocket
  (libax25-Shim) fehlt damit die Quelle. Gebraucht wird eine Anmeldung auf
  PID und Port und die Ausgabe im TNC2-Format, also die Gegenrichtung des
  schon vorhandenen Zeilenformats. Spaetere Phase, die Senderichtung genuegt
  fuer den ersten Bau.

  DAZU GEHOERT EINE PORTBESCHRAENKUNG, und die betrifft "listen add"
  genauso. Heute ist ein Rufzeichen global erreichbar - die Mailbox auf
  SSID-8 auf allen Interfaces -, und das ist meistens genau richtig. Es gibt
  aber Aufbauten, wo an einem Port APRS haengt und dort niemand connecten
  soll. SABM/UA dort ganz abzuschalten ist ausdruecklich NICHT gewollt;
  gewollt ist, das Lauschen auf bestimmte Eingangsports zu begrenzen.
  Gedacht als Liste, entweder als Aufzaehlung ("a,b") oder als Ausschluss
  ("!c,d").

  Der Gedanke kam ueber die Empfangsrichtung der Datagramme: welche Pakete
  will ich ueberhaupt an libax25 weiterreichen, und von welchem Port will ich
  gar nichts erst sehen. Fuer Datagramme und fuer Verbindungen ist es
  dieselbe Frage, also soll es dieselbe Angabe sein.

  Vor dem Bauen zu entscheiden:
    * Gilt die Angabe fuers Lauschen (welche SABM angenommen werden), fuer
      die Datagramm-Weitergabe, oder fuer beides getrennt einstellbar?
    * Was passiert mit einem SABM auf einem nicht erlaubten Port? Der Knoten
      soll sich verhalten, als lausche dort niemand - sein normales
      Verhalten also, kein Sonderweg.
    * Verhaeltnis zu den interfacebezogenen Rufzeichen, die "listen add"
      schon kennt. Verwandt, aber nicht dasselbe: dort geht es um das
      Rufzeichen des Interfaces, hier um den Eingangsport.
    * Namen und Syntax. Die Ausschlussform "!c,d" ist bequem, muss sich aber
      mit der Aufzaehlung vertragen und darf nicht mit der Shell kollidieren,
      wenn jemand die Zeile aus einem Skript setzt.

- libax25-Anbindung: GEBAUT, getestet, getagt als wampes-bridge-2026-08-14 in
  wampes-git, libax25 und ax25-tools. Beide Richtungen laufen; beschrieben ist
  alles in doc/LIBAX25-BRIDGE.md, hier steht nur noch, was aussteht:
    * der Test auf Linux gegen ein echtes ax25d und axspawn. Zwei Dinge zeigt
      der Mac prinzipiell nicht: die Framegrenzen (kein AF_UNIX SOCK_SEQPACKET,
      faellt auf Strom zurueck - geprueft ist nur die Uebergabe) und ein
      echtes axspawn (braucht eine Kontendatenbank).
    * AXSOCK_BACKEND und WAMPES_SOCKET sind nur noch Uebersteuerung und sollen
      weg - aber erst nach dem Linux-Test, dort vielleicht zum Debuggen noch
      gebraucht.
    * Datagramme eingehend, siehe den eigenen Punkt oben.
  Nicht offen, sondern entschieden: ein nicht blockierendes connect() gibt es
  absichtlich nicht (hiesse poll/select abfangen, also den Datenweg wieder
  belegen; und niemand verlangt es).

------------------------------------------------------------------------
EAX25 (erweitertes AX.25, Modulo 128, Fenster bis 63)
------------------------------------------------------------------------
WAMPES kann es LESEN, nicht SPRECHEN. Vorhanden sind: SABME (0x6f) und
EMMASK (0x7f) in lapb.h, die Ausgabe in ax25dump.c (Name "SABME", FRMR mit
Modulo-128-Masken), und das Lesen der SSID-Bits SSID_EAX25 / SSID_DAMA in
ax25hdr.c. In lapb.c, also in der Zustandsmaschine, kommt nichts davon vor,
und domaxframe() begrenzt das Fenster hart auf 1..7. Wieder das Muster:
die Vorkehrung ist da, angeschlossen ist sie nie worden.

STATUS: GEBAUT UND VOLLSTAENDIG GEMESSEN (2026-08-17). Modulus je Link
(axp->mmask), zweites Steuerbyte, EMaxframe, Rueckfall auf DM/FRMR sofort
und auf Schweigen nach drei Proben, dreiwertiges Gedaechtnis im
Routeneintrag, Interface-Achse "ifconfig <if> eax25 off|caller|always".
Einzelheiten und Messwerte in doc/EAX25.md, "always" seit 2026-08-17 auch
(Abschnitt "always, and the relay it needs").

OFFEN AUS DIESER ARBEIT, jeweils mit eigener Ueberschrift weiter unten:
  * T1: NICHT DER TIMER IST FALSCH, SONDERN DAS LIMIT (Thomas, 2026-08-17)
    - dazu jetzt eine gerechnete Abschaetzung, siehe dort
  * WIR SAGTEN AUF DEM DRAHT NICHT, WELCHEN MODULUS WIR SPRECHEN
    (behoben, und meine erste Diagnose dazu war falsch)
  * EINE PROBE STATT DREI auf einem Relaisbein - kein Fehler, aber eine
    Eigenschaft, die man kennen muss
------------------------------------------------------------------------
lapb.c:1421 rechnet mdev aus T1init * (1 + 2*Digis), also T1 = 5 s direkt,
25 s bei zwei Digis, 85 s bei acht (MAXDIGIS). Mit N2=10 und dem 1,25er-
Backoff in recover() ergibt das als Gesamtwartezeit bis zur Aufgabe:

          0 Digis   2 Digis   8 Digis
 heute      166 s     831 s    2826 s   (47 min)

THOMAS' EINWAND: sich eine dreiviertel Stunde mit SABMs zu blockieren ist
unsinnig, wenn Aufgeben nach fuenf Sekunden und ein neuer Versuch viel
schneller zum Ziel fuehrt. Es geht NICHT darum, frueher aufgeben zu wollen -
bei zaehen Strecken oder Mailbox-S&F darf es ruhig dauern -, sondern darum,
dass die Formel die Wirklichkeit falsch schaetzt. Zwoelf Minuten waeren in
Ordnung, achtundvierzig nicht.

KEINE PROTOKOLLAENDERUNG. Das hatte ich zuerst so genannt und es stimmt
nicht: T1 steht nirgends in AX.25, das ist eine reine
Implementierungswahl. Es ist eine Verhaltensaenderung mit grosser
Reichweite - jede Verbindung -, also ein eigener Commit mit eigenen
Messungen, mehr nicht.

WARUM DER BACKOFF ES NICHT AUFFAENGT (Thomas' Rueckfrage): Blimit
(ax25subr.c, Vorgabe 16) begrenzt nicht den WERT, sondern die
Wachstumsrate, und es feuert nie, weil N2=10 kleiner ist. Der Timer waechst
also zehnmal ungebremst um 1,25.

WAS NICHT ZU AENDERN IST, und das ist Thomas' Korrektur an einer falschen
Faehrte von mir: build_path() setzt axp->srt = 0 (lapb.c:1456), verwirft
also die gemessene Umlaufzeit beim Neuaufbau. Das ist RICHTIG SO, aus zwei
Gruenden.

Erstens inhaltlich: wuerde man sie behalten, startete ein Link, der einmal
gebrochen ist, mit einer ungeheuer langen Wartezeit fuer sein SABM.
Schnell anfangen und je Versuch wachsen ist die richtige Reihenfolge.

Zweitens strukturell, und das ist der schaerfere Grund: der Kontrollblock
GEHOERT DER SESSION. Ist sie abgebaut, gibt es keinen Ort mehr, an dem der
Wert laege - und innerhalb einer bestehenden Session faellt gar kein SABM
an. Die Anfangsschaetzung wirkt also ausschliesslich im Aufbau, und einen
Speicher, der sie ueberdauert, muesste man erst erfinden. Genau das waere
mein Vorschlag "ins Routenverzeichnis, wie die EAX25-Faehigkeit" gewesen,
und er ist nach dem ersten Grund unerwuenscht. Der Fehler sitzt nicht
dort.
(Nebenbei: lapb.h:221 deklariert "extern int32 Axirtt", initial rtt, und
die Variable ist nirgends definiert oder benutzt. Dasselbe Muster wie
SABME und EMMASK - die Vorkehrung ist da, angeschlossen wurde sie nie.
Nach dem Obigen ist sie aber auch nicht die Loesung.)

THOMAS' IDEE - DAS LIMIT SCHAETZEN, NICHT DEN TIMER: der Timer darf ruhig
schnell anfangen und wachsen, was fehlt ist eine OBERGRENZE fuer die
Gesamtdauer des Aufbaus. Und die Daten dafuer liegen schon im Knoten:

  * FLEXNET KENNT ZIELE UND IHRE UMLAUFZEIT. struct quality traegt delay
    in 100-ms-Einheiten (flexnet.c:60), gespeist aus gemessenen Polls
    (locdelay/remdelay) und je Hop aufsummiert; find_best_quality() gibt
    den besten Weg zu einem dest. Ist das AX.25-ZIEL ein FlexNet-Ziel,
    oder ist der letzte DIGI VOR dem Ziel eines, dann haben wir eine
    begruendete Erwartung, was der Weg kostet - und daraus ein Limit,
    etwa "erwartete Umlaufzeit mal Faktor, plus Gnadenfrist von 30 s".
    Findet sich nichts, bleibt es beim heutigen Verhalten.
  * NETROM hat auch Umlaufzeiten, taugt hier aber nur als Indiz: dort
    connecten wir nicht bis zum Ende durch. Verwendbar waere es fuer den
    Timeout des NET/ROM-L4-Aufbaus und fuer den Proxy-Connect, nicht fuer
    die AX.25-SABM-Phase.

ZUR SSID: find_dest() vergleicht mit addreq() ueber sieben Bytes, ein
FlexNet-Eintrag traegt aber einen SSID-BEREICH (call[ALEN+1]). Thomas'
Entscheidung dazu: die Unschaerfe genuegt fuer eine Abschaetzung - wir
suchen ja keine Route, sondern eine Groessenordnung. Also den Bereich
mitnehmen und nicht daran haengenbleiben.

UND DIE KURVE DOCH, nur anders (Thomas' Praezisierung an meinem "nicht die
Kurve, sondern das Limit"): die beiden komponieren. Statt unbegrenzt mit
1,25 zu wachsen und dann hart zu kappen, laesst man die Kurve sich der
Obergrenze ANNAEHERN - wie ein Sinus, der sich langsam an 1 herantastet.
Ganzzahlig ist das eine Zeile in recover():

	t1 = t1 + (Tmax - t1) / 4;      /* statt t1 = t1 * 5 / 4 */

Weit unter der Grenze waechst das fast so schnell wie heute, nahe daran
fast gar nicht, und der Sprung von zehn auf zwanzig Minuten kommt nie
zustande. Tmax kommt aus der FlexNet-Schaetzung oben; findet sich keine,
bleibt es beim heutigen Verhalten.

WO WAS HINGEHOERT, damit die beiden Sachen nicht durcheinandergeraten:

  * Die EAX25-FAEHIGKEIT steht im AX.25-Routeneintrag (struct ax_route,
    Feld eax25) und gehoert genau dorthin: sie ist eine Eigenschaft des
    DIREKTEN NACHBARN, mit dem wir SABM oder SABME tauschen. Ein
    FlexNet-Ziel ist ein Knoten viele Hops entfernt, dem wir nie ein SABME
    schicken - dort waere so ein Bit sinnlos, und wenn es je dort auftaucht,
    muss dabeistehen warum.
  * Die UMLAUFZEIT ist umgekehrt gelagert: eine Eigenschaft des WEGES, und
    deshalb dort richtig, wo FlexNet sie ohnehin fuehrt.

Beide Speicher sind fluechtig - bricht der Link weg, ist der Eintrag weg -,
und das ist in Ordnung. In stabilen Verhaeltnissen steht die Zahl da, wo
man sie braucht, und die Listen gibt es ohnehin.

Gerechnete Varianten fuer den Fall, dass man stattdessen doch die Kurve
anfassen will:

          0 Digis   2 Digis   8 Digis
 Blimit 4    87 s     437 s    1486 s
 Blimit 2    61 s     306 s    1041 s
 N2 6        56 s     281 s     957 s
 N2 6, B 3   42 s     212 s     722 s

Auch die schaerfste laesst bei acht Digis zwoelf Minuten stehen - weil der
Anfangswert dominiert und nicht die Kurve. Das spricht fuer Thomas' Weg.

------------------------------------------------------------------------
ABSCHAETZUNG DAZU (2026-08-17), gerechnet statt geschaetzt
------------------------------------------------------------------------
WAS ES BRINGT. Die Kurve gegen eine Obergrenze, Gesamtwartezeit bis zur
Aufgabe bei N2=10 und T1init 5 s:

                                 0 Digis   2 Digis   8 Digis
 heute                             166 s     831 s    2826 s
 Kurve gegen Tmax=20, Start heute   143 s     219 s     445 s
 Kurve gegen Tmax=45, Start heute   299 s     375 s     601 s
 Kurve gegen Tmax=90, Start heute   579 s     655 s     881 s

Das ist der Punkt, an dem meine frueheren Zahlen oben zu pessimistisch
waren: sie rechneten mit Blimit und N2, und dort dominierte tatsaechlich der
Anfangswert. Gegen eine ECHTE Obergrenze faellt der Achtdigi-Fall von
siebenundvierzig auf sieben bis fuenfzehn Minuten, weil die Kurve dort nicht
waechst, sondern SCHRUMPFT - T1 startet bei 85 s und laeuft nach unten auf
Tmax zu.

UND DIE SAUBERERE VARIANTE. Wenn ohnehin eine Schaetzung vorliegt, ist der
Anfangswert T1init*(1+2*Digis) nur noch die schlechtere Information. Faengt
man stattdessen bei T1init an und laesst die Kurve nach OBEN auf Tmax
zulaufen, verschwindet die Digizahl vollstaendig aus der Rechnung:

 Kurve gegen Tmax=20, Start T1init  143 s     143 s     143 s
 Kurve gegen Tmax=45, Start T1init  299 s     299 s     299 s
 Kurve gegen Tmax=90, Start T1init  579 s     579 s     579 s

Genau das ist Thomas' "der Timer darf ruhig schnell anfangen und wachsen",
zu Ende gedacht. Nur da anwenden, wo eine Schaetzung existiert; ohne eine
bleibt der Digizaehler das Beste, was wir haben.

KORREKTUR AN MEINER EIGENEN NEBENBEMERKUNG. Ich hatte geschrieben, der
EAX25-Rueckfall werde auf derselben Kurve "automatisch mit kuerzer". Das
gilt nur mit Digis. Ohne welche wird er LAENGER, weil die Kurve gegen ein
fernes Tmax schneller steigt als der 1,25er-Backoff:

 Was drei Proben kosten:        0 Digis  2 Digis  8 Digis
 heute                             19 s     95 s    324 s
 Kurve gegen Tmax=20               25 s     72 s    210 s
 Kurve gegen Tmax=45               42 s     89 s    228 s
 Kurve gegen Tmax=90               73 s    120 s    258 s

Verhaeltnismaessig bleibt es in Ordnung - 42 s Proben gegen 299 s Geduld
ist dieselbe Groessenordnung wie heute 19 gegen 166 -, aber die Zahlen in
doc/EAX25.md ("Falling back", 19 s und 94 s) muessen dann mit.

WAS ZU BAUEN IST, drei Stuecke:

  1. Eine Auskunft aus FlexNet, etwa "int flex_expected_rtt(const uint8
     *call)" in flexnet.c, deklariert in flexnet.h. Vorhanden ist alles:
     struct quality traegt delay in 100-ms-Einheiten je Weg,
     find_best_quality() liefert den besten. locdelay wird als
     TDIFF(...)/200 gebildet, ist also die HALBE Umlaufzeit - fuer eine
     Umlaufzeit gehoert der Faktor 2 dazu. MAXDELAY ist 3000, also 300 s;
     was darueber steht, heisst "unerreichbar" und darf kein Limit werden.
     Die SSID-Unschaerfe ist entschieden (Bereich mitnehmen, nicht daran
     haengenbleiben); der Vergleich laeuft ueber die sechs Rufzeichenbytes
     und den Bereich in call[ALEN+1], nicht ueber addreq(). Etwa 40 Zeilen.
  2. Ein Feld axp->tmax, gefuellt in build_path() - erst fuer das Ziel,
     sonst fuer den letzten Digi vor dem Ziel, sonst 0 = kein Limit.
     Etwa 15 Zeilen. Achtung: build_path() laeuft im Relaisfall bei JEDEM
     wiederholten SABM des Rufers erneut, also die Auskunft nicht teuer
     machen oder das Ergebnis behalten.
  3. Die eine Zeile in recover(): statt (dur_timer*5+2)/4 dann
     t1 + (tmax - t1)/4, wenn tmax gesetzt ist. Der DAMA-Zweig kehrt vorher
     zurueck und ist nicht betroffen; das Relaisbein wiederholt ohnehin
     nicht und merkt von der Kurve nur die Ablaufzeitpunkte.

AUFWAND: rund 60 Zeilen Code, ein Commit, und der groessere Teil ist die
Messung. Die geht mit vorhandenem Werkzeug: flexpeer.py --announce setzt
eine Umlaufzeit, danach ein Connect ueber diesen Weg und die Zeitpunkte der
SABM-Wiederholungen aus "trace" ablesen. Zu pruefen sind drei Dinge - dass
ohne FlexNet-Wissen alles bleibt wie es ist, dass eine Aenderung der
angekuendigten Umlaufzeit sich auf einen NEUEN Aufbau auswirkt und auf einen
laufenden nicht, und dass ein "unerreichbar" (MAXDELAY+1) nicht als Limit
durchschlaegt.

DIE HOPS RECHTS VON UNS - SCHON RICHTIG (Thomas' Rueckfrage, geprueft)
Sein Einwand: wir quittieren hop-to-hop. Sind wir Hop 4 und sollen noch
zwei weiter, dann kommen UA, RR und DM ueber diese zwei Hops zurueck und
nicht ueber alle sechs - T1 darf also nur die noch NICHT wiederholten
Digis rechts von uns zaehlen, sonst wachsen die Wartezeiten nach links
auf.

Das tut es bereits, und die Stelle ist eindeutig (lapb.c):

    axp->mdev = (T1init * (1 + 2 * (axp->hdr.ndigis - axp->hdr.nextdigi))
                 + 2) / 4;

ndigis minus nextdigi ist genau die Zahl der noch offenen Digis.
Nachgemessen am Relais: das zweite Bein traegt uns selbst als bereits
wiederholten Digi, also ndigis 1 minus nextdigi 1 = 0, und sein T1 war
5 s; der Rufer davor, der einen offenen Digi vor sich hat, rechnete mit
15 s. Kein Handlungsbedarf, nur die Bestaetigung, damit es nicht ein
zweites Mal gefragt wird.

DIE ANDEREN UHREN STEHEN NICHT IM WEG (Thomas' Rueckfrage, nachgelesen)
Vorgaben: T1 5 s, T3 900 s (Idle-Poll), T4 60 s (busy), T5 3600 s
(Idle-Disconnect). Ein grosses T1 kollidiert mit keiner davon:

  * T3 und T1 sind VERRIEGELT, nicht durch Zahlenwahl sondern durch Bau.
    est_link() macht stop_timer(&axp->t3), bevor es T1 startet - im Aufbau
    laeuft T3 gar nicht. Verbunden laeuft T3 nur bei unack == 0
    ("stop_timer(&axp->t1); start_timer(&axp->t3);"), und wenn T3 feuert,
    macht tx_enq() als Letztes wieder stop t3 / start t1. Es laeuft immer
    genau einer von beiden. T4 muendet in dieselbe Funktion.
  * T5 laeuft im Aufbau nicht. Alle acht start_timer(&axp->t5) sitzen auf
    verbundenen Pfaden (UA in SETUP, angenommenes SABM, I-Rahmen, eigenes
    Senden). Ein langer Aufbau kann also nicht vom Idle-Disconnect
    abgeschnitten werden.
  * N2 ist kein Gegenspieler, sondern der MULTIPLIKATOR: es begrenzt die
    Anzahl, nicht die Zeit, und die Gesamtdauer ist rund 33 mal T1init.
    N2=10 wurde fuer eine Welt gewaehlt, in der T1 fuenf Sekunden war.
    (Auf einem Relaisbein gilt N2 ohnehin nicht, dort ist bei retries > 2
    Schluss - siehe den eigenen Abschnitt weiter unten.)

DER VERLORENE UA, GEMESSEN (Thomas' zweite Frage)
Geht ein UA auf der Strecke verloren, laufen die beiden Seiten auf
Uhren ohne jeden Bezug zueinander: B haelt sich in dem Moment fuer
verbunden, in dem sein UA hinausgeht (lapb.c setzt CONNECTED und startet
T3 und T5; sein T1 laeuft gar nicht, weil er kein Kommando offen hat),
waehrend A weiter im Aufbau auf T1 und N2 sitzt.

Gemessen mit verkleinerten Timern (T1 2 s, T3 8 s, N2 3, Massstab 1:100)
und eaxpeer.py --ua-verloren:

     0,0 s  SABM -> UA, das UA wird verworfen
     8,0 s  B fragt nach: RR(P)     A schweigt (LAPB_SETUP faellt in
    10,0 s  RR(P)                   "default: All other frames ignored")
    12,5 s  RR(P)
    15,6 s  RR(P)
    19,5 s  B gibt auf und schickt DM

Auf die Vorgaben hochgerechnet endet B's Phantomverbindung bei T3 plus
seinem eigenen Zyklus:

  Digis   A gibt auf     B gibt auf              Phantom
   0        166 s         1066 s (DM)            15,0 min
   2        831 s         1731 s (DM)            15,0 min
   8       2826 s         3600 s (T5 schickt DISC, bevor der Zyklus
                          bei 3726 s fertig waere)   12,9 min

ZWEI DINGE DARAN. Erstens ist der Abstand IMMER T3, denn beide Seiten
rechnen mit demselben T1 mal demselben N2 - A ist genau eine
Viertelstunde frueher fertig. Deshalb trifft B's DM einen A, der schon
weg ist, und der Fall "DM im Aufbau heisst 'abgelehnt'" (lapb.c, case DM
in LAPB_SETUP, reason LB_DM) schlaegt bei symmetrischen Einstellungen
NICHT zu. Er braucht eine Asymmetrie: ein groesseres N2 oder T1init auf
der A-Seite, einen Linux-Partner mit seinem laengeren Zyklus, oder
verschiedene Digizahlen auf Hin- und Rueckweg. Dann ist die Diagnose
falsch - "abgelehnt" fuer einen Versuch, den das naechste SABM getragen
haette.

Zweitens, und das ist fuer die Auslegung wichtiger: die Phantomdauer
haengt an T3, nicht an T1. Ein Deckel auf T1 verkuerzt A's Zappeln, aber
nicht B's Irrtum.

WO DIE VIERZIG MINUTEN WOHNEN
Thomas' Zahl aus dem ersten Einwand - "40 min fuer das naechste SABM" -
steht wirklich im Code, und zwar in Blimit. Das ist 16, N2 nur 10, also
DUERFTE die Kurve sechzehnmal wachsen und wird bloss zufaellig frueher
gestoppt. Der sechzehnte Abstand waere bei acht Digis

    85 s * 1,25^15 = 2416 s = 40,3 min

Sie ist also nicht gegriffen, sondern das, was die vorhandene Bremse
erlaubt - und wer N2 hochsetzt, bekommt sie geliefert.

DER DECKEL GEHOERT AUFS INKREMENT, NICHT AUF DIE GESAMTDAUER
Ich hatte zuerst vorgeschlagen, die Aufbauphase bei T3 zu kappen. Thomas'
Einwand dagegen ist richtig und beendet die Variante: das nimmt dem
Nutzer Versuche weg (bei acht Digis sechs statt zehn), und wer zehnmal
probieren will, soll das duerfen. Stoerend ist nicht die Zahl der
Versuche, sondern der ABSTAND zwischen zweien.

Also den Wachstumswert deckeln und alle zehn Versuche lassen. Mit
C = 120 s, Thomas' Wahl:

  Digis  SABM-Zeitpunkte (s)                        Summe   groesster
                                                             Abstand
   0 h   0 5 11 19 29 41 56 75 99 129                166 s     37 s
   0 C   unveraendert
   2 h   0 25 56 95 144 205 281 377 496 645          831 s    186 s
   2 C   0 25 56 95 144 205 281 377 496 616          736 s    120 s
   8 h   0 85 191 324 490 698 957 1281 1687 2193    2826 s    633 s
   8 C   0 85 191 311 431 551 671 791 911 1031      1151 s    120 s

Der Nahbereich bleibt unberuehrt, acht Digis fallen von siebenundvierzig
auf neunzehn Minuten, und kein Abstand ueberschreitet zwei Minuten. Bei
zwei Digis greift der Deckel am Ende leicht (186 auf 120 s) - das ist
gewollt, nicht uebersehen.

WAS DIE ZAHLEN SAGEN, und daran haengt die Auslegung: der Deckel regelt
den ABSTAND direkt und die SUMME nur nebenbei. Bei acht Digis faellt der
Abstand um 81 Prozent, die Summe um 59 - der Rest steckt in T1(0) = 85 s,
und die kommt aus der Digi-Schaetzung. Die Sache zerfaellt damit sauber
in zwei Werkzeuge fuer zwei verschiedene Beschwerden:

  absurder ABSTAND zwischen zwei SABMs   Deckel aufs Inkrement
  absurde SUMME bis zum Aufgeben          T1(0) reparieren, also die
                                          FlexNet-Schaetzung oder der
                                          Start bei T1init

Beide zusammen ergeben 166 s Summe und 37 s groessten Abstand, und zwar
UNABHAENGIG von der Digizahl - die 0-Digi-Zeile gilt dann fuer alle.

DER EINGRIFF ist eine Zeile, und zwar genau die, in der Blimit schon
sitzt (lapbtime.c):

    if(axp->retries < Blimit)
	    set_timer(&axp->t1,(dur_timer(&axp->t1)*5+2)/4);

VORSICHT DABEI, sonst wird daraus ein Fehler: recover() ist der T1 fuer
ALLE Zustaende, nicht nur fuer den Aufbau. In CONNECTED und RECOVERY ist
T1 gemessen (4*mdev+srt), und ein schlichtes min(neu, C) wuerde einen
gemessenen Wert oberhalb von C nach UNTEN ziehen - also auf einer
wirklich zaehen Strecke wiederholen, bevor eine Antwort ueberhaupt
eintreffen kann. Der Deckel darf nur das WACHSTUM begrenzen, nie kuerzen:

    neu = (dur_timer(&axp->t1)*5+2)/4;
    set_timer(&axp->t1, max(dur_timer(&axp->t1), min(neu, C)));

Zu entscheiden bleibt, ob C nur im Aufbau gilt oder ueberall. Fuer
"ueberall" spricht, dass ein Abstand von vierzig Minuten auch in
RECOVERY unsinnig ist; dagegen, dass dort gemessen wird statt geraten und
eine Messung mehr Vertrauen verdient als eine Schaetzung.

WAS ICH VOR DEM BAUEN GEFRAGT HAETTE, damit es nicht zweimal gebaut wird:
  * Faktor und Gnadenfrist. "Umlaufzeit mal Faktor plus 30 s" stand oben;
    mit Faktor 2 und einer typischen Dreihop-Strecke (delay 40, also 4 s
    einfach) kaeme Tmax = 46 s heraus. Ist das die Groessenordnung?
  * Start bei T1init, sobald eine Schaetzung da ist - ja oder nein? Das ist
    die Variante, die die Digizahl ganz aus der Rechnung nimmt, und sie ist
    die groessere Verhaltensaenderung von beiden.
  * Gilt das Limit auch fuer den NET/ROM-L4-Aufbau und den Proxy-Connect?
    Oben steht, NETROM tauge dafuer als Indiz; dann waere es dieselbe
    Auskunft an einer zweiten Stelle, aber ein eigener Commit.
  * C ist gesetzt: 120 s, Thomas' Wahl (2026-08-18). Offen ist nur noch,
    ob es fest verdrahtet wird oder als "ax25 tmax" einstellbar. Fuer
    einstellbar spricht, dass jeder andere Timer hier es auch ist; dagegen,
    dass eine Zahl mehr auch eine Zahl mehr zum Falscheinstellen ist.
  * Gilt C nur im Aufbau oder in jedem Zustand? ENTSCHIEDEN am 2026-08-18:
    NUR IM AUFBAU. Thomas' Begruendung, und sie ist besser als meine
    Warnung von oben: ein Connect, der nach max retry scheitert und beim
    zweiten Anlauf gelingt, hat die aufsummierten T1 beider Versuche
    gekostet - und wenn die Verbindung dann endlich steht, soll sie nicht
    nach zehn oder fuenfzehn Minuten wieder abgebaut werden. Die
    urspruengliche Idee des T1 bleibt bei bestehender Verbindung also
    unangetastet. Er wollte ausdruecklich nicht auf gut Glueck den
    Normalbetrieb gegen den Standard verstellen.

------------------------------------------------------------------------
6PACK UND PACLEN - ERLEDIGT, MIT DER RICHTIGEN ZAHL (2026-08-17)
------------------------------------------------------------------------
GESCHLOSSEN ist die Falle: sixpack_encode() verwarf zu lange Rahmen
wortlos. Das zaehlt jetzt in sp->errors, struct iface hat framemax (der
Treiber setzt es beim Attach, 510 fuer 6pack, NR4MAXINFO fuers NET/ROM-
Interface), ifmtu() weist hoehere Werte ab und ax25_apply_iface_limits()
kappt paclen darauf. paclen, maxframe und emaxframe sind ausserdem je Port
setzbar.

DIE FRAGE "muss SIXP_MAX_FRAME bei 512 stehen" IST ERLEDIGT, und zwar
anders als gedacht. Thomas' Hinweis: der 6pack-Treiber ist UNSERER, gebaut
2026-08-16, die Puffergroesse war unsere Schaetzung, samt der Notiz, sie
fuer EAX25 vielleicht anzupassen. Ein selbstgelegtes Ei also - nur bindet
die Gegenstelle frueher:

  6pack.c:169   if (len > sp->mtu) ... out_drop
  6pack.c:590   sp->mtu = AX25_MTU + 73          /* 256 + 73 = 329 */
  6pack.c:573   len = dev->mtu * 2               /* Puffer 2*256+4 = 516 */

Der Linux-Treiber wirft alles ueber 329 Byte weg. Unsere 510 sind also
schon die groessere Zahl, und die geschaetzten 512 liegen fast genau auf
seinen 516. Erhoehen braechte gegen einen Linux-Partner nichts.

MODULO-128 IST DARIN ENTHALTEN: include/net/ax25.h:26 setzt
AX25_HEADER_LEN auf 17, also 14 Adresse + ZWEI Steuerbytes + PID. Es gibt
keine Ein-Byte-Klippe bei EAX25 mit vollem Digi-Pfad; 256 paclen + 8 Digis
+ 2 Steuer + PID sind genau die 329.

WAS BLEIBT, ist eine Zahl fuer die Doku und keine Aufgabe: ueber 6pack ist
paclen ueber 256 gegen Linux nicht nutzbar, ganz gleich was wir einstellen.

------------------------------------------------------------------------
AX.25-RELAIS: DER DIGIPEAT-WEG WAR VIER TAGE KAPUTT (2026-08-17, behoben)
------------------------------------------------------------------------
Gefunden beim Messen von "eax25 always", betrifft aber NICHT EAX25 sondern
jede Verbindung, die durch uns hindurchgeht: wer uns als Digi im Adressfeld
nennt, bekam seit dem 13.08. keine Verbindung mehr. Sichtbar war nur, dass
der Knoten das UA der Gegenstelle empfaengt und nichts damit tut.

DIE URSACHE. Commit a0641c7 ("Listen for callsigns of our own choosing")
hat vier Kopien der Frage "ist diese Adresse meine" zu ax_answers_to()
zusammengefasst, und die kennt drei Quellen: das Rufzeichen des Interfaces,
die Rufzeichen, unter denen unsere Links geoeffnet wurden, und die, auf die
wir lauschen. Die zweite Quelle liest axp->hdr.source ueber ALLE
Kontrollbloecke - und dieses Feld bedeutet zweierlei:

  bei einem eigenen Link      unser Rufzeichen, auf das wir antworten
  bei einem RELAISBEIN        das Rufzeichen des RUFERS, das wir
                              weitertragen, damit die Gegenstelle
                              "er ueber uns" sieht (axroute())

Auf das zweite antworten wir nicht, wir sprechen in seinem Namen. Es zu
zaehlen liess lapb_input() die Antwort der Gegenstelle fuer an uns gerichtet
halten.

WARUM DAS DURCHGERUTSCHT IST, und das ist der eigentliche Lerngegenstand:
die Aenderung war mit "der Satz wird nur groesser" begruendet. Das stimmte
auch. Nur ist an dieser Stelle keine Filterfrage gestellt, sondern eine
WEICHE, und beide Zweige tun etwas:

  digipeat = (ismyax25addr(dest) == NULL && !ax_answers_to(iface,dest));
  if (digipeat)  ... eigene Suche ueber das Adresspaar - findet NUR
                     Relaispaare
  else           ... find_ax25(), und die ueberspringt Relaisbeine
                     ausdruecklich (axp->peer == NULL)

Die beiden Zweige teilen die Kontrollbloecke unter sich auf, und jeder kann
nur seine Haelfte finden. Ein weiteres Wahrwerden der Bedingung verschiebt
also Rahmen von dem Zweig, der sie findet, in den, der sie nicht findet.
"Nur groesser" ist bei einer Weiche keine sichere Richtung - bei einem
Filter waere es eine gewesen.

Dass find_ax25() die Unterscheidung schon trifft, stand direkt im anderen
Zweig derselben if-Anweisung. Die Korrektur ist genau diese: ein eigener
Link hat keinen peer. axp->peer wird an genau einer Stelle gesetzt
(lapb.c:87), naemlich fuer dieses Paar, also ist der Test scharf.

WARUM ES VIER TAGE UNBEMERKT BLIEB: die Gegenprobe zu a0641c7 war ein
Connect AUF ein gelauschtes Rufzeichen - der Fall, um den es ging, und der
funktionierte. Fuer den Weg DURCH uns gab es keinen Prueftreiber; genau das
stand als "eaxpeer.py kann keinen Digipeat-Pfad" im TODO. Der einzige Pfad,
den die Aenderung zerbrach, war der einzige ohne Pruefstand. Und der Fehler
ist stumm: keine Meldung, kein Zaehler, von aussen sieht es aus, als
antworte die Gegenstelle nicht.

Gegengeprobt sind beide Richtungen: zwei Staende nebeneinander gebaut, mit
und ohne Korrektur, auf reiner AX.25-Strecke ohne jedes modulo-128 - der
alte laesst das Relais in "Conn pend" haengen, der neue bringt es durch.
Und "listen ax25 add", wofuer a0641c7 gebaut war, antwortet weiter.

------------------------------------------------------------------------
EAX25: "always" IST GEMESSEN (2026-08-17)
------------------------------------------------------------------------
ERLEDIGT. eaxpeer.py kann jetzt einen Digipeat-Weg: --via legt den Digi ins
Adressfeld, --zielstation spielt das andere Ende auf einem EIGENEN Socket
(noetig, weil axip sich den UDP-Quellport je Host UND Interface merkt),
--strom schickt einen durchzaehlenden Datenstrom, --t1 laesst den Rufer sein
SABM wiederholen. Der Pruefstand ist ein Knoten mit zwei axip-Ports:
Benutzerzugang ax0 "accept", Interlink ax1 "always".

GEMESSEN, alles in doc/EAX25.md eingetragen:
  * "always": Rufer kommt mit SABM, das zweite Bein geht mit SABME hinaus.
    200 Rahmen zu 256 Byte, 51200 Byte, lueckenlos durchgezaehlt, N(S) ueber
    127 hinaus und einmal herum. "ax25 status": Mod 128, Unack 0/32 auf dem
    zweiten Bein neben Mod 8 auf dem ersten. Im Routenverzeichnis das E.
  * Gegenprobe "caller": SABM herein -> SABM hinaus, SABME herein -> SABME
    hinaus. Der Unterschied liegt also wirklich am Schalter.
  * Rueckfall im Relais: DM sofort; Schweigen -> SABM nach 19 s, der Rufer
    hat seine Verbindung bei 20,2 s, danach steht das kleine e im
    Routenverzeichnis und der naechste Ruf probt gar nicht mehr.

Der Pruefstand hat dabei einen Fehler aufgedeckt, der nichts mit EAX25 zu
tun hat: siehe den Abschnitt "AX.25-RELAIS" weiter oben. Ohne dessen
Korrektur ist keine dieser Messungen moeglich.

------------------------------------------------------------------------
WIR SAGTEN AUF DEM DRAHT NICHT, WELCHEN MODULUS WIR SPRECHEN (2026-08-17)
------------------------------------------------------------------------
BEHOBEN, und zuerst von mir falsch diagnostiziert. Ich hatte notiert, der
Monitor koenne kein modulo-128 und aus dem Rahmen allein sei es nicht zu
entscheiden. Beides ist falsch. Thomas' Rueckfrage ("trace kann kein
eax25?") hat es aufgedeckt.

Es GIBT ein Bit dafuer: SSID_EAX25 im Absender-SSID, active low,
geschrieben von htonax25() und gelesen von ntohax25(). ax25dump.c
entscheidet daran, ob es ein oder zwei Steuerohktette dekodiert, und
schreibt dann "EAX25:" statt "AX25:". Der Dumper ist also in Ordnung.

Gesetzt wurde das Bit aber an genau EINER Stelle: wenn wir ein SABME
ANNEHMEN (lapb.c). Ein Link, den wir selbst mit SABME oeffnen, lief
deshalb modulo-128 und sagte dabei "kann ich nicht". Folgen: unser eigener
Monitor konnte unsere eigenen Rahmen nicht lesen, jeder fremde auch nicht,
und TNN, dessen "EAXMODE 1, by MHEARD" genau dieses Bit auswertet, konnte
von uns nie lernen, dass wir es koennen.

Die Gegenprobe aus den eigenen Logs war eindeutig - derselbe Knoten, zwei
Faelle:

  Rufer kam mit SABME:   EAX25: DL1BBB->DL9SAU-9 ... RR NR=16
  wir riefen mit SABME:  AX25:  DL9SAU-9->DL1BBB ... I NR=0 NS=0 pid=0x0

mmask ist die Wahrheit, eax25_mark() folgt ihr jetzt - an den zwei
Stellen, die mmask festlegen, und am Rueckfall.

UND ES WAR KEINE KOSMETIK, SONDERN INTEROPERABILITAET (nachgetragen am
2026-08-18, nachdem die TNN-Quellen vorlagen). TNNs Empfangspfad haengt
ausschliesslich am Bit AM RAHMEN und nie am geglaubten Linkzustand:
rxfEAX entscheidet, ob ueberhaupt ein zweites Steuerbyte gelesen wird, wo
das Poll/Final-Bit sitzt und wo es zurueckgesetzt wird (l2rx.c). Ein
unmarkierter I-Rahmen einer modulo-128-Strecke wird dort also als
gewoehnliches AX.25 gelesen. Damit war ein modulo-128-Link, den WIR zu
einem TNN-Nachbarn geoeffnet haben, vom ersten I-Rahmen an unlesbar - nur
die Links, die TNN aufgemacht hat, funktionierten, weil das Annehmen
eines SABME die eine Stelle war, die das Bit setzte. Nachgemessen: das zweite
Bein zeigt "EAX25: ... pid=Text" mit N(S) ueber 127 hinaus, die
modulo-8-Haelfte daneben bleibt "AX25:". Das Bit folgt der Strecke, nicht
dem Knoten.

------------------------------------------------------------------------
EINE PROBE STATT DREI AUF EINEM RELAISBEIN (2026-08-17)
------------------------------------------------------------------------
Kein Fehler, aber es steht nirgends. recover() wiederholt fuer einen Link,
dessen peer noch DISCONNECTED ist, NICHT - es startet nur T1 neu und gibt
nach drei Ablaeufen auf (lapbtime.c, Zweig "axp->peer && peer->state ==
DISCONNECTED"). Die Wiederholung, auf die es ankommt, ist die des RUFERS:
sein naechstes SABM laeuft durch build_path() und sendctl() und schickt
damit die zweite Probe (solange routing_changes < 3).

Gemessen: ein Rufer, der nicht wiederholt, kostet genau EINE SABME und
19 s Stille; einer mit T1 15 s ergibt zwei Proben, bei 0 und bei 15 s, und
den Rueckfall bei 20 s. Gewollt ist das - es wird die Geduld einer Station
ausgegeben statt der von zweien. Die Kehrseite: eine auf der Funkstrecke
verlorene SABME machen wir nicht wieder gut.

------------------------------------------------------------------------
EIN VERSPAETETES UA: WORAUF BEZIEHT ES SICH? (Thomas, 2026-08-18)
------------------------------------------------------------------------
Seine Frage: wenn wir SABME senden, keine Antwort bekommen, auf SABM
zurueckfallen und DANN ein UA eintrifft - sagt irgendetwas, welches der
beiden es beantwortet?

IN AX.25 SELBST: NEIN. Das UA ist ein Oktett, das F-Bit spiegelt nur das
P des Kommandos, und beide, SABME und SABM, gingen mit P hinaus. Es gibt
kein Feld, das die Rueckfrage beantwortet.

WAS ES GIBT, ist SSID_EAX25 - dasselbe Bit wie im Abschnitt darueber. Ein
WAMPES oder TNN, das unser SABME angenommen hat, traegt es im UA. Nur
LESEN wir es nicht: hdr->ext eines eingehenden Rahmens wird in lapb.c an
genau einer Stelle ausgewertet, und dort fuer DAMA. "case UA" entscheidet
aus axp->mmask, also aus UNSEREM Zustand. Ein verspaetetes UA wird damit
dem zuletzt Gesendeten zugeschrieben.

GEMESSEN (T1 auf 2 s verkleinert, eaxpeer.py --ua-spaet): SABME bei 2,6 /
4,0 / 6,5 s, Rueckfall auf SABM bei 9,6 s, das UA bei 14,6 s. Ergebnis am
Knoten: "Mod 8 Connected" - und im Routenverzeichnis ein kleines e, also
"kann kein modulo-128", ueber eine Station, die es gerade bewiesen hat.

WAS DIE DESYNCHRONISATION DANN TUT, beide Richtungen gemessen:

  wir -> er   Unser "I NR=1 NS=0 pid=Text" liest er zweioktettig als
              N(S)=16, N(R)=120 - die PID 0xf0 wird ihm zum zweiten
              Steuerbyte. Ausser der Reihe, also verworfen. Wir
              wiederholen, gehen nach Recovery (gemessen: Rt 7) und geben
              erst nach dem vollen N2-Zyklus auf. Es kommen KEINE Daten
              durch und es faellt nichts auf.
  er -> wir   Sein zweioktettiger I-Rahmen wird von uns einoktettig
              gelesen, wir NEHMEN IHN AN und quittieren ihn (gemessen:
              RR NR=1). Der Inhalt ist um ein Oktett verschoben, und als
              PID lesen wir sein zweites Steuerbyte, also N(R)<<1|P/F.

KEIN FRMR, in keiner Richtung. Die Bedingung wird sogar erkannt -
ackours() gibt bei "Acking unsent frame" -1 zurueck -, aber die Pruefung
beim Aufrufer ist auskommentiert: "ackours(axp,nr,0); /** == -1) */", an
beiden Stellen (lapb.c). Das ist altes KA9Q-Erbe, nicht aus dieser Arbeit.

DIE GEFAEHRLICHE STELLE ist die verschobene PID. N(R)<<1|P/F trifft
0x06, 0x07 und 0x08 - und das sind PID_VJCOMP, PID_VJUNCOMP und
PID_SEGMENT. Bei N(R)=0 kommt 0x00 heraus, das in Axlink[] niemand
beansprucht, und genau das zeigte die Messung. Gebunden ist N(R) aber
nicht: hat vor dem Auseinanderlaufen noch nichts in unsere Richtung
gezaehlt, nimmt die Gegenstelle unsere ersten Rahmen an (ihr gelesenes
N(S) stimmt, solange unser N(R) 0 ist), ihr N(R) waechst - und bei 3
oder 4 landet ein um ein Byte verschobener Inhalt im VJ-Dekompressor
oder in der Segment-Zusammensetzung.

DIE ABWEHR - UND MEIN ERSTER VORSCHLAG DAZU WAR FALSCH. Ich hatte
geschrieben, man solle das SSID_EAX25 des eingehenden UA lesen und ihm
glauben, und dabei behauptet, TNN setze das Bit. Thomas hat nachgefragt,
und die TNN-Quellen sagen das Gegenteil (l2tx.c):

    if ((txfEAX == TRUE) && ((txfctl & 0x03) != 0x03))
      txfhdr[L2ILEN - 1] &= ~L2CEAX;

(txfctl & 3) != 3 heisst "KEIN U-Rahmen". TNN markiert also nur I- und
S-Rahmen, niemals ein UA. Und beim Empfang steht ausdruecklich
"Nur auf EAX.25 umschalten wenn SABME, EAX-Flag ignorieren" (l2rx.c).
Ein UA von TNN traegt das Bit nicht - mein Vorschlag haette daraus
"modulo-8" geschlossen, waehrend TNN auf modulo-128 umgeschaltet hat,
also genau die Desynchronisation ERZEUGT, die er verhindern sollte.

WAS TNN STATTDESSEN TUT, und das ist der bessere Bau: das Bit ist dort
kein Faehigkeitsmerkmal, sondern eine RAHMENMARKIERUNG. Beim Empfang
entscheidet es je Rahmen, ob ein zweites Steuerbyte gelesen wird:

    rxfEAX = !(rxfhdr[L2ILEN-1] & L2CEAX);
    if (rxfEAX && !((rxfctl & 0x03) == 3))
      rxfctlE = getchr(fbp);

Damit ist TNN gegen eine Modulus-Desynchronisation in Empfangsrichtung
von selbst unempfindlich - es parst, was markiert ist, statt was der
Link zu sein glaubt. WAMPES liest axp->mmask und ist es nicht. Und es
degradiert richtig: eine alte Station laesst die reservierten Bits auf 1,
also kommt rxfEAX = FALSE heraus und es wird einoktettig gelesen.

Seine Faehigkeitserkennung zieht TNN woanders her - aus dem GEHOERTEN
SABM/SABME (l7moni.c: "case L2CSABME: mhp->eax_link = TRUE"), nicht aus
dem Flag. "EAXMODE 1, by MHEARD" meint das, und nicht das SSID-Bit.

UND GENAU DA HAT TNN EINEN FEHLER, den Thomas benannt hat, bevor er die
Stelle sah: dieselbe Auswertung setzt das Merkmal bei einem gehoerten
SABM wieder ZURUECK ("case L2CSABM: mhp->eax_link = FALSE"), unter
MHEAX_LINKFIX sogar bei einem UA. Ein Benutzer darf aber zu der einen
Station mit SABME und zur anderen mit SABM verbinden - beides aus guten
Gruenden -, und sein SABM sagt dann nichts darueber, was er KANN, sondern
nur, was er gerade WOLLTE. Wer daraus "kann kein EAX.25" macht, verlernt
bei jedem gemischten Betrieb wieder, was er gerade gelernt hatte.

WIR MACHEN DAS RICHTIG, und das ist keine Absicht gewesen sondern faellt
aus dem Aufbau: eax25_remember() wird beim Empfang eines SABM gar nicht
gerufen. AXR_EAX25_NO entsteht nur aus einem DM oder FRMR auf UNSER SABME
oder aus Schweigen mit anschliessendem UA auf unser SABM - also immer aus
einem Versuch, den WIR gemacht haben, nie aus dem, was jemand mit einem
Dritten treibt.

BLEIBEN ZWEI WEGE, und Thomas' ist der erste:
  1. DEN RUECKFALL GAR NICHT SENDEN, wo wir Beweise haben. Der
     Routeneintrag kennt AXR_EAX25_YES, und der steht nur nach einem UA
     auf unser SABME oder einem SABME von ihm - also nach einem
     nachweislich gelungenen modulo-128-Aufbau. Zu so einem Partner
     keinen SABM-Rueckfall zu schicken heisst, dass ein UA gar nichts
     anderes beantworten KANN. Die Mehrdeutigkeit entsteht dann nicht,
     statt hinterher aufgeloest zu werden.
     THOMAS' EINWAND DAGEGEN, und er wiegt schwerer als mein
     Milderungsvorschlag: was, wenn der gelernte Partner erst in sechs
     Stunden wieder ruft? Dann steht "kann es" im Gedaechtnis, waehrend
     die Technik laengst getauscht ist - und wir proben bis zum Ende des
     N2-Zyklus ohne Rueckfall, also gar kein Connect mehr. Ein
     Dauerausfall gegen eine seltene Fehldeutung eingetauscht, und wir
     haben gerade gelernt, wohin SABME-Missverstaendnisse fuehren.
     DAS ERLEDIGT DIE VARIANTE IN DIESER FORM. Ein Gedaechtnis, das
     mit der Route altert (24,8 Tage), taugt nicht als Grundlage. Wenn
     ueberhaupt, dann mit einer EIGENEN, kurzen Frist - Minuten bis
     Stunden -, und das ist ein zusaetzliches Feld und eine zusaetzliche
     Entscheidung.

  3. DIE LEBENDE GEGENPROBE, und die kommt ohne jedes Gedaechtnis aus.
     Halten wir einen Link fuer modulo-8 und empfangen darauf einen I-
     oder S-Rahmen, dessen EAX-Bit gesetzt ist, dann ist das ein
     Widerspruch AUS DIESEM LINK, JETZT - kein gespeicherter Glaube,
     sondern der Rahmen in der Hand. Umgekehrt genauso. Die richtige
     Antwort darauf ist ein Neuaufbau: einmal Umlaufzeit kostet er, und
     danach stimmt der Modulus, statt dass stillschweigend falsch
     geparst wird.
     Nur auf I- und S-Rahmen pruefen, denn genau die markiert TNN; auf
     U-Rahmen unterscheiden sich die Gepflogenheiten (wir markieren sie,
     TNN nicht), und dort waere die Pruefung eine Fehlerquelle statt
     einer Absicherung.
     Falsch anschlagen kann sie bei einer Station, die die reservierten
     Bits pauschal nullt. Selten, und die Folge - ein Neuaufbau - ist
     verkraftbar.

     UND SIE KOSTET NICHTS, was Thomas' Einwand gegen die naheliegende
     Variante beantwortet. Er hatte ueberlegt, aus gehoerten I- und
     S-Rahmen mit EAX-Bit einen Sprecher zu MARKIEREN, und selbst
     eingewandt, dass man dafuer jedes Paket ansehen und die MHeard
     nachtragen muesste - fuer den mageren Gewinn, eine ueberfluessige
     Probe zu sparen. Richtig, aber das ist eine ANDERE Sache:

       Faehigkeit aus fremdem Verkehr lernen
         wo      jeder gehoerte Rahmen, auch der an Dritte
         Kosten  Bittest je Rahmen plus Eintrag in eine Liste
         Nutzen  spart eine Probe (19 s, einmal je Station) - aber nur,
                 wenn wir ihn ueberhaupt mit Dritten hoeren, und auf
                 einem exklusiven Interlink gibt es keine Dritten. Genau
                 dort, wo modulo-128 sich lohnt, faellt der Nutzen weg.
                 Ausserdem beweist fremder Verkehr nur, dass es mit
                 JEMAND ANDEREM ueber EINEN ANDEREN Weg geht - dieselbe
                 Schlussweise, die wir oben gerade verworfen haben.

       Widerspruch auf dem EIGENEN Link erkennen
         wo      nur Rahmen unserer eigenen Verbindungen
         Kosten  KEINE. ntohax25() rechnet hdr->ext |= SSID_EAX25 auf
                 jedem Rahmen ohnehin aus, und lapb_input() liest
                 hdr->ext heute nur fuer DAMA. Das Bit liegt schon da und
                 wird weggeworfen; es zu benutzen ist ein Vergleich gegen
                 axp->mmask.
         Nutzen  faengt die Desynchronisation, also den echten Schaden -
                 nicht die gesparte Probe.

     Die Probe zu sparen ist ausserdem schon abgedeckt, ohne neue
     Maschinerie: durch den Routeneintrag (AXR_EAX25_YES, kostenlos, nur
     eben alternd) und auf einem Interlink durch die Konfiguration, wo
     der Betreiber es ohnehin weiss - dafuer gibt es "eax25 always".
  2. JE RAHMEN PARSEN wie TNN, statt je Link. Das repariert auch eine
     Desynchronisation, die auf anderem Weg entstanden ist, und kostet
     eine Abfrage im Empfangspfad.
Die beiden schliessen sich nicht aus: der erste vermeidet, der zweite
faengt auf.

------------------------------------------------------------------------
PROTOKOLL-GATTER JE PORT, STATT DER DRITTEN ZUGANGSSTUFE (Thomas, 2026-08-18)
------------------------------------------------------------------------
STATUS: GEBAUT UND GEMESSEN (2026-08-18), beide Richtungen.
"ifconfig <iface> pid in|out block|allow <protokoll>...", Namen oder
Zahlen, Einzelheiten und Messwerte in doc/PID-FILTER.md. Der Rest dieses
Abschnitts ist die Ueberlegung, die dahin gefuehrt hat, und stimmt.

WAS BEIM BAUEN DAZUKAM, und es war nicht vorherzusehen:

  * Die Ausgangsrichtung hat FUENF Torstellen, nicht eine. ax_send_ui(),
    ax_output(), send_ax25() zweimal - mit PID als Argument und mit -1,
    wo sie schon im Rahmen steht - und broadcast_to() in netrom.c. Das
    letzte ist der einzige Sendeweg des Knotens, der nicht ueber
    ax_send_ui() geht: alloc_broadcast_packet() schreibt UI und PID
    selbst in den Puffer und gibt den Rahmen direkt an den Treiber. Ohne
    diese Stelle haette "pid out block netrom" alles ausser dem
    Nodes-Broadcast gestoppt.
  * doifconfig() liest NAME/WERT-PAARE. Ein Unterbefehl mit einer Liste
    passt da nicht hinein; er bekommt jetzt den Rest der Zeile und muss
    deshalb als letztes stehen. Nebenbei fiel dabei der unerreichbare
    Zweig aus Punkt 4 der Durchsicht weg: "argc == 3" meldete
    "Argument missing", bevor der Unterbefehl je gerufen wurde.

THOMAS' EINSCHRAENKUNG, und sie ist im Bau so umgesetzt: das Gatter
betrifft nur Rahmen, die AN UNS gehen oder VON UNS kommen. Was wir bloss
digipeaten, ist unberuehrt - die Weiterleitung in ax_recv() entscheidet,
bevor irgendwer eine PID gelesen hat, dort gibt es nur eine Adresse zum
Weiterruecken. Es auch auf durchgereichten Verkehr anzuwenden waere ein
eigener Schalter; seine Entscheidung dazu: waere machbar, aber es lohnt
die Verkomplizierung nicht.

Gegen die dritte Zugangsstufe "darf gar nicht sprechen" sprach nie etwas -
sie wurde fuer die zwei Achsen einfach nicht gebraucht, und in
doc/ROUTE-FILTER.md steht nur, dass "in none" nahekommt und nicht dasselbe
ist (die Rahmen werden weiter geparst, der Peer existiert weiter).

THOMAS' VERALLGEMEINERUNG IST DIE BESSERE FASSUNG: die Regel nicht an
Protokollen festmachen, sondern an PIDs. Dann heisst "kein NET/ROM von
diesem Port" dasselbe wie sein Beispiel "PID=ip weg, in UI und in I".

Und das ist billiger als die urspruengliche Idee, weil es genau EINE
Tabelle gibt:

    struct axlink Axlink[] = {          /* config.c */
        { PID_IP, axip }, { PID_ARP, axarp },
        { PID_FLEXNET, flexnet_input }, { PID_NETROM, axnr }, ...

benutzt von ax_recv() fuer UI UND von lapb.c fuer den verbundenen Fall.
Ein Gatter je Port vor diesem Nachschlagen deckt beide Rahmenarten in
einem Griff ab.

DER UNTERSCHIED ZUM ROUTE-FILTER, und hier hatte ich mich zuerst vertan:
ich hatte geschrieben, das Gatter greife je PORT und der Filter je
RUFZEICHEN. Das stimmt nicht - der Filter kennt drei Formen, "default",
"port=<name>" und "<call>", und "flexnet filter port=useraccess2m in
none" ist genau der Portfall.

Der Unterschied ist nicht der SCHLUESSEL, sondern die TIEFE:

  in none      der Rahmen wird geparst, der Peer existiert, wir sprechen
               weiter mit ihm - wir LERNEN nur nichts von ihm
  PID-Gatter   der Rahmen erreicht das Protokoll gar nicht, es gibt
               keinen Peer und keinen Zustand

Damit ist Thomas' Frage beantwortet, ob "filter port=xx in none" dann
entfallen kann: nein, aber aus diesem Grund und nicht aus meinem. Die
beiden sind zwei Stufen derselben Leiter, nicht zwei Schluessel. Dass
der Filter zusaetzlich einzelne Rufzeichen treffen kann, kommt hinzu -
das kann ein Portgatter nicht.

Zu klaeren beim Bauen: die Reihenfolge. Das Gatter wirkt VOR dem Parsen,
der Filter danach - das gehoert in die Doku, sonst sucht jemand den Fehler
im Filter, waehrend das Gatter den Rahmen schon verworfen hat.

------------------------------------------------------------------------
DER FEHLENDE OUT-FILTER - UND DASS ES ZWEIMAL DASSELBE IST (2026-08-18)
------------------------------------------------------------------------
Zwei Punkte, die Thomas aus verschiedenen Richtungen aufgemacht hat und
die auf dieselbe Sache hinauslaufen:

  * "flexnet filter ... in ..." gibt es, "out" fehlt.
  * der VERKUERZTE Broadcast je Eintrag, oben unter Route-Filter.

Beides ist derselbe fehlende Freiheitsgrad: die vorhandenen Achsen sagen,
OB jemand etwas erfaehrt (advert, feed), keine sagt WIE VIEL. Ein
"out"-Filter ist genau der Inhaltsfilter, der bei NET/ROM "verkuerzter
Broadcast" heisst.

SEIN ANWENDUNGSFALL, und er ist besser als der, mit dem der Punkt
urspruenglich aufgeschrieben war: WAMPES als USER-Station. Wir senden
NODES und kuendigen keine Nachbarn an. Fuer einen Benutzer bringt das
wenig - er kennt die Knoten des Netzes nicht und kann nicht nach hinten
connecten, allenfalls im NET/ROM-Protokoll zu uns. Wer aber WAMPES selbst
als User betreibt, will nicht, dass der Funkkanal mit den eigenen Knoten
verstopft wird, die man sich womoeglich ueber einen Internet-Tunnel
geholt hat. Der verkuerzte Broadcast ist fuer ihn der Ausgangsfilter.

SEINE EINSCHRAENKUNG ZU FLEXNET, mitnotiert, weil sie gegen den Bau dort
spricht: wenn die Gegenseite nichts empfaengt, haelt sie den Weg
vielleicht fuer tot und gibt ihn auf. Bei NET/ROM, wo der Broadcast an
alle auf dem Port geht, ist das nicht so. Also: bei NET/ROM sinnvoll, bei
FlexNet erst nach genauerem Hinsehen.

NACHTRAG (2026-08-18): DER VERKUERZTE BROADCAST WAR SCHON DA, und das ist
die eigentliche Erkenntnis dieses Punktes. "feed no" tut genau das - bei
NET/ROM geht der Broadcast mit Identifier und ohne Eintraege hinaus, bei
FlexNet FLEX_INIT ohne FLEX_ROUT. Beides gemessen:

    net.rc                             DL1ABC-7 hoert
    (nichts)                           FLEX_INIT + FLEX_ROUT, drei Ziele
    flexnet filter port=axip feed no   FLEX_INIT, sonst nichts
    ifconfig axip pid out block flexnet   gar nichts

    net.rc                             der NET/ROM-Nachbar hoert
    (nichts)                           Ident + drei Eintraege
    netrom filter port=axip feed no    Ident, keine Eintraege
    ifconfig axip pid out block netrom gar nichts

Es fehlte also nicht der Freiheitsgrad, sondern der Weg, ihn zu finden.
Thomas hat den Punkt selbst so aufgemacht - "fuer den ui broadcast haben
wir schon fast alles, alleine die default-Einstellung fuers iface fehlt"
-, und die Antwort darauf ist keine neue Achse, sondern eine Spalte:
"netrom broadcast" zeigt jetzt Contents (nodes / us only) neben State,
und der Usage-Text sagt, mit welchem Befehl man sie umstellt. Zwei Wege,
dieselbe Sache zu sagen, waeren schlechter als einer, den man findet.

WAS DAMIT WIRKLICH FEHLT, enger gefasst als oben: ein TEILWEISER
Broadcast - nur ein Teil der Tabelle statt alles oder nichts. Alles und
nichts gibt es beides. Das steht unter "Not built" in
doc/ROUTE-FILTER.md und bleibt dort.

------------------------------------------------------------------------
FILTER-KOMMANDO: KEINE HILFE, UND ES LEGT PHANTOMSTATIONEN AN (Thomas,
2026-08-18)
------------------------------------------------------------------------
Von Thomas beim Ausprobieren gefunden:

    dl9sau> flex filter add
    dl9sau> flex fil
    Station           In         Advert  Feed
    ADD               all        yes     yes        <- eine Station "ADD"
    dl9sau> flex filter add ?
    Only one station per line
    dl9sau> netrom fi ?
    Station           In         Advert  Feed
    ?                 all        yes     yes        <- und eine "?"

URSACHE, zwei Stellen:

  1. rf_cmd() nimmt JEDES Wort, das kein Schluesselwort ist, als
     Stationsangabe. "add" gibt es in der Syntax gar nicht - geloescht wird
     mit "--delete" -, also wird "add" zum Rufzeichen. Beim zweiten Wort
     ist der Platz belegt, daher die irrefuehrende Meldung "Only one
     station per line" auf ein "?".
  2. setcall() prueft nur Laenge <= 6 und SSID <= 15, keine
     Zeichenklassen. "ADD" und "?" gehen also glatt durch.

MEIN VORSCHLAG MIT lib/callvalid.c WAR FALSCH, und Thomas hat gesagt
warum: "filter flexnet add haette ich nie versucht wenn die Hilfe da
gewesen waere." Der Pruefer haette das Symptom behandelt und die Ursache
stehen lassen. Zwei weitere Gruende, beide ausreichend fuer sich:

  * Er laesst hoechstens zwei Ziffern zu - ein Sonderrufzeichen wie
    DL60ABC faellt durch. Ein Pruefer, der gueltige Eingaben abweist, ist
    schlechter als keiner.
  * Und das ist der Punkt, den ich uebersehen hatte: das Argument kann
    "default", "port=<name>" ODER ein Rufzeichen sein. Zwei der drei
    Formen sind gar keine Rufzeichen. Ein Rufzeichenpruefer haette die
    Frage also nie beantworten koennen, egal wie streng.

GEBAUT WURDE STATTDESSEN: die Hilfe (siehe unten), und eine Zeile fuer
den Fall, den Thomas tatsaechlich gemacht hat - "add", "delete", "list",
"show" und Verwandte werden ausdruecklich abgewiesen mit dem Hinweis,
dass es die hier nicht gibt und wie es stattdessen geht. Sonst bleibt es
bei setcall(): mehr Strenge ist beim Rufzeichen nicht gewollt.

DAZU FEHLT DIE HILFE. Die Kommandotabellen tragen fuer "filter" keinen
Usage-Text (flexnet.c und netrom.c, letztes Feld 0 bzw. NULL), waehrend
die Nachbarn einen haben - "flex li ?" listet seine Unterbefehle, "flex
query" zeigt seine Syntax. Deshalb sagt "flex fi ?" nichts.

ERLEDIGT (2026-08-18). Der Hilfetext steht einmal in routefilter.c
(Rf_usage_flexnet / Rf_usage_netrom) und die beiden Kommandotabellen
zeigen darauf, damit die Formulierung nicht zweimal existiert. "?" und
"help" beantworten ihn an jeder Stelle der Zeile.

DAMIT DAS UEBERHAUPT WIRKEN KONNTE, musste cmdparse.c mit: der
Usage-Text wurde bis dahin NUR bei zu wenigen Argumenten gedruckt, und
"filter" hat argcmin 0 - der Text waere also nie erschienen. Jetzt
beantwortet ein "?" als erstes Argument den Usage-Text ueberall, in
cmdparse() wie in subcmd(), und der Befehl wird dabei GAR NICHT gerufen.

ZWEI SACKGASSEN AUF DEM WEG DAHIN, beide notiert, weil sie plausibel
aussehen:
  * "?" ueberall abfangen, auch ohne Usage-Text. Falsch: ein Eintrag
    ohne Text ist meistens ein Multiplexer, und subcmd() beantwortet "?"
    dort mit der LISTE der Unterbefehle. Das ist die bessere Antwort;
    sie ihm wegzunehmen tauscht eine Liste gegen einen Satz ohne Inhalt.
    Gemessen an "flexnet ?", das dabei aufhoerte zu listen.
  * "?" als "keine Argumente" weiterreichen, damit eine Einstellung
    ihren Wert zeigt. Auch falsch: "reset" und "close" ohne Argument
    wirken auf die LAUFENDE Session. "reset ?" haette sie
    zurueckgesetzt. Eine Frage darf nicht mit einer Handlung beantwortet
    werden.

------------------------------------------------------------------------
DURCHSICHT: HILFE UND EINGABEPRUEFUNG DER NEUEN BEFEHLE (2026-08-18)
------------------------------------------------------------------------
Auf Thomas' Frage, ob alle seit dem Import gebauten Befehle eine
Beschreibung fuer ihre Unterbefehle bzw. ein Usage haben. Verglichen
wurden die Kommandotabellen gegen den Stand vor dieser Arbeit (0ecbf55),
danach jeder neue Befehl am laufenden Knoten mit "?" durchprobiert.

IN ORDNUNG:
  axsock, listen, start axtcp        eigener Usage-Text
  ifconfig <if> eax25                "Valid options: off accept caller always"
  ifconfig <if> dama                 "Dama must be off or slave"
  ax25 emaxframe                     "must be 1..63" ueber setintrc
  rip allow/noallow/learn            eigener Usage-Text
("stop axtcp" braucht keinen, und "axtcp" allein ist zu Recht unbekannt -
es ist ein Unterbefehl von start und stop.)

BEFUNDE:

 1. ifconfig <if> tncinit <wort> RISS DEN KNOTEN UM. Eigener Abschnitt
    entfaellt, es ist behoben (Commit "tncinit: a range check is not an
    ownership check"): xdev wird nur in slip.c gesetzt, jedes andere
    Attach laesst die calloc-Null stehen, und Null ist ein gueltiger
    Index. Jetzt fragt tncinit_serial() ueber den Rueckzeiger in
    struct slip, ob der Steckplatz wirklich uns gehoert.

 2. flexnet filter / netrom filter: kein Usage, kein "?", und jedes Wort
    wird zur Station. Eigener Abschnitt weiter oben.

 3. ERLEDIGT. setintrc() NAHM JEDES WORT ALS ZAHL, weil es atoi() benutzt - "?"
    wird zu 0. Das ist alter Code und faellt sonst nicht auf, weil die
    meisten Bereiche bei 1 anfangen und die 0 dann abgewiesen wird
    ("must be 1..63"). Bei UNSEREN Portwerten ist die 0 aber gueltig und
    bedeutet "nimm die des Knotens" - ein Tippfehler setzt den Port also
    STILL zurueck. Gemessen:

        ifconfig ax0 paclen 128
        ifconfig ax0     ->  paclen 128 maxframe 7 emaxframe 32
        ifconfig ax0 paclen ?        (keine Ausgabe)
        ifconfig ax0     ->  die Zeile ist weg

    Es wurde das Erste: cmd_getnum() in cmdparse.c, strtol mit
    Endzeiger- und ERANGE-Pruefung, und setint, setlong, setshort,
    setuns und setintrc gehen alle hindurch. Ein Wort, das keine Zahl
    ist, wird jetzt abgewiesen statt als Null genommen. Basis 10 und
    NICHT strtols Basis 0 - "010" hat hier immer zehn geheissen, und das
    ohne ein Wort in acht zu verwandeln waere eine stille Aenderung an
    bestehenden net.rc.

    Nachgemessen:
        ifconfig axip paclen 128
        ifconfig axip paclen ?     -> Usage, Wert bleibt 128
        ifconfig axip paclen quatsch -> "ist keine Zahl", Wert bleibt 128

 4. ERLEDIGT. iftncinit() hatte einen unerreichbaren Zweig fuer
    "argc < 2", weil doifconfig() bei argc == 3 vorher "Argument
    missing" meldete. Der Fall geht jetzt in den Unterbefehl: der zeigt
    seinen Wert, oder subcmd() druckt seinen Usage-Text - beides bessere
    Antworten als ein Satz, der keinen Befehl nennt.

 5. NEU BEIM DURCHGEHEN (2026-08-18): "listen ax25 drop" nahm "pid="
    nur NACH dem Wort I/UI an, "listen ax25 add" in beliebiger
    Reihenfolge. "drop pid=netrom UI <call>" - dieselbe Zeile, mit der
    der Eintrag angelegt wurde - fand deshalb nichts. Jetzt beide in
    beliebiger Reihenfolge.

 6. NOCH OFFEN, aber nur noch Fleissarbeit: die ALTEN Befehle aus der
    Zeit vor dem Import haben groesstenteils keinen Usage-Text. "?"
    beantwortet dort nichts (bei den Multiplexern listet subcmd die
    Unterbefehle, das ist in Ordnung). Betroffen sind unter anderem
    echo, eol, escape, log, trace, topt, hostname, record, upload,
    session, close, reset, kick, ping, status, nrstat, asystat, memory,
    ps, login, smtp. Ein Text je Zeile, aber jeder will nachgesehen
    werden - ein falscher Usage-Text ist schlechter als keiner.

------------------------------------------------------------------------
EINE STELLE FUER DIE PID-NAMEN (Thomas, 2026-08-18) - ERLEDIGT
------------------------------------------------------------------------
Sein Einwand beim Zusehen: "da man manchmal pid= angibt, und ich glaube
da haben wir nicht immer ein Mapping auf die AX.25-PIDs." Er hatte recht,
und es war schlimmer als eine fehlende Bequemlichkeit - es gab VIER
Stellen mit vier verschiedenen Antworten:

  listen ax25 add pid=      strtol, nur Zahlen
  listen ax25 drop pid=     strtol, nur Zahlen, und nur nach I/UI
  service socket, listen    strtol, nur Zahlen
  connect --pid             strtol, nur Zahlen; "flextalk" davor als
                            einziger Name, fest verdrahtet
  Anzeige "listen"          Spalte "text" oder "pid" - also nie, WELCHE
  AX25_PID an ein Programm  das Wort "pid", was einem Programm gar nichts
                            sagt und der eigenen Doku widersprach, die
                            "text oder 0xcf" verspricht

Jetzt eine Tabelle in pidfilter.c, zwei Funktionen darum (pid_number(),
pid_name()) und alle sechs Stellen gehen hindurch. Dazu "ax25 pid-info",
das die Tabelle druckt oder einen Eintrag aufloest - "ax25 pid-info 05",
"ax25 pid-info 0x1f", "ax25 pid-info netrom".

DIE UNBEKANNTEN ZAHLEN NICHT MIT "unbekannt" ABTUN, und das ist die eine
inhaltliche Entscheidung darin: die Nummern sind AX.25 2.2 Bild 3.1, und
eine Zahl, die dort fuer nichts steht, ist auf dem Draht trotzdem
gueltig. Bei gesetzten Bits 5-4 (01 oder 10) sagt die Norm ausdruecklich
"AX.25 layer 3 implemented" - das wird angezeigt, statt zu schweigen.

NICHT VERALLGEMEINERT, und der Grund gehoert dazu: das Protokollwort VOR
dem Ziel eines connect bleibt auf "flextalk" beschraenkt (nur die Zahl
kommt jetzt aus derselben Tabelle). "arp" und "lq" sind Woerter, die
setcall() als Rufzeichen annimmt, und das Ziel steht an genau derselben
Stelle der Zeile. Fuer alles andere gibt es "--pid", das jetzt Namen
nimmt.

NEBENBEFUND BEIM UMBAU: axspawn_fd() entschied aus dem Protokollwort
("netrom"), ob es eine NET/ROM-Sitzung ist. Sobald das Wort der echte
PID-Name ist, waere ein AX.25-Listener mit pid=netrom fuer eine
NET/ROM-Sitzung gehalten worden und haette PROTOCOL=NET/ROM und
NETROM_NODE gesetzt statt AX25_DEST. Die Welt kommt jetzt als eigenes
Argument, nicht aus einem Namensvergleich.

------------------------------------------------------------------------
FLEXNET: EIN UMGEKEHRTER SSID-BEREICH WIRD ANGENOMMEN (Befund, 2026-08-18)
------------------------------------------------------------------------
Beim Messen des PID-Gatters fiel Thomas eine Zeile auf: "DB0AAA-5-0". Er
hat die Notation selbst erkannt - das ist FlexNets SSID-BEREICH, min-max,
und den gibt es im Protokoll wirklich (etwa -5 bis -15). WAMPES schreibt
ihn in sprintflexcall(): beide Null -> nur das Rufzeichen, gleich -> "-N",
verschieden -> "-min-max".

Dahinter steckt aber ein echter Befund. Die beiden Haelften kommen aus
verschiedenen Quellen:

  min   die SSID, unter der der Peer die AX.25-Verbindung aufgebaut hat
  max   die Ziffer aus seinem FLEX_INIT (recv_init, flexnet.c:1106) -
        "Max SSID", so nennt es auch flexnet_dump()

Sagt einer max < min, entsteht ein LEERER Bereich, und niemand prueft das.
addrmatch() verlangt dann SSID >= 5 UND SSID <= 0:

    return ((*axcall & SSID) >= (flexcall[0] & SSID) &&
            (*axcall & SSID) <= (flexcall[1] & SSID));

Das kann nie zutreffen. Der Peer steht in der Zieltabelle, wird anderen
Knoten in FLEX_ROUT angekuendigt - und ist ueber uns nicht erreichbar,
denn find_dest() findet ihn fuer keine einzige SSID. Ein stiller Fehler:
das Verzeichnis sieht richtig aus, die Vermittlung geht nicht.

Aufgefallen ist es, weil testtools/flexpeer.py in seinem FLEX_INIT eine
0 schickt, sich aber als DB0AAA-5 verbindet - also genau dieser Fall. Ein
echtes FlexNet duerfte das nicht tun; darauf verlassen sollte man sich
nicht.

ZU ENTSCHEIDEN, absichtlich nicht nebenbei erledigt - es ist eine
Verhaltensaenderung im Routing:
  a) max < min auf min anheben. Der Bereich ist dann "nur er selbst",
     was der Wahrheit am naechsten kommt: unter dieser SSID hat er ja
     tatsaechlich angerufen.
  b) den Peer gar nicht erst zum Ziel machen und es im Trace vermerken.
  c) so lassen und nur dokumentieren.
Ich neige zu (a): es macht aus einem unerreichbaren Eintrag einen
erreichbaren, ohne etwas zu erfinden. Aber das ist Thomas' Entscheidung.

------------------------------------------------------------------------
FLEXNET: WELCHE SSIDs WIR VON UNS ANKUENDIGEN (Thomas, 2026-08-18)
------------------------------------------------------------------------
Aus der Frage nach "DB0AAA-5-0" gewachsen, und die eigentliche Frage ist
groesser: wir kuendigen uns als GENAU EINE SSID an.

WIE DER BEREICH ENTSTEHT, zwei Quellen und nur eine steht im Rahmen:

  min   die SSID, unter der die AX.25-Verbindung laeuft
  max   die Ziffer aus FLEX_INIT (send_init/recv_init, flexnet.c)

Das Feld ist ein Oktett, "0" + SSID, also ":;<=>?" fuer 10 bis 15.
flexnet_dump() nennt es "Max SSID".

GEMESSEN (2026-08-18): Knoten mit "ax25 mycall DL9SAU-1" und "ifconfig
axip linkaddress DL9SAU-11", ein Peer ruft DL9SAU-11 an. Der Knoten
schickt FLEX_INIT b'0;  !\r' - ';' ist "0"+11. Also die SSID DES LINKS,
nicht die von Mycall. Der Nachbar rechnet min=11, max=11 und traegt
DL9SAU-11-11 ein.

WAS DAS KOSTET: alles andere, worauf wir hoeren - andere Portrufzeichen,
"listen"-Eintraege, weitere SSIDs von Mycall - ist im FlexNet unsichtbar.
Ueber einen Link als DL9SAU-11 ist DL9SAU-8 nicht erreichbar. Und es gibt
keinen zweiten Weg: FLEX_ROUT traegt min und max je Ziel ausdruecklich
mit, aber wir tragen uns selbst nie in Dests ein und erscheinen deshalb
nie in unserem eigenen FLEX_ROUT. FLEX_INIT ist der einzige Kanal fuer
unseren eigenen Bereich.

THOMAS' VORSCHLAG: den Bereich aus unseren Interface-Rufzeichen UND den
ax25-Listenern herleiten, plus eine Konfigurationsoption fuer den Fall,
dass die Automatik nicht passt - "ich bin DL9SAU-0-4 und DL9SAU-8".

KORREKTUR AN MIR SELBST (nach Thomas' Einwand). Ich hatte daraus eine
"harte Grenze" gemacht: das Minimum sei nicht waehlbar, weil es die SSID
des Links ist. Das steht so im Code, ist aber viel weniger schlimm, als
ich es dargestellt habe - und Thomas hat den Grund genannt: NORMALE
KNOTEN UND REPEATER LAUFEN AUF -0. Dann IST das Minimum 0, und genau das
will man. Er erinnert sich, dass sich solche Knoten frueher als 0-15 im
FlexNet gemeldet haben.

WAS DAS OKTETT IST, von Thomas beantwortet und damit keine Auslegung
mehr: es ist die STOP-SSID, und mehr laesst das FlexNet-Protokoll an
dieser Stelle nicht zu. Der START ist das Rufzeichen, unter dem sich das
System meldet - also die SSID des Links. Genau deshalb sieht die Liste
so aus, wie sie aussieht:

    DB0AVH-0  Stop 3    der RMNC
    DB0AVH-4  Stop 15   das XNET, das die weiteren Ports intern
                        weiterreicht - eine Art Proxy

Zwei Systeme, zwei Rufzeichen, je eine Spanne. Ein Standort deckt damit
0-15 ab, ohne dass irgendwer zwei Bereiche in einem Rahmen unterbringen
muesste. Die EMPFANGSSEITE von WAMPES macht es genau so und ist richtig.

THOMAS' KORREKTUR, und sie macht den Bau groesser statt kleiner: der
START IST EINE EIGENSCHAFT DES KNOTENS, nicht des Links. Seine Begruendung
laesst keine andere zu: haette ein Knoten auf einem Link die SSID -4 und
auf einem anderen -6, entstuenden im Netz zwei Ankuendigungen,
DB0AAA-4-10 und DB0AAA-6-10. Das gaukelt zwei Stationen vor und spamt die
Nodeliste. Nach aussen, ueber welchen Link auch immer, ist ein Knoten
EINDEUTIG - nur so wird er ueber den besten Pfad gefunden.

GEMESSEN, DASS WAMPES GENAU DAS TUT (2026-08-18). Zwei axip-Ports,
DL9SAU-4 und DL9SAU-6, je ein FlexNet-Partner:

    Link A   DL9SAU-4->DB0AAA-5   FLEX_INIT b'04  !\r'   -> DL9SAU-4-4
    Link B   DL9SAU-6->DB0BBB-5   FLEX_INIT b'06  !\r'   -> DL9SAU-6-6

Zwei Eintraege fuer einen Knoten, mit verschiedenen Delays. Die Ursache
steht in build_path(): axroute() waehlt das Interface und STEMPELT DESSEN
RUFZEICHEN UEBER DIE QUELLADRESSE, und setaxp() uebergibt kein opts, gibt
also nichts zurueck. Der Bereich ist damit eine Eigenschaft des Links
geworden, wo er eine des Knotens sein muesste.

UND EINE REGEL DAZU, aus Thomas' Liste: Bereiche fuer ein Rufzeichen
UEBERLAPPEN NIE. Luecken gibt es (OE2XZR 0-10, 11-11, 13-13 - 12 und
14-15 fehlen), Ueberlappungen nicht. Damit ist eine AUTOMATISCHE
Herleitung des Maximums erledigt: ein selbst errechneter Wert koennte in
den Bereich eines Nachbarsystems am selben Standort hineinlaufen. Welcher
Bereich uns zusteht, weiss nur der Sysop.

DIE SENDESEITE IST DIE SCHWACHE, und zwar unabhaengig von der Lesart.
send_init() setzt UNSERE EIGENE SSID in das Oktett. Gemessen
(2026-08-18), Knoten als DL9SAU ohne SSID:

    FLEX_INIT  b'00  !\r'      -> Ziffer 0

Also kuendigen wir an, dass wir GENAU SSID 0 bedienen - "0-0" unter
beiden Lesarten. Ein FlexNet-Repeater auf -0 meldete 0-15. Damit ist
DL9SAU-8 oder DL9SAU-4 im FlexNet nicht erreichbar, und das ist kein
Auslegungsstreit, sondern faellt in beiden Lesarten gleich aus.

WAS DAS FUER DEN ENTWURF HEISST: die Aenderung gehoert auf die
Sendeseite, sie ist EIN Oktett, und fuer den Normalfall braucht sie die
Protokollfrage gar nicht beantwortet - laeuft der Link auf SSID 0, sagen
beide Lesarten min=0, und strittig ist nur das Maximum, das wir setzen.
Erst bei einem Link unter SSID != 0 muss man wissen, was die Gegenseite
als min nimmt.

Was bleibt: zwei GETRENNTE Bereiche ("0-4 und 8") sind ueber einen Link
nicht sagbar, denn das Protokoll kennt an dieser Stelle nur die
Stop-SSID. Dafuer braucht es zwei Links unter zwei Rufzeichen - so wie
DB0AVH und DB0BLO es machen.

UND EINE PFLICHT KOMMT MIT DER ANSAGE: eine Stop-SSID ist ein
VERSPRECHEN. Wer 0-4 ankuendigt, muss unter DL9SAU-1 bis -4 auch etwas
anzubieten haben; XNET loest das mit seinem internen Weiterreichen. WAMPES
hat kein solches Proxy - ein Connect auf eine angekuendigte SSID muss auf
ein Portrufzeichen oder einen "listen"-Eintrag treffen, sonst laeuft er
ins Leere. Das ist das Hauptargument GEGEN eine automatische Herleitung
und FUER eine Angabe des Sysops: nur er weiss, was hinter den SSIDs
steht, die er verspricht.

SEINE XNET-LISTE STUETZT DAS, und er hat den Grund dazu gegeben: dort
stehen DB0AAT 0-9 und DB0AAT 10-15, DB0AVH 0-3/4-15, DB0WAL 0-6/7-12 -
jeweils mit VERSCHIEDENEN Delays. Das ist UNTERSCHIEDLICHE INFRASTRUKTUR
an einem Standort unter einem Rufzeichen: bei DB0BLO ist 0-3 das
FlexNet-RMNC und 4-15 das XNET. Bei DB0AVH dasselbe, mit derselben
Trennung 0-3 / 4-15. Zwei Standorte mit derselben Aufteilung sind kein
Zufall, sondern eine Konvention: RMNC bekommt 0-3, XNET 4-15. Zwei
Systeme, zwei Links, je ein Bereich - und nicht ein Knoten, der zwei
Bereiche ueber einen Link ankuendigt.

Ein Repeater auf -0, der Stop 15 sendet, meldet sich als 0-15 - Thomas'
Erinnerung, und sie passt widerspruchsfrei dazu. Zu tun ist also nichts
an der Empfangsseite, sondern an send_init().

VARIANTEN, wenn die Messung vorliegt:

WAS DEM ENTWURF JETZT IM WEG STEHT, und es sind zwei Sachen:

  1. EINGEHENDE LINKS. Ruft uns jemand unter DL9SAU-6 an, antworten wir
     unter DL9SAU-6 - build_path() im reverse-Zweig nimmt hdr->dest, und
     etwas anderes waere auch falsch, der Anrufer wuerde die Antwort
     nicht wiedererkennen. Ueber einen eingehenden Link koennen wir
     unseren Start also gar nicht praesentieren, jedenfalls nicht ueber
     die AX.25-Quelladresse. Solange der Start von dort kommt, ist ein
     eingehender FlexNet-Link nicht richtig anzukuendigen.

  2. FLEX_INIT HAT DREI FELDER, DIE WIR WEDER FUELLEN NOCH LESEN. Der
     Rahmen ist sechs Oktette, send_init() schickt:

         '0'                     FLEX_INIT
         '0' + ssid              die Ziffer, oben besprochen
         ' '                     ?
         ' '                     ?
         ' ' + 1                 ?
         '\r'

     Gemessen als b'04  !\r'. recv_init() liest EIN Zeichen und hoert
     auf; flexnet_dump() entschluesselt ebenfalls nur das erste ("Max
     SSID"). FlexNet zaehlt an anderen Stellen mit ' '+wert (der
     Hopcount im Query-Paket tut es), diese Felder sind also
     wahrscheinlich Zahlen: 0, 0, 1. Was sie bedeuten, steht nirgends im
     Quelltext. Eines davon KOENNTE der Start sein - dann waere Punkt 1
     gegenstandslos und der ganze Bau eine andere Sache.

  DESHALB IST DER NAECHSTE SCHRITT KEIN CODE, sondern ein Mitschnitt:
  ein FLEX_INIT von einem echten FlexNet/XNET, dazu die Frage, ob es
  ueber einen eingehenden Link anders aussieht als ueber einen
  ausgehenden. Ohne das raet man an drei Feldern herum, die man nicht
  kennt.

VARIANTEN, wenn der Mitschnitt vorliegt:

  a) Nur das max setzen, je Port:
        ifconfig <iface> flexssid <max>
     Ein Oktett, und es aendert nichts daran, unter welchem Rufzeichen
     wir anrufen. Auf einem -0-Knoten ist das genau die Ansage "ich
     bediene 0 bis N", also das, was fehlt. Deckt "0-4" ab, nicht
     "0-4 und 8".
     Vorgabe: das heutige Verhalten (die eigene SSID), damit sich bei
     niemandem etwas aendert, der nichts konfiguriert. Ob eine bessere
     Vorgabe automatisch herzuleiten waere - das Maximum ueber die
     SSIDs, auf die wir tatsaechlich hoeren (Portrufzeichen, listen,
     Mycall) - ist zu entscheiden. Dagegen spricht, dass ein Bereich
     0..N behauptet, wir bedienten ALLES dazwischen, was wir vielleicht
     nicht tun; FlexNet-Bereiche sind aber ohnehin grob (siehe
     DB0AAT 0-9 in Thomas' Liste).
  b) Der Bereich als Eigenschaft des KNOTENS, eine Angabe fuer alle
     Links:
        flexnet ssid <start>-<stop>
     Sie taete zweierlei: die Quelladresse jedes FlexNet-Links, den wir
     aufbauen, ist unser Rufzeichen mit <start> - auf JEDEM Port, damit
     im Netz ein Eintrag steht und nicht einer je Port -, und FLEX_INIT
     traegt <stop>. Das ist nach Thomas' Korrektur die einzige Variante,
     die das Problem loest; (a) allein wuerde die Phantomeintraege
     lassen und nur ihre Obergrenze verschieben.
     Offen bleibt Punkt 1 oben: eingehende Links. Und die Aenderung
     greift in setaxp() ein, das heute kein opts uebergibt.
     Das ist genau das, was DB0BLO und DB0AVH tun: die RMNC-Haelfte auf
     -0 mit Stop 3, die XNET-Haelfte auf -4 mit Stop 15.
  c) Mehrere Bereiche: nur ueber mehrere Links, also mehrere
     "flexnet link add" mit verschiedenen eigenen Rufzeichen. Das
     braucht eine Quelladresse JE PEER, die es heute nicht gibt
     (setaxp() laesst hdr.source leer und ueberlaesst sie dem Routing).

     Das waere zugleich die Antwort auf die Automatik: aus den
     Portrufzeichen und den Listenern laesst sich ableiten, WELCHE SSIDs
     wir bedienen - aber ansagen koennen wir je Link nur eine
     zusammenhaengende Spanne, die bei unserer eigenen anfaengt.

SIEHE AUCH den Abschnitt oben ueber den umgekehrten Bereich (max < min):
solange niemand prueft, macht eine falsche Ziffer aus einem Eintrag
einen, den addrmatch() nie trifft.

------------------------------------------------------------------------
FLEXNET-SSID, ZWEITER DURCHGANG: WAS WIR BEDIENEN IST KEIN BEREICH
(Thomas, 2026-08-18)
------------------------------------------------------------------------
DAS DRAHTFORMAT IST BESTAETIGT. Thomas hat einen XNET-Mitschnitt
geliefert, FLEX_ROUT von DB0BLO-4 an IGATEB:

    3DB0AVH4?301 DB0BLO03301 IGATE 0?301 DB0AVH03306 \r

Mit WAMPES' eigenem Format gelesen (send_rout/pullflexcall):

    DB0AVH   4-15  delay 301
    DB0BLO    0-3  delay 301
    IGATE    0-15  delay 301
    DB0AVH    0-3  delay 306

Passt Byte fuer Byte: sechs Zeichen Rufzeichen mit Leerzeichen
aufgefuellt ("IGATE "), zwei SSID-Oktette als "0"+wert, also "?" fuer 15,
dann die Verzoegerung dezimal und ein Leerzeichen. An FLEX_ROUT ist
nichts zu aendern.

UND XNET KUENDIGT SICH SELBST NICHT AN. In dem Rahmen steht DB0BLO 0-3 -
die RMNC-Haelfte desselben Standorts, also ein NACHBAR -, aber nicht
DB0BLO 4-15, der Absender selbst. Genau wie WAMPES: der eigene Bereich
reist ausschliesslich ueber FLEX_INIT. Das schliesst die naheliegende
Abkuerzung aus, sich selbst mehrfach in FLEX_ROUT zu setzen, um mehrere
Bereiche anzukuendigen - kein anderer tut das, und ob es angenommen
wuerde, weiss niemand.

NEBENBEI, aus dem Speedtest im selben Mitschnitt: DB0BLO-4 pollt
DB0BLO-0 mit den 201 Leerzeichen und bekommt "10\r" - FLEX_RPRT mit
Delay 0. WAMPES sendet dort nie 0 (recv_poll schickt DEFAULTDELAY, wenn
locdelay 0 ist) und ignoriert eine empfangene 0 (recv_rprt). Kein
Fehler, nur eine Ungleichheit: von XNET lernen wir "praktisch keine
Verzoegerung" nicht, sondern messen selbst.

WAS "ax25 mycall" IST, nachgelesen auf Thomas' Frage:

  * WAMPES hoert NICHT auf Mycall. ismyax25addr() vergleicht mit den
    hwaddr der INTERFACES; ax_answers_to() nimmt zusaetzlich die
    Rufzeichen, unter denen eigene Links geoeffnet wurden, und die
    "listen"-Eintraege.
  * Mycall kommt nur dadurch hinein, dass JEDES AX.25-Attach es beim
    Anlegen in ifp->hwaddr kopiert - kiss.c:56, axip.c:329,
    sixpack.c:110, nrs.c:50, krnlif.c:614, ethertap.c:471. Deshalb
    erben bei db0sao alle Ports db0sao-0, ohne dass eine Zeile das sagt.
    "ifconfig <if> linkaddress" ueberschreibt hinterher.
  * DIE REIHENFOLGE IN net.rc IST DAMIT BINDEND: "ax25 mycall" muss vor
    den "attach"-Zeilen stehen. domycall() setzt nur die globale
    Variable und ruehrt bestehende Interfaces nicht an.
  * Mycall ist NICHT die Quelle ausgehender Connects.
    ax25_parse_target() setzt sie als Platzhalter, aber axroute() in
    build_path() stempelt das Rufzeichen des gewaehlten INTERFACES
    darueber; nur --mycall / "< CALL" (opts->ownsource) haelt dagegen.
    Dass es meist gleich aussieht, liegt am Erben beim Attach.
  * KLEINER BEFUND: netrom haengt anders daran. mynode->call = Mycall
    ist ein ZEIGER auf die globale Variable (netrom.c:295). Ein
    "ax25 mycall" zur Laufzeit benennt also den NET/ROM-Knoten um,
    waehrend die Ports ihr altes Rufzeichen behalten. In net.rc faellt
    das nie auf, interaktiv ist es eine Falle.

DARAUS FOLGT DAS EIGENTLICHE PROBLEM, und Thomas hat es benannt: was wir
bedienen, IST KEIN ZUSAMMENHAENGENDER BEREICH. Sein Beispiel - Ports auf
SSID 1, 2, 3, die Mailbox als listen auf 8, convers auf 11, und
mycall DL9SAU also -0 fuer jeden Port ohne eigenes linkaddress - ergibt
die Menge {0,1,2,3,8,11} und damit drei Laeufe:

    DL9SAU-0-3      DL9SAU-8      DL9SAU-11

FlexNet kann je Link EINEN Bereich ankuendigen. Drei Laeufe brauchen
drei Ankuendigungen, und der einzige beobachtete Weg dahin sind mehrere
SYSTEME unter mehreren Rufzeichen (DB0AVH: RMNC 0-3, XNET 4-15). Ein
WAMPES mit einem Interlink kann das nicht nachbauen.

DIE MENGE IST ABER BERECHENBAR, und das ist der brauchbare Teil: die
Interface-hwaddr plus die listen-Eintraege, beides steht im Knoten. Ein
Befehl, der sie in maximale Laeufe zerlegt und anzeigt, aendert nichts
auf dem Draht und liefert genau die Eingabe, die die Entscheidung
braucht:

    flexnet ssid            zeigt: bedient 0-3, 8, 11; angekuendigt 0-0
    flexnet ssid 0-3        der Sysop sagt, welcher Lauf angekuendigt wird

Das waere der erste Schritt, wenn Thomas ihn will - er ist folgenlos.
Der zweite (die Quelladresse aller FlexNet-Links auf <start> zwingen,
FLEX_INIT auf <stop>) braucht weiterhin den FLEX_INIT-Mitschnitt, wegen
der drei ungelesenen Felder und wegen der eingehenden Links.

------------------------------------------------------------------------
FLEXNET: WAS DER ZWEITE MITSCHNITT SAGT (2026-08-18)
------------------------------------------------------------------------
Zwei FLEX_ROUT von DB0BLO-4 an IGATEB, kurz nacheinander:

    DB0AVH  4-15  delay 301  ->  2
    DB0BLO   0-3  delay 301  ->  2
    IGATE   0-15  delay 301  ->  2
    DB0AVH   0-3  delay 306  ->  7

ALLE VIER FALLEN UM EXAKT 299. Das ist punktgenau die Form von WAMPES'
eigener Umrechnung in recv_rprt():

    if (delay != olddelay)
        for (...) if (pq->peer == pp && pq->delay)
            pq->delay = pq->delay - olddelay + delay;

Aendert sich die Verzoegerung EINES NACHBARN, verschieben sich alle
Ziele ueber ihn um die Differenz. XNET macht dasselbe, und seine
Schaetzung fuer einen Link ging von 300 auf 1 - 301 = 300+1,
306 = 300+6. Der Uebergang von "noch nicht gemessen" zu "gemessen".

DARAUS EINE INTEROP-FRAGE, am naechsten Mitschnitt zu pruefen: WAMPES'
"keine Ahnung"-Wert ist DEFAULTDELAY 600 (Einheit 100 ms, also 60 s),
und recv_rprt() verwirft eine EINGEHENDE 600 ausdruecklich:

    if (delay > 0 && delay != DEFAULTDELAY) { ... uebernehmen ... }

Ist XNETs Platzhalter 300, greift diese Pruefung gegen XNET nie: wir
naehmen 30 s als echte Messung und glaetteten sie ein. Und unsere 600
koennte umgekehrt bei XNET als echte 60 s ankommen. Zu klaeren, bevor
jemand an den Werten dreht - es ist kein Fehler, den man sieht, sondern
einer, der Wege unattraktiv macht.

ZU "IGATE": nach den Bytes sind das ZWEI Rufzeichen. Das AX.25-Ziel ist
IGATEB (sechs Buchstaben), der FlexNet-Eintrag ist 49 47 41 54 45 20,
also "IGATE " - fuenf plus Fuell-Leerzeichen. Dann schickt DB0BLO-4
nicht den Partner zurueck, sondern eine dritte Station, und die
Split-Horizon-Frage stellt sich nicht. Waeren es doch dieselben, waere
es ein echter Unterschied zu uns: send_rout() ueberspringt den
Empfaenger ausdruecklich (if (addreq(pd->call, pp->call)) continue;).

WAS DER MITSCHNITT NICHT BEANTWORTET: FLEX_INIT. Die drei ungelesenen
Felder und die eingehenden Links stehen weiter offen.

------------------------------------------------------------------------
FLEXNET-SSID: DIE FRAGE IST BEANTWORTET (Thomas, 2026-08-18)
------------------------------------------------------------------------
Drei Auskuenfte von Thomas schliessen sie, ohne dass ein FLEX_INIT-
Mitschnitt noch noetig waere.

1. IN FLEX_INIT IST KEIN PLATZ FUER EIN RUFZEICHEN. Der Rahmen ist sechs
   Oktette - "0", die Ziffer, zwei Leerzeichen, " "+1, CR. Ein Call
   passt da nicht hinein. Also KANN der Start nicht im Rahmen stehen,
   und es bleibt nur die AX.25-Quelladresse des Links. Damit ist die
   Lesart entschieden, ohne Raten an den drei Feldern.

2. FLEXNET SELBST KONFIGURIERT GENAU EINE ZAHL. Aus seiner autoexec.net:

       ro fl pa ssid 15

   Die Stop-SSID, node-weit, ein Parameter. Kein Start - der ist das
   eigene Rufzeichen. Genau das Modell, das oben aus der Nodeliste
   erschlossen wurde, hier als Konfigurationszeile.

   Die Partner stehen daneben, je Port:

       rou flex add 1 db0blo
       rou flex add 2 igate
       rou flex add 3 igateb
       rou flex add 4 db0avh-4

   Ohne SSID heisst -0; db0avh-4 traegt seine, weil das XNET dort unter
   -4 laeuft. Das Partnerrufzeichen IST der Start des Partners.

3. SPLIT HORIZON GILT BEIDSEITIG. Die Verbindung ging zu IGATEB, und
   IGATEB kommt in der Ankuendigung an IGATEB nicht vor; umgekehrt sah
   Thomas kein IGATE in der Gegenrichtung. IGATE und IGATEB sind zwei
   Knoten desselben Projekts an verschiedenen Standorten. WAMPES macht
   es identisch (send_rout: if (addreq(pd->call, pp->call)) continue;).
   Kein Unterschied, nichts zu tun.

WARUM MAN DEN EIGENEN BEREICH IM TRACE NICHT SIEHT, und das ist kein
Monitoring-Fehler: ein Knoten kuendigt sich nie in FLEX_ROUT an. Sein
Bereich reist ausschliesslich in FLEX_INIT, und das kommt genau einmal,
beim Linkaufbau. Auf einem stehenden Link ist nichts zu sehen. Wer es
mitschneiden will, muss einen FlexNet-Link neu hochkommen lassen.

DAMIT STEHT DER BAU FEST:

    Start   das Rufzeichen, unter dem ALLE unsere FlexNet-Links laufen -
            eines fuer den Knoten, nicht eines je Port. Heute stempelt
            axroute() das Rufzeichen des Interfaces darueber
            (build_path, non-reverse-Zweig), und setaxp() uebergibt kein
            opts, gibt also nichts zurueck. Gemessen: zwei Ports mit
            DL9SAU-4 und DL9SAU-6 ergeben zwei Ankuendigungen,
            DL9SAU-4-4 und DL9SAU-6-6.
    Stop    eine Zahl fuer den Knoten, wie FlexNets "ro fl pa ssid".
            Heute setzt send_init() dort unsere eigene SSID, also
            Start == Stop.

VORSCHLAG, nach FlexNets eigenem Vorbild geformt:

    flexnet ssid                  zeigt an: bedient 0-3, 8, 11;
                                  angekuendigt DL9SAU-0-0
    flexnet ssid <start>-<stop>   beides setzen, node-weit

Die Anzeige allein ist folgenlos und laesst sich sofort bauen: die
bedienten SSIDs stehen im Knoten (Interface-hwaddr plus
listen-Eintraege) und zerfallen in maximale Laeufe. Erst das Setzen
aendert etwas auf dem Draht.

EINGEHENDE LINKS, jetzt beantwortbar: in FlexNet hat ein Knoten EIN
Rufzeichen fuer seine FlexNet-Links, und die Partner sind mit genau
diesem Rufzeichen konfiguriert. Ein eingehender FlexNet-Link auf eine
andere SSID von uns ist demnach kein Fall, den das Protokoll vorsieht,
sondern eine Fehlkonfiguration auf der Gegenseite. WAMPES sollte dann
nicht schweigen: ein Hinweis im Trace ist billiger als ein Bereich, der
im Netz falsch steht.

WAS OFFEN BLEIBT, und es blockiert nichts mehr:
  * Die drei Konstanten in FLEX_INIT (" ", " ", " "+1). Kein Rufzeichen,
    also fuer die SSID-Frage ohne Belang. Was sie sind, waere trotzdem
    gut zu wissen.
  * Der "keine Ahnung"-Wert. Unserer ist DEFAULTDELAY 600 (60 s, die
    Einheit ist eine Zehntelsekunde einfacher Laufzeit), und recv_rprt()
    verwirft eine eingehende 600. Thomas' Trace legt bei XNET 300 nahe -
    dann greift unsere Pruefung gegen XNET nie und wir naehmen 30 s als
    Messung. Er hat XNET getract und weiss nicht, ob der RMNC es anders
    haelt. Zu klaeren, bevor jemand an den Werten dreht.

------------------------------------------------------------------------
FLEXNET: DIE XNET-KONFIGURATION, EINS ZU EINS (Thomas, 2026-08-18)
------------------------------------------------------------------------
Thomas hat die autoexec.net von DB0BLO-4 geliefert. Sie beantwortet den
Rest und macht den Bau kleiner, nicht groesser.

    my call db0blo-4                        der Start, EINE Stelle
    ro fl pa ssid 15                        der Stop, EINE Zahl
    attach ip2 axUDP 3 1 193.168.82.22      der Port zu IGATEB
    rou flex add 3 igateb                   Partner je Port; das
                                            Rufzeichen ist DESSEN Start
    ro local add 0 db0blo-5  - p            # frmaster
    ro local add 0 db0blo-8  - p            # dpbox
    ro local add 0 db0blo-9  - p
    ro local add 0 db0blo-10 - p            # linux
    ro local add 0 db0blo-11 - p            # convers
    ro local add 0 db0blo-12 - p            # wconvers
    ro local add 0 db0blo-13 - p
    ro local add 0 db0blo-14 - p
    ro local add 7 db0blo-15 - p            # rpc am rmnc

"ro local add" ist genau unser "listen ax25 add": eine SSID und was
dahinter steckt.

UND DIE LISTE HAT LUECKEN: 6 und 7 kommen nicht vor, angekuendigt wird
trotzdem 4-15. Ein Connect auf DB0BLO-6 ueber FlexNet laeuft also ins
Leere. Damit war meine Warnung weiter oben ("eine Stop-SSID ist ein
Versprechen, und WAMPES hat kein Proxy") ZU STRENG: der Bereich ist eine
ZUTEILUNG, die der Sysop erklaert, keine Ableitung aus dem, was
tatsaechlich laeuft. Es gibt nichts abzuleiten, es gibt nur zu erklaeren
- also genau die Konfigurationsoption, die Thomas gleich zu Anfang
vorgeschlagen hatte.

DIE ABBILDUNG AUF WAMPES IST DAMIT EINS ZU EINS:

    my call db0blo-4           ax25 mycall            schon da, node-weit
    ro fl pa ssid 15           -                      FEHLT
    ro local add 0 ...-8       listen ax25 add ...-8  schon da
    rou flex add 3 igateb      flexnet link add       schon da

Es fehlt genau eine Zahl.

UND DER START IST IM NORMALFALL SCHON RICHTIG. Ohne "ifconfig <if>
linkaddress" erben alle Ports Mycall (jedes Attach kopiert es), jeder
Link laeuft unter demselben Rufzeichen, es entsteht EIN Eintrag. Die
gemessenen Phantomknoten brauchten zwei Ports mit EIGENEN Rufzeichen -
eine WAMPES-Faehigkeit, die FlexNets Modell nicht kennt. Der Bau ist
also: fuer FlexNet-Links die Quelladresse an das FlexNet-Rufzeichen des
Knotens binden statt an das Interface. Das Werkzeug ist da -
struct ax25_opts { iface, ownsource }, und ax_answers_to() erkennt eine
so geoeffnete Quelle wieder (die Schleife ueber Ax25_cb).

ZU BAUEN, jetzt vollstaendig bestimmt:
  * "flexnet ssid" ohne Argument: was wir bedienen (Interface-hwaddr
    plus listen-Eintraege, in maximale Laeufe zerlegt) und was wir
    ankuendigen. Folgenlos.
  * "flexnet ssid <start>-<stop>": Start ist das Rufzeichen, unter dem
    setaxp() FlexNet-Links oeffnet (ueber opts, ownsource gesetzt);
    Stop geht in send_init(). Vorgabe: das heutige Verhalten.
  * Ein Hinweis, wenn ein eingehender FlexNet-Link auf einer anderen
    SSID ankommt als unserem Start - dann steht unser Bereich beim
    Nachbarn falsch, und schweigen hilft niemandem.

------------------------------------------------------------------------
FLEXNET: DIE VERZOEGERUNG 600 IST EIN SENTINEL IM WERTEBEREICH
(Thomas, 2026-08-18)
------------------------------------------------------------------------
Sein Einwand, und er stimmt in BEIDE Richtungen:

    recv_poll():  "%c%d\r", FLEX_RPRT, pp->locdelay ? pp->locdelay
                                                     : DEFAULTDELAY
    recv_rprt():  if (delay > 0 && delay != DEFAULTDELAY) { uebernehmen }

Misst unsere Poll-Antwort zufaellig genau 600 - 120,0 s Umlaufzeit, auf
0,1 s gerundet, denn die Einheit ist eine Zehntelsekunde EINFACHER
Laufzeit -, senden wir 600, und die Gegenseite liest "keine Ahnung",
obwohl gemessen wurde. Umgekehrt verwerfen wir eine echte 600 vom
Partner. MAXDELAY ist 3000, 600 ist also ein voellig legaler Messwert.
Selten, aber auf einem so langsamen Pfad zappelt der Wert um die Grenze
und die Verbindung wechselt zwischen "gemessen" und "unbekannt".

DIE 0 HAT DASSELBE PROBLEM VON DER ANDEREN SEITE. "delay > 0" verwirft
sie, aber XNET BENUTZT SIE ALS ECHTEN WERT: im Speedtest-Mitschnitt
antwortet DB0BLO-4 dem DB0BLO-0 mit "10\r", also FLEX_RPRT Delay 0 fuer
den Link am selben Standort. Von XNET nehmen wir "praktisch keine
Verzoegerung" also nie an. Der praktische Schaden ist gering, weil
unsere eigene Poll-Messung einspringt (und locdelay wird auf mindestens
1 angehoben, unsere eigene 0 kann also nicht entstehen) - sauber ist es
nicht.

DIE BILLIGE HAELFTE DES FIXES, ohne Protokollfrage: einen GEMESSENEN
Wert nie als Sentinel senden. Kommt locdelay auf genau DEFAULTDELAY
heraus, eine Einheit danebenlegen - eine Luege von 0,1 s ist besser als
ein falsches "unbekannt". Eine Zeile in recv_poll().

DIE ANDERE HAELFTE braucht den Mitschnitt: was XNET sendet, wenn es
nichts gemessen hat. Thomas' Trace legt intern 300 nahe (die
Verschiebung um 299), aber was auf dem Draht als "keine Ahnung" steht,
ist damit nicht gesagt. Er hat XNET getract und weiss nicht, ob der RMNC
es anders haelt.

------------------------------------------------------------------------
FLEX_INIT: WAS WAMPES BEIM EMPFANG DAMIT TUT (Thomas' Frage, 2026-08-18)
------------------------------------------------------------------------
Seine Frage, und sie ist die richtige: verarbeitet WAMPES aus einem
eingehenden FLEX_INIT die MAX-SSID des Partners, und die MIN-SSID aus
dem Rufzeichen, mit dem er verbunden hat?

JA, GENAU SO. recv_init() liest EIN Zeichen und setzt ausschliesslich
call[ALEN+1], also das Maximum:

    if ((chr = PULLCHAR(bpp)) != -1)
        pp->call[ALEN + 1] = ((chr << 1) & SSID) | 0x60;

Das Minimum steht da schon und wird nie angefasst. Es entsteht auf einem
von zwei Wegen:

  * er ruft uns an -> flexnet_input():
        addrcp(call, src); call[ALEN + 1] = call[ALEN];
    min = max = die SSID SEINER QUELLADRESSE
  * wir tragen ihn ein -> "flexnet link add DB0AAA-5" -> flexsetcall():
    min = max = 5, wie geschrieben

Danach ueberschreibt FLEX_INIT nur das max. Nebenbei tut recv_init()
zweierlei: es entscheidet den Token ueber den Vergleich der beiden
Adressen (memcmp, also hat genau eine Seite ihn) und nullt mit
clear_all_via_peer() alles, was wir ueber diesen Partner wussten - ein
frischer Linkaufbau macht das ungueltig.

SEINE SORGE, "0 15" KOENNTE EIN PAAR SEIN, ist ausgeraeumt, und zwar
durch seine eigene Konfigurationszeile statt durch unseren Quelltext:
"ro fl pa ssid 15" ist EIN Parameter. Laege im Rahmen ein Paar, gaebe es
zwei. Die Byte-Ebene passt dazu: Byte 0 ist der Funktionscode "0", Byte
1 die Ziffer; ein -0-Knoten schickt "0","0", ein -15-Knoten "0","?".
Laege an Byte 2 ein zweites SSID-Feld, muesste dort eine Ziffer stehen -
dort steht " " = 0x20, also "0" minus 16.

SEINE RECHNUNG ZUM START, und sie stimmt: nennt man sich DL9SAU-14, dann
sind mit Stop 15 genau -14 und -15 angekuendigt und 0-13 ueber
FlexNet-Routing unerreichbar. Konsistent - und ein Grund, den Start tief
zu waehlen.

DIE DREI KONSTANTEN, Thomas' Vermutung: zwei reserviert, die dritte die
Protokollversion. Das passt zu FlexNets Zahlencodierung " "+wert - im
Query-Paket ist der Hopcount genau "chr - ' '". Dann lesen sich die drei
als 0, 0, 1. Von hier nicht zu bestaetigen. Was man sagen kann: wir
senden immer 1 und lesen nie, was zurueckkommt - ginge FlexNet je auf 2,
behaupteten wir weiter 1 und merkten es nicht.

UND WOHIN "ro local add 0" ZEIGT: Port 0 ist bei DB0BLO
"attach ip0 axUDP 0 1 ... 192.168.2.110  # Verbindung zum linux digi".
Die "lokalen" Eintraege sind also gar keine internen Dienste, sondern
Weiterleitungen auf eine ANDERE MASCHINE - genau unser
"listen ax25 add <call> tcp:host:port" bzw. unix: oder ein Programm. Der
angekuendigte Bereich deckt damit Dinge ab, die im Knoten selbst nicht
existieren. Noch ein Beleg dafuer, dass er eine Zuteilung ist und kein
Versprechen.

------------------------------------------------------------------------
FLEX_RPRT: DIE ZWEITE STELLE IST DIE VERZOEGERUNG (2026-08-18)
------------------------------------------------------------------------
Thomas sah "31 30 0D" und "31 31 0D" auf einem Link und fragte, ob die
zweite Stelle etwas bedeutet. Ja - und es ist kein Feld, sondern eine
Zahl beliebiger Laenge:

    recv_poll():  sprintf(bp->data, "%c%d\r", FLEX_RPRT,
                          pp->locdelay ? pp->locdelay : DEFAULTDELAY);
    recv_rprt():  pullnumber() sammelt Ziffern bis zum ersten
                  Nicht-Ziffer-Zeichen

    31 30 0D    "1" "0"    FLEX_RPRT, Verzoegerung 0
    31 31 0D    "1" "1"    FLEX_RPRT, Verzoegerung 1
    31 33 30 31 0D         waere Verzoegerung 301

Die Falle beim Lesen: "10" heisst NICHT Verzoegerung 10, sondern "1" und
"0". Einheit wie in FLEX_ROUT, Zehntelsekunden einfacher Laufzeit.

DAMIT IST DER 0-SENTINEL BELEGT und nicht mehr nur vermutet: IGATEB
sendet echte Nullen fuer einen schnellen Link, und WAMPES' Pruefung

    if (delay > 0 && delay != DEFAULTDELAY)

wirft sie weg. Gegen einen schnellen XNET-Partner uebernehmen wir dessen
Messung nie und verlassen uns allein auf unsere eigene. Kein grosser
Schaden - unsere Messung ist da -, aber die Glaettung ueber beide Werte,
die der Code vorsieht, findet gegen XNET nicht statt.

UND ES ENGT DIE ANDERE HAELFTE EIN: die 300 aus der 299er-Verschiebung
ist XNETs INTERNER Anfangswert fuer einen ungemessenen Link. Was XNET in
FLEX_RPRT sendet, wenn es nichts gemessen hat, ist weiterhin ungesehen -
in den Mitschnitten stehen nur 0 und 1, und das sind Messungen.

------------------------------------------------------------------------
"flexnet ssid" GEBAUT - DIE ANZEIGE (2026-08-18)
------------------------------------------------------------------------
Schritt 1 von dreien, und der folgenlose: er zeigt nur, er aendert
nichts auf dem Draht.

    flexnet ssid

Zwei Zeilen nebeneinander, die es vorher nirgends zusammen gab:
was wir BEDIENEN und was wir ANKUENDIGEN.

Bedient wird ueber einen Durchlauf durch alle sechzehn SSIDs unseres
Rufzeichens, mit den beiden Quellen, die darueber entscheiden -
ismyax25addr() fuer die Portrufzeichen und axlisten_active() fuer die
Listener. Nicht durch Hineingreifen in die beiden Listen: so kann die
Anzeige nicht aus dem Tritt geraten, wenn sich dort etwas aendert. Das
Ergebnis wird in maximale Laeufe zerlegt.

Angekuendigt wird je Link, denn dort wird es heute entschieden: Start
ist die Quelladresse des Links, Stop dieselbe SSID, weil send_init() sie
daraus nimmt.

GEMESSEN, der kranke Fall - zwei Ports mit eigenen Rufzeichen, ein
Login-Listener auf -8:

    SSIDs of DL9SAU this node answers to: 4, 6, 8
    Announced, per link:
      DB0BBB-5     as DL9SAU-6   -> DL9SAU-6-6
      DB0AAA-5     as DL9SAU-4   -> DL9SAU-4-4
    Two or more ranges for one node: to the network that is 2 stations,
    not one.
    1 served SSID outside every announced range - not reachable by
    FlexNet routing.

UND DIE GEGENPROBE, dieselbe Konfiguration ohne die beiden
linkaddress-Zeilen, also der Normalfall:

    SSIDs of DL9SAU this node answers to: 0, 8
    Announced, per link:
      DB0BBB-5     as DL9SAU     -> DL9SAU-0-0
      DB0AAA-5     as DL9SAU     -> DL9SAU-0-0
    1 served SSID outside every announced range - not reachable by
    FlexNet routing.

Keine Ruege ueber zwei Stationen, denn es gibt nur eine. Uebrig bleibt
der zutreffende Hinweis auf die -8 - genau das fehlende
"ro fl pa ssid". Das war die Gegenprobe, die zaehlt: eine gewoehnliche
Konfiguration darf nicht fuer etwas gescholten werden, das sie nicht
tut.

DIE BEIDEN HINWEISZEILEN sind absichtlich verschieden scharf. Ein
Bereich mit Luecken ist gewoehnliches FlexNet (DB0BLO kuendigt 4-15 an,
-6 und -7 gibt es nirgends) - der zweite Satz nennt also nur eine
Tatsache. Zwei Bereiche fuer einen Knoten sind fuer alle anderen zwei
Stationen, und das ist ein Fehler; der erste Satz sagt es so.

OFFEN BLEIBEN SCHRITT 2 UND 3:
  2. recv_poll(): einen gemessenen Wert nie als Sentinel senden.
  3. "flexnet ssid <start>-<stop>": Quelladresse aller FlexNet-Links an
     den Start binden (opts/ownsource), Stop in send_init().

------------------------------------------------------------------------
FLEXNET: WOHER locdelay KOMMT, UND WAS KEIN ERSATZ DAFUER IST
(Thomas' Rueckfragen, 2026-08-18)
------------------------------------------------------------------------
Seine Frage: ist locdelay der Wert aus unserem AX.25-Stack oder eine
eigene Messung? Und was tun wir, solange wir auf einer neuen Verbindung
noch keine haben?

EIGENE MESSUNG, und eine absichtlich grobe:

    send_poll():  pp->lastpolltime = msclock();   /* 201 Byte Leerzeichen */
    recv_rprt():  pp->locdelay =
                      iround(TDIFF(msclock(), pp->lastpolltime) / 200.0);
                  if (pp->locdelay < 1) pp->locdelay = 1;

Halbe Umlaufzeit in 100-ms-Einheiten, mindestens 1. Die 201 Byte sind
kein Zufall: gemessen werden soll die KANALGESCHWINDIGKEIT, nicht die
Latenz eines kurzen Rahmens. Gepollt wird nur, wer permanent ist.

DAS FENSTER OHNE MESSUNG ist kurz und unvermeidlich: setaxp() schickt
beim Linkaufbau FLEX_INIT und sofort FLEX_POLL, beide Seiten pollen also
gleich. Trifft SEIN Poll ein, bevor unsere Antwort zurueck ist, haben wir
nichts und antworten mit DEFAULTDELAY. Das ist die ehrliche Verwendung
des Sentinels.

DEN AX.25-WERT STATTDESSEN ZU NEHMEN geht nicht ohne Weiteres, und die
Gruende sind messbar:
  * axp->srt misst, was gerade fliesst, nicht eine 201-Byte-Probe. Auf
    einem langsamen Kanal unterschaetzt er genau das, was FlexNet
    vergleichen will.
  * Er startet bei 0 (build_path), und die erste Aktualisierung ist
    ((axp->srt * 7) + rtt + 4) >> 3, mit srt=0 also rtt/8 - EIN ACHTEL
    der Wirklichkeit. Zu schnell ist die gefaehrliche Richtung: so ein
    Link wird bevorzugt.
  Als Startwert waere er erst nach ein paar Quittungen brauchbar. Wenn
  ueberhaupt, dann so - nicht als Ersatz.

XNETs 300 EBENSO ZU VERWERFEN: nein. Wir haben nie gesehen, dass XNET
300 SENDET; wir haben sie als seinen INTERNEN Anfangswert aus der
299er-Verschiebung in FLEX_ROUT erschlossen. Eine zweite magische Zahl,
diesmal aus fremden Interna geraten, wiederholte den Fehler, der die 600
schon ist - und 30 s ist auf einem langsamen Pfad ein legitimer Messwert,
den wir dann wegwuerfen. Weniger magische Werte, nicht mehr.

WAS ALSO ZU TUN WAERE, unveraendert und klein: auf der SENDESEITE einen
gemessenen locdelay von genau DEFAULTDELAY um eine Einheit danebenlegen.
Eine Luege von 0,1 s gegen ein falsches "unbekannt". Auf der
Empfangsseite bleibt es beim Verwerfen - die Zweideutigkeit sitzt im
Protokoll, und eine angenommene 600 kostete auf einem frischen
Partnerlink acht Poll-Zyklen (40 Minuten) Konvergenz, weil pp->delay bei
einem eingetragenen Partner sonst von der ersten echten Messung DIREKT
gesetzt wird.

------------------------------------------------------------------------
"flexnet ssid <start>-<stop>" GEBAUT (2026-08-18)
------------------------------------------------------------------------
Schritt 3, und er brauchte den FLEX_INIT-Mitschnitt am Ende doch nicht:
Thomas' Einwand, dass im Rahmen kein Rufzeichen Platz hat, und seine
Konfigurationszeile "ro fl pa ssid 15" beantworten die Frage vollstaendig.
Die drei unbekannten Oktette koennen den Start nicht tragen, also kommt er
aus der Quelladresse - und mehr musste man nicht wissen.

Gebaut ist genau das Modell von XNET, eine Angabe fuer den Knoten:

    flexnet ssid                    zeigen
    flexnet ssid <start>-<stop>     setzen
    flexnet ssid none               zurueck auf das alte Verhalten

Der Start wird das Rufzeichen, unter dem setaxp() jeden FlexNet-Link von
uns oeffnet (ax25_opts mit ownsource, weil axroute() sonst das
Interface-Rufzeichen darueberstempelt); der Stop geht in send_init().
Vorgabe -1/-1, also unveraendert.

GEMESSEN, alles am laufenden Knoten mit zwei Ports (DL9SAU-4, DL9SAU-6)
und einem Login-Listener auf -8:

    flexnet ssid 4-15
      ax25 status:  DL9SAU-4->DB0AAA-5 und DL9SAU-4->DB0BBB-5
                    (vorher -4 und -6, also zwei Stationen)
      auf dem Draht: FLEX_INIT b'0?  !\r', "?" ist "0"+15
      Anzeige:      Configured range: DL9SAU-4-15, keine Ruege

    flexnet ssid 0-15
      Anzeige warnt: der Start ist eine SSID, auf die wir nicht hoeren -
      richtig, denn die Ports tragen -4 und -6

    flexnet ssid 9-3 / 0..15 / 3-99   abgewiesen
    flexnet ssid none                 zurueck, Links behalten ihr
                                      Rufzeichen

    Gegenprobe ohne jede Zeile: unveraendert wie vorher.

ZWEI ENTSCHEIDUNGEN IM BAU, beide im Quelltext begruendet:

  * Eingehende Links bleiben unberuehrt. Wer uns anruft, erreicht das
    Rufzeichen, das er gewaehlt hat, und die Antwort muss es tragen,
    sonst erkennt er sie nicht. In FlexNet gibt es den Fall nicht - ein
    Partner ist mit dem einen Rufzeichen konfiguriert, das der Knoten
    benutzt -, also ist ein anderswo eintreffender Link eine
    Fehlkonfiguration bei ihm. Die Anzeige markiert ihn mit "he called
    this callsign", statt ihn zu verstecken.
  * KEIN erneutes FLEX_INIT an bestehende Links. Es saehe ordentlicher
    aus und ist eine Falle: recv_init() ruft am anderen Ende
    clear_all_via_peer(), er vergisst also alles, was er von uns gelernt
    hat - waehrend unser send_rout() nur AENDERUNGEN schickt, es ihm
    also nicht wieder erzaehlt. Ein Bereich, der beim naechsten
    Linkaufbau ankommt, ist besser als eine Tabelle, die still leerlaeuft.

Einzelheiten in doc/FLEXNET-SSID.md.

------------------------------------------------------------------------
OFFEN: recv_poll() UND DAS SENTINEL 600 (Thomas will es ueberdenken)
------------------------------------------------------------------------
Nicht gebaut, auf Thomas' Wunsch. Der Stand zum Nachdenken:

  * DEFAULTDELAY 600 hat ZWEI Rollen. In create_peer() ist es eine
    Annahme ("ungemessen, also unattraktiv", 60 s, absichtlich
    pessimistisch, und fuer einen neuen direkten Partner sogar sehr
    pessimistisch). In recv_poll()/recv_rprt() ist es ein SENTINEL
    ("keine Ahnung"). Eine Konstante, zwei Bedeutungen.
  * Die Zweideutigkeit auf dem Draht ist die eigentliche Sache: 600 ist
    ein legaler Messwert (MAXDELAY ist 3000), also senden wir eine echte
    Messung von 120,0 s Umlaufzeit als "keine Ahnung" und verwerfen eine
    echte 600 vom Partner.
  * Die billige Haelfte waere eine Zeile in recv_poll(): ein gemessenes
    locdelay von genau 600 um eine Einheit danebenlegen.
  * Gegen das Annehmen einer eingehenden 600 spricht die Rechnung: bei
    einem eingetragenen Partner steht pp->delay auf 0, die erste echte
    Messung setzt ihn also DIREKT. Nimmt man 600 vorher an, konvergiert
    er mit Gewicht 1/8 ueber acht Poll-Zyklen - 40 Minuten mit einem viel
    zu pessimistischen Wert.
  * XNETs 300 ebenso zu verwerfen waere falsch: das ist sein INTERNER
    Anfangswert, aus der 299er-Verschiebung erschlossen, nicht etwas, das
    wir auf dem Draht gesehen haetten.
